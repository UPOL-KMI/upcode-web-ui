# ReCodEx New Frontend — Design Decisions

**Status:** Living document
**Date:** 2026-05-11

---

## Architecture

| #       | Decision                                       | Rationale                                                                                             | Alternatives Considered                                            |
| ------- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| DEC-001 | **Next.js 16.3 App Router**                    | Required by brief. Server Components native. App Router is the future of Next.js.                     | Next.js 15 (older), Remix, SvelteKit                               |
| DEC-002 | **TypeScript strict mode**                     | Catch errors at build time, not runtime. Brief requirement.                                           | Loose TS (faster, less safe)                                       |
| DEC-003 | **pnpm over yarn/npm**                         | Strict `node_modules` catches phantom deps. Better Docker layer caching. Brief requirement.           | yarn (legacy uses it), npm                                         |
| DEC-004 | **Tailwind CSS + shadcn/ui**                   | Modern, utility-first. shadcn/ui provides accessible primitives. Vendored into repo for full control. | CSS Modules, styled-components, Chakra UI                          |
| DEC-005 | **Server Components by default**               | Brief §6.4: `"use client"` on interactive leaves only. Reduces client bundle.                         | Client-heavy SPA (legacy approach)                                 |
| DEC-006 | **httpOnly cookie + BFF for auth**             | Brief §5: token never reaches client JS. SameSite=lax for CSRF protection.                            | localStorage (legacy — insecure), sessionStorage                   |
| DEC-007 | **Two API URLs (internal + public)**           | Brief §4: separate URLs for server-side fetches and browser-facing redirects.                         | Single URL (breaks in container)                                   |
| DEC-008 | **Next-intl for i18n**                         | App Router-native, works in Server Components. cs/en both required.                                   | react-intl (legacy), i18next                                       |
| DEC-009 | **TanStack Query for client data**             | Brief §4: only for genuinely client-driven cases. Caching, deduplication, background refetch.         | SWR, Apollo Client, raw fetch                                      |
| DEC-010 | **Server Actions for mutations**               | Brief §4: Zod-validated, revalidateTag/Path. Public HTTP endpoint with nice syntax.                   | API Routes, tRPC                                                   |
| DEC-011 | **Route Handlers for file uploads**            | Brief §6.7: Server Actions have 1MB body limit. Route Handlers stream to core-api.                    | Server Actions (insufficient), direct browser-to-API (breaks auth) |
| DEC-012 | **Shiki for code display**                     | Server-side rendering, no client JS. Better than highlight.js/prism.                                  | highlight.js (legacy), prismjs                                     |
| DEC-013 | **CodeMirror 6 for editing**                   | Modern, accessible, extensible. Client-only via `next/dynamic`.                                       | Ace Editor (legacy), Monaco                                        |
| DEC-014 | **React markdown + remark-gfm + rehype-katex** | Modern, maintained. Must test compatibility with legacy markdown-it.                                  | markdown-it (legacy), MDX                                          |

---

## IA & Routing

| #       | Decision                        | Rationale                                                                                                     | Alternatives Considered                |
| ------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| DEC-015 | **No persona route groups**     | Brief §2: one person is routinely both student and teacher. Routing follows context (group), not global role. | `(student)` / `(teacher)` route groups |
| DEC-016 | **Dashboard as landing page**   | Brief §2: "what do I owe, what am I responsible for" on one screen. No unnecessary clicks.                    | Legacy home page → dashboard redirect  |
| DEC-017 | **Group-centric navigation**    | Brief §9: navigation reflects how people work, not database shape. Groups are the primary context.            | Exercise-centric, user-centric         |
| DEC-018 | **Central breadcrumb manifest** | Brief §6.12: breadcrumbs from one mechanism, async resolvers for entity names.                                | Per-page breadcrumbs (inconsistent)    |
| DEC-019 | **Deep-linkable searchParams**  | Brief §9: filters, tabs, pagination, sort in URL. Shareable state.                                            | Local state only (lost on refresh)     |
| DEC-020 | **`/dev/kitchen-sink` route**   | Brief §10: cheaper than Storybook for internal component showcase.                                            | Storybook (heavier, separate build)    |

---

## State & Data

| #       | Decision                                                                                                                                                                                                                               | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Alternatives Considered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEC-021 | **No client-side auth state**                                                                                                                                                                                                          | Auth state lives in httpOnly cookie. Server Components read from `cookies()`. Client components never see the token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Redux auth state (legacy), Context API                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-022 | **`requireSession()` on every data access**                                                                                                                                                                                            | Brief §5: authorisation boundary is the data access layer, not the page or layout.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Middleware-only auth (insecure — §6.1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-023 | **Server Actions for simple mutations (not login/logout)**                                                                                                                                                                             | Form submissions (group creation, exercise editing, etc.), Zod-validated on server. **Corrected during F-016**: this row originally also listed "Login, logout" — recon-phase error, contradicted by the brief's own §5 text ("**Login**: Route Handler receives credentials...") and by `docs/BACKLOG.md`'s own F-016/F-017 titles ("Auth BFF: login Route Handler" / "...logout Route Handler"), which the recon phase apparently wrote down correctly in one document and then contradicted in this one. Login and logout are Route Handlers — see DEC-038.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | API Routes (more boilerplate)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DEC-024 | **TanStack Query for live data**                                                                                                                                                                                                       | Evaluation progress, notifications. Polling or WebSocket-backed query.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | setInterval polling (no deduplication)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-025 | **URL state for UI preferences**                                                                                                                                                                                                       | Tab, filter, sort, page in `searchParams`. Survives refresh, shareable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | LocalStorage (not shareable), sessionStorage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DEC-026 | **`Dockerfile` lives in this repo's own root, not the compose repo's `services/<name>/`**                                                                                                                                              | The `services/<name>` pattern the other ReCodEx components use exists because their source is pulled by the compose repo's `pull-repos.sh` from `github.com/ReCodEx/*` into a shared build context. This repo isn't one of those — it's this project's own separate repo. A self-contained Dockerfile (standard practice for a standalone Next.js app) is simpler and doesn't force an artificial cross-repo COPY. The compose entry (F-004) will point its `build.context` at this repo's path instead of the compose repo root.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Mirroring `services/<name>/` anyway (rejected — adds a COPY indirection for no benefit, since there's no upstream-pull step to share)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-027 | **`outputFileTracingRoot` + `outputFileTracingIncludes` in `next.config.ts`**                                                                                                                                                          | Next's standalone-output file tracer (NFT) under-includes `@swc/helpers` with this pnpm layout — copies `cjs/` but not `esm/`, which `next`'s own `require-hook.js` needs at runtime. Diagnosed by actually running the built image (`docker run`), reading the crash, and diffing the traced output against the full pnpm store — not guessed from a GitHub issue. `outputFileTracingRoot` alone (the fix suggested for the superficially-similar "monorepo symlink" issue) did **not** fix it; `outputFileTracingIncludes: { "/**": [".../@swc/helpers/**"] }` did. Kept both — the root is harmless/recommended regardless.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `node-linker: hoisted` in `pnpm-workspace.yaml` (rejected — defeats DEC-003's strict-`node_modules` rationale for phantom-dependency detection); manually copying the missing directory in the Dockerfile as a `RUN cp` step (rejected — `next.config.ts` is the correct layer, not a build-script patch)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| DEC-028 | **`web-next` compose service publishes its own host port directly, bypassing `proxy`**                                                                                                                                                 | F-004. Operator approved this shape as shown, unedited. Simpler for side-by-side dev against the legacy `web-app` (still on `:8080` behind `proxy` on `:80`) — no `nginx.conf.template` changes needed, no risk to the working legacy path while this app is still pre-parity. Container's internal `PORT` is `3000` (matches `.env.local`/Dockerfile default); host mapping is `WEB_NEXT_PORT=3001` — `3000` was already bound by something else on the host, discovered live at `docker compose up -d` and switched without further operator round-trip (recorded here, not asked about, since it's a pure host-port renumbering with zero behavioural difference). `API_BASE_INTERNAL` changed from equal-to-public (F-006's placeholder, see PROGRESS.md) to the real internal hostname `http://api:80/v1`, now that the container is genuinely on the `recodex` network — verified reachable via `docker exec ... getent hosts api`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Routing `web-next` through `proxy` on a subpath (e.g. `/next/`) instead (rejected for now — adds `nginx.conf.template` risk to a working legacy deployment for no benefit while this app is pre-parity; revisit once cutover is closer)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DEC-029 | **`scripts/seed.ts` builds exactly one gradeable exercise, reused via many assignments**                                                                                                                                               | F-025. Creating a fully-configured, gradeable exercise via the public API is genuinely a 7-step chain (hardware-groups, environment-configs, tests, detail, supplementary file + config with per-pipeline variable discovery, limits, reference solution) with no documented shortcut — confirmed empirically against the live instance, not assumed from the swagger spec alone (which is too generic on the `tests`/`config`/`limits` shapes to build from directly; the real shapes came from reading `repos/web-app/src/helpers/exercise/configSimple.js` and `configAdvanced.js`). Since ReCodEx allows assigning the _same_ exercise to a group multiple times (verified live — two `POST /exercise-assignments` calls with identical `exerciseId`+`groupId` both succeed with distinct IDs), the pagination-filler requirement (25 assignments) is satisfied by reusing one exercise rather than repeating the 7-step build 25 times.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | One exercise per assignment (rejected — 25x the API calls and the fragile per-exercise config/variable discovery dance, for no benefit since the brief only needs _rows_, not _distinct_ exercises)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DEC-030 | **Reference solutions are required to exist (not to be evaluated) before an exercise can be assigned**                                                                                                                                 | Discovered live: `POST /v1/exercise-assignments` 400s with "Exercise does not have any reference solutions" until at least one `POST /v1/reference-solutions/exercise/{id}/submit` has been made — but the check is existence-only, it does not wait for or require a successful evaluation. Relevant given DEC-031 (evaluation is currently broken on this dev machine) — the seed script's own reference solution submission does not need to (and cannot, right now) reach a real pass/fail either.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| DEC-031 | **Real pass/fail evaluation states cannot be produced on this dev machine right now**                                                                                                                                                  | `scripts/seed.ts`'s submissions resolve to an infrastructure `evaluation_failure` ("Isolate init error. Return value: 2"), confirmed via `docker logs recodex-worker-1` showing `Checking for cgroup support for memory ... CAUTION`. This is the **same, already-documented** limitation in the compose repo's `README.md` ("Before going to production, read this: worker needs cgroup v1" — cgroup v2-only hosts, explicitly including "macOS/Docker Desktop", cannot run the vendored isolate 1.8.1 sandbox) — re-discovered while verifying F-025 end-to-end, not a regression introduced by this work. The seed script still submits logically-correct-vs-logically-wrong solutions (so the _data_ is right), but genuine pass/fail differentiation must be re-verified on a cgroup v1 host before relying on it for evaluation-state UI work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Attempting to fix Docker Desktop's cgroup mode from within this session (rejected — host/VM-level change outside this repo's scope, already investigated and documented as a production prerequisite, not a bug to patch around)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DEC-032 | **Theme tokens: Tailwind v4 CSS-first config + shadcn/ui's own variable names + `oklch` + `next-themes`**                                                                                                                              | F-010. Tailwind v4 (latest, `4.3.3` per `npm view`) replaced the JS `tailwind.config.js` with a CSS-first `@theme`/`@import "tailwindcss"` approach — installed `tailwindcss` + `@tailwindcss/postcss` (the official Next.js/Turbopack integration path) rather than the old `tailwind.config.ts` + `postcss-import` setup. Token _names_ (`--background`, `--foreground`, `--primary`, etc.) match shadcn/ui's own convention exactly, since DEC-004 already commits to vendoring shadcn/ui — using its expected variable names now means future `shadcn add` component output will just work against these tokens instead of needing a rename pass later. `oklch` is shadcn's current default color space (perceptually uniform, better interpolation); Lightning CSS (Tailwind v4's bundled CSS transform) auto-downgrades it to `lab()`/hex for older browsers — confirmed in the actual compiled `.next/static/chunks/*.css` output, not assumed. `next-themes` provides the `.dark` class toggle + a blocking anti-FOUC inline script (`attribute="class"`, `defaultTheme="system"`, `enableSystem`) — confirmed present and correctly configured in the rendered HTML (`curl` against a real `next start`), not just "the package is installed".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Tailwind v3 + JS config (rejected — not the current major version, brief's own "verify latest patch" philosophy applies to majors too here since v4 is a real breaking change, not a patch); rolling a custom light/dark mechanism instead of `next-themes` (rejected — reinvents a small, standard, well-tested package for no benefit)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DEC-033 | **i18n: `next-intl@4.13.7` with `app/[locale]/...`, `localePrefix: "always"`, `requestLocale` (not `next/root-params`)**                                                                                                               | F-011. `next-intl` per DEC-008; version/routing API verified against the actually-installed package (`node_modules/.pnpm/next-intl@.../dist/types/**`), not from memory — e.g. confirmed `defineRouting`'s default `localePrefix` is already `"always"`, and confirmed the plugin's default `i18n/request.ts` search path by reading its compiled `getNextConfig.js`. `localePrefix: "always"` (both `/en/...` and `/cs/...` explicitly prefixed, `/` redirects) rather than `"as-needed"`, matching the brief's "Czech and English, both complete, always" — neither locale is an implicit unprefixed default outranking the other. `getRequestConfig`'s `requestLocale` param is marked `@deprecated` in favor of `next/root-params`, but checked and **`next/root-params` isn't actually usable in the installed Next 16.3.1**: its files are listed in `next`'s `package.json` `"files"` array but don't exist in `node_modules` — so `requestLocale` was used anyway as the only currently-working option; revisit when root-params actually ships (brief §6 footgun 11 explicitly calls for verifying this rather than assuming). `defaultLocale: "en"` matches the legacy app's own fallback (`repos/web-app/src/locales/index.js`'s `getDefaultLang()` falls back to `'en'` when `navigator.language` isn't `cs`/`en`). Root route structure: `app/[locale]/layout.tsx` **is** the root layout (renders `<html>`/`<body>`) — no separate top-level `app/layout.tsx`, since `[locale]` is the only top-level segment; `app/globals.css` stays at `app/` root (locale-independent) and is imported with a relative `../globals.css` path. `proxy.ts` (not `middleware.ts` — footgun 1) wraps `next-intl/middleware`'s default export directly, confirmed valid per `node_modules/next/dist/docs/.../proxy.md`'s "default export or named `proxy`" rule. Verified end-to-end for real: `next build` (both `/en`/`/cs` statically prerendered via `generateStaticParams`, `Proxy (Middleware)` present in the route list), a live `next dev` session (`/` → 307 to `/en` with no `Accept-Language`, → `/cs` with `Accept-Language: cs`, both locale pages render with the correct `<html lang>`), and — since proxy/middleware bundling under `output: standalone` has a history of subtle breakage — a full `docker build` + `docker run` repeating the same three checks against the actual production container image, not just `next dev`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `localePrefix: "as-needed"` (rejected — would make English the implicit unprefixed default, contradicting "both complete, always"); porting all ~2200 legacy message keys now (rejected — nothing to translate yet beyond the placeholder page; real message migration happens per-screen as each S-/T-/D-series ticket lands, sourced from `repos/web-app/src/locales/{en,cs}.json`'s wording, not its flat `app.foo.bar` key scheme, which next-intl doesn't use)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DEC-034 | **Error/not-found/forbidden conventions: standard `error.tsx`/`not-found.tsx` (not `catchError`) + `forbidden()`/`unauthorized()` behind `experimental.authInterrupts` + `global-not-found.tsx` behind `experimental.globalNotFound`** | F-012. `error.tsx`'s `retry` prop is stable since 16.3.0 and gives route-segment error boundaries the same re-fetch-and-re-render recovery `catchError` provides — confirmed via the bundled docs' own "Good to know": "You don't need to wrap `error.js` default exports with `catchError`." `catchError` (AGENTS.md footgun 8) is reserved for _component-level_ boundaries that aren't tied to a route segment (e.g. a single dashboard widget) — none exist yet, so not used here. `forbidden()`/`unauthorized()` are marked `experimental` in the bundled docs and gated behind `experimental.authInterrupts`; enabled anyway since there's no non-experimental way to get a real 403/401 status from the App Router and ReCodEx is permission-heavy throughout (groups, exercises, admin) — ADR philosophy matches DEF-001/DEF-002's "don't enable experimental features without a specific reason," and F-012 _is_ that reason. **Found live, not from docs alone:** a bogus path under a valid locale prefix (e.g. `/en/bogus`) fell through to Next's bare built-in 404 instead of the themed `app/[locale]/not-found.tsx` — the bundled `not-found.js` doc explicitly names this failure mode ("root layout... top-level dynamic segments... makes composing a consistent 404 page harder") and its fix, `global-not-found.tsx` (also experimental); added it and re-verified the same bogus-path request now renders the themed page. `app/global-error.tsx` and `app/global-not-found.tsx` both bypass the normal `[locale]` tree (Next's own docs: they render their own `<html>`/`<body>` and don't get app styles/theme/next-intl context), so both use hardcoded bilingual (en/cs together) text rather than `getTranslations` — the deliberate, narrow exception to "no hardcoded user-facing strings," since these are exactly the two places where the normal i18n machinery itself may be what failed or may never have loaded. Verified end-to-end for real via temporary throwaway test routes (`forbidden()`, `unauthorized()`, a thrown `Error`) plus a genuinely bogus URL and an invalid-locale URL, each checked for both the correct HTTP status (403/401/500/404) and the correct rendered text, in three environments: `next build`'s route manifest, a live `next dev` session, and a full `docker build` + `docker run` against the production standalone container (test routes deleted before committing; not part of the shipped code).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `catchError` for route-segment boundaries (rejected per the docs' own guidance above); leaving `forbidden`/`unauthorized` unbuilt until F-016+ auth lands (rejected — F-012 is specifically the ticket for this convention, and building it once now means every later permission check just calls the function, no boilerplate); skipping `global-not-found.tsx` (rejected after live-testing showed the common "bogus URL under a valid locale" case was hitting Next's unthemed default 404, not a rare edge case)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-035 | **F-013 route skeleton: top-level static routes only, real content deferred, shared `PlaceholderPage`**                                                                                                                                | `(anon)`/`(app)` route groups under `app/[locale]/...` (organizational only — route groups never add a URL segment, confirmed in `next build`'s route list: `/[locale]/dashboard`, not `/[locale]/(app)/dashboard`) split by auth requirement, not persona (DEC-015 still holds — no student/teacher split). Scoped to the **top-level static paths** named directly in `docs/IA.md`'s sitemap (16 routes: 7 anon, 9 app) — deliberately excludes every dynamic segment (`/groups/[groupId]`, `/exercises/[id]/edit-config`, etc.), since those belong to the S-/T-/A-/AD- ticket that actually builds real data-fetching against them; a stub for a route with no real ID behind it proves nothing structural that the static routes don't already prove. Each stub renders a shared `components/placeholder-page.tsx` with its own translated title (sourced from the legacy app's actual sidebar/page-title strings in `repos/web-app/src/locales/{en,cs}.json` — e.g. confirmed `app.sidebar.menu.exercises` is "Úlohy" in Czech, not the more literal "Cvičení" a guess would have produced) and one shared "not built yet" message. Both route-group layouts are deliberate passthroughs (`<div>{children}</div>`) with a comment pointing at the ticket that replaces them (D-series for chrome, F-015 for the actual `(app)` access check) — not pre-built speculatively. Stub pages render dynamically (no `generateStaticParams`/`setRequestLocale`), unlike the `[locale]` root page — not worth the extra per-page boilerplate for placeholder content about to be replaced. Verified all three ways again: `next build`'s route list, a live `next dev` session (status + translated content for a sample of routes in both locales), and a full `docker build` + `docker run`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Building every nested dynamic route now too (rejected — no real ID/data to render against yet, and duplicates work the owning phase ticket will do properly); per-page bespoke placeholder text instead of a shared component (rejected — pure duplication, the "not built yet" copy is identical everywhere); guessing Czech titles instead of sourcing them from the legacy app (rejected — `app.sidebar.menu.exercises` is real, checkable ground truth, not a translation call to make up)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEC-036 | **`proxy.ts` auth redirect: presence-only cookie check, three-way pathname split, composed with next-intl by short-circuiting on its redirects**                                                                                       | F-014. Brief §5 is explicit that `proxy.ts` is _not_ the security boundary ("Four of the thirteen May 2026 advisories were middleware/proxy bypasses") — this only checks `request.cookies.has(SESSION_COOKIE_NAME)`, never validates the token; the real check is `requireSession()` in the data layer, landing in F-015. New `lib/auth/session-cookie.ts` exports just the cookie _name_ (derived from `SESSION_COOKIE_PREFIX`) so F-016 (login handler, which actually sets the cookie) can't drift from what this file checks. Pathnames split three ways: `AUTH_ONLY_PATHNAMES` (login, register — redirected to `/dashboard` if a session cookie is already present, since showing that form to a signed-in visitor is pointless), `PUBLIC_PATHNAMES` (forgot-password [+ change], email-verification, accept-invitation, faq, and `/` — reachable regardless of auth state; a signed-in user resetting a password or reading the FAQ is normal, not a bug), and everything else (every `(app)` route — redirected to `/login?from=<original path>` if no cookie). This list is maintained by hand against the `(anon)`/`(app)` folders from F-013, since route groups never appear in the URL and can't be introspected at runtime. **Composition with next-intl's own middleware**: run `intlMiddleware(request)` first; if its response is itself a redirect (locale detection sending bare `/dashboard` to `/en/dashboard`), return that immediately rather than layering auth logic on an in-flight locale redirect — the browser's follow-up request hits `proxy.ts` again with a resolved locale, and the auth check applies cleanly then. Verified this exact chain live: `curl` on bare `/dashboard` (no locale, no cookie) returns a 307 to `/en/dashboard`; following that redirect lands on `/en/login?from=%2Fen%2Fdashboard`, not a broken intermediate state. All six presence/pathname combinations (app-route × cookie-present/absent, login × cookie-present/absent, public-page × cookie-present/absent) and the chained-redirect case verified with real requests against both `next dev` and a full `docker build`+`run` — proxy/middleware bundling under `output: standalone` already had a real history of subtle breakage in this project (F-011), so `next dev` alone wasn't trusted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Validating the token in `proxy.ts` (rejected outright — brief §5's explicit warning, and the whole reason `requireSession()` exists as the single boundary in F-015); redirecting `/` based on auth state too (rejected for now — `/` isn't yet cleanly owned by either route group, see DEC-016's note that dashboard-as-landing-page was chosen specifically _instead of_ a home-page-redirects-to-dashboard pattern; revisit once `/` itself is assigned real behavior); redirecting forgot-password/email-verification/accept-invitation/faq away from signed-in users (rejected — legitimate to visit while signed in, unlike login/register)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEC-037 | **`requireSession()`: decode-and-check-expiry only, no signature verification, redirects with no `?from=`, never called from a layout**                                                                                                | F-015, the actual authorisation boundary (brief §5). Decodes the session cookie's JWT payload (base64url, `Buffer.from(part, "base64url")`, no library) purely to read `sub`/`exp` — grounded in a **real token from a live login** (`docker` stack still running from F-025), not an assumed shape: confirmed `sub` is the user UUID and `exp`/`iat`/`nbf` are Unix-seconds claims, plus an `effrole` (null on a normal login — presumably populated for F-021's restricted-token case) and a `scopes` array (`["master","refresh"]` on a fresh login — useful groundwork for F-018's refresh logic later). Deliberately does **not** verify the JWT signature: this cookie only ever holds a token _we_ set verbatim from core-api's own login response, so re-verifying our own signature proves nothing — the real validation happens when core-api itself rejects a bad Authorization header on the next actual API call. Redirects to a plain `/login` (no `?from=`) on failure, unlike `proxy.ts`'s redirect — this is the defense-in-depth/direct-invocation path (a bypassed proxy check, or a Server Action running after the page's cookie has since expired), not the common case, and not worth threading the current path through every future caller for. **Not wired into `(app)/layout.tsx`** — brief §5 is explicit: "Not the caller. Not the page. Not the layout. Itself," meaning every individual data-touching function must call this itself once real data-fetching (F-022) exists, rather than relying on one layout-level choke point that could itself become a bypassable middleware-like boundary. Verified live via a temporary test route (deleted before commit, same pattern as F-012): no cookie (caught upstream by `proxy.ts`), a real valid JWT from an actual login (correct `{token, userId}` returned), a JWT-shaped-but-expired token (redirected), and a garbage cookie value (redirected) — all four behave correctly, and the expired/malformed cases specifically prove this adds real checking beyond `proxy.ts`'s shallow presence-only test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Verifying the JWT signature locally too (rejected — no shared secret to verify against belongs in this app; core-api is the signing authority and the sole source of truth for whether a token is genuinely valid); preserving `?from=` in `requireSession()`'s redirect (rejected for now — adds complexity for what should be the rare path once `proxy.ts` is doing its job; revisit if it turns out Server Actions hit this often enough to matter); gating `(app)/layout.tsx` with this function (rejected per the brief's explicit "not the layout" — would recreate exactly the single-choke-point risk §5 warns about)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEC-038 | **Login is a Route Handler (corrects DEC-023), `sessionCookieOptions()` derives `secure` from `API_BASE_PUBLIC`'s scheme, cookie `maxAge` sized from the JWT's real `exp`**                                                            | F-016. `app/api/auth/login/route.ts` — brief §5's own words: "Login: Route Handler receives credentials → calls core-api → stores the token in an httpOnly, sameSite=lax cookie." DEC-023 originally listed "Login, logout" under Server Actions, contradicting this and contradicting `docs/BACKLOG.md`'s own F-016/F-017 titles — corrected in place rather than left standing. New `sessionCookieOptions(maxAgeSeconds?)` in `lib/auth/session-cookie.ts`: `httpOnly: true, sameSite: "lax"`, and `secure` derived from `new URL(API_BASE_PUBLIC).protocol === "https:"` — **not** `NODE_ENV === "production"`, since this deployment can (and currently does, see the compose repo's `README.md`) run "in production" over plain HTTP with no TLS certificate yet; `secure: true` there would make the browser silently drop the cookie, exactly the "login looks like it worked but never signs you in" failure brief §5 warns about by name. `API_BASE_PUBLIC` was chosen over reading the live request's `X-Forwarded-Proto` header (confirmed nginx sets it, the compose repo's `services/proxy/nginx.conf.template`) specifically so the helper's signature stays request-free — brief §5 calls for "one helper... used everywhere," and threading a request object into every call site would work against that. Route Handler presents a clean `{email, password}` contract to the frontend and maps `email` → core-api's own `username` field internally (confirmed live: ReCodEx has no separate "username" concept, it's just core-api's internal field name for the email). Input validated with `z.email()` (not the deprecated `z.string().email()` chain — confirmed against the installed zod 4.4.3's own type defs, a real API change this session would have gotten wrong from v3-era memory). Extracted the JWT-payload decoder from F-015's `require-session.ts` into a shared `lib/auth/jwt.ts` (`decodeJwtPayload`) once a second real consumer (sizing the cookie's `maxAge` against the token's actual `exp`, rather than a guessed constant) appeared, rather than duplicating the parsing logic. Verified for real, repeatedly, not just "the build succeeds": correct credentials (200, `Set-Cookie` with the exact right `Max-Age` and no `Secure` flag on this plain-HTTP deployment, confirmed byte-for-byte against `curl -i`'s raw header), wrong password (401, core-api's own message passed through), malformed input (400, Zod), and the **full round trip** — login, save the cookie, use it against `proxy.ts` (F-014), use it against `requireSession()` (F-015), confirm `/login` itself now redirects away — first in `next dev`, then again against the actual production compose container talking to `api` over the internal Docker network (`http://api:80/v1`), not the dev-mode public-URL fallback.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Server Action for login (rejected — contradicts the brief's explicit, unambiguous §5 wording, and DEC-023 was simply wrong on this point); `NODE_ENV`-based `secure` flag (rejected — provably wrong for this specific deployment's current state, see above); a fixed/guessed cookie `maxAge` instead of deriving it from the token's own `exp` (rejected — would let the cookie outlive the token, or expire it early, for no reason when the real value is one JWT decode away); passing the request object into `sessionCookieOptions()` to read `X-Forwarded-Proto` per-call (rejected — works, but fights the brief's "one helper, used everywhere" framing by making every call site thread a request through)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-039 | **`request.url` is unreliable in a `output: standalone` Route Handler — use the `Host` header instead**                                                                                                                                | F-017. Discovered live, not in any doc: `app/api/auth/logout/route.ts` originally built its redirect target with `new URL("/login", request.url)` (the same pattern used everywhere else in this codebase, e.g. `redirect()` calls). Worked fine in `next dev`, but in the actual `output: standalone` Docker container, the resulting `Location` header pointed at `http://0.0.0.0:3000/login` — the container's own internal bind address (`HOSTNAME`/`PORT` from the Dockerfile) — instead of the address the browser actually used (confirmed with a temporary debug endpoint, not committed, echoing both `request.url` and `request.headers.get("host")`: the `Host` header was correct — `localhost:3001` — while `request.url` was not). This is specific to plain `Request.url` in a standalone-served Route Handler; `proxy.ts`'s `request.nextUrl` does **not** have this problem (re-confirmed against F-014's already-verified behavior). A relative `Location` header was tried as a simpler fix and also failed — confirmed live, `NextResponse.redirect()` throws `Error: URL is malformed "/login". Please use only absolute URLs`, despite its parameter being typed as accepting a plain `string`. The working fix: build the absolute URL from `request.headers.get("host")` (confirmed correct) plus the protocol from `API_BASE_PUBLIC` (same derivation as DEC-038's `sessionCookieOptions()`). **Any future code that builds an absolute URL from `request.url` inside a Route Handler should be treated as suspect and re-verified against a real `output: standalone` build, not just `next dev`** — this is exactly the kind of divergence that only shows up under the production build config, matching the pattern already seen twice before in this project (F-011's `@swc/helpers` tracing gap, F-012's `global-not-found` gap).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Reconstructing the URL from `request.url` with a manual host substitution (rejected — more code than just reading `Host` directly, and still depends on the broken value for the protocol/path parts); using a relative redirect string (rejected — confirmed to throw at runtime despite the type signature suggesting it would work)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-040 | **Token refresh: proactive, 24h-before-expiry threshold, in-memory per-token de-duplication in `proxy.ts`**                                                                                                                            | F-018. `lib/auth/refresh-session.ts`'s `maybeRefreshSession()`, called from `proxy.ts` (brief §5: "Token refresh and expiry handling happen in `proxy.ts` or Route Handlers only") and applied to whichever response `proxy.ts` ends up returning, not just the pass-through case. Threshold is a flat 24h before the token's own `exp` (tokens last 7 days by default, confirmed live) -- simple, ample buffer. De-duplication keyed by the token string itself, not `userId`: a `Map<string, Promise<...>>` at module scope, correct within one running Node.js process (this deployment's current single-instance shape, ASS-004), not across horizontally-scaled replicas -- noted as a real limitation, not silently assumed away. **Two things discovered only by testing live against core-api, not from any doc**: (1) refreshing does not invalidate the token used to request it -- both old and new tokens kept working afterward, so the actual failure mode is "waste a redundant call," not the brief's literal "invalidate each other"; (2) core-api's JWTs are second-granularity deterministic (HMAC-SHA256 over a payload whose only varying claim is `iat`) -- two refresh calls within the same wall-clock second produce a byte-identical token, which would make a broken de-duplication test (comparing resulting token strings) look correct by accident. Verified properly instead by counting actual upstream calls (temporary logging, not committed): 6 concurrent requests sharing one soon-to-expire cookie produced exactly one upstream `POST /login/refresh` call, all 6 resolving in ~150ms. Also verified the negative case with the real 24h threshold: a freshly-issued 7-day token triggers no refresh at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Fraction-of-lifetime threshold instead of a flat 24h (rejected -- needless complexity for a fixed, known token lifetime); de-duplicating by `userId` instead of the token string (rejected -- token-as-key is simpler and self-invalidating); verifying de-duplication by comparing final token values (rejected after discovering the same-second-determinism issue -- would have been a false-positive-prone test)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| DEC-041 | **External-auth (CAS-and-similar) callback: generic `[authenticatorName]` Route Handler, `token` query param, no ticket-exchange step**                                                                                                | F-019. `app/api/auth/external/[authenticatorName]/callback/route.ts`. Brief §5 describes this only loosely ("exchanges the ticket with core-api"); the legacy app's own two implementations disagree with each other -- `repos/web-app/src/helpers/cas.js` is a CUNI-specific popup + client-side CAS `serviceValidate` flow using a `ticket` param, hardcoded to `idp.cuni.cz`, while the page actually wired into the login flow, `repos/web-app/src/pages/LoginExternFinalization/LoginExternFinalization.js`, reads a `token` param and does nothing CAS-specific at all. Resolved by reading core-api's own PHP source (`repos/api/app/helpers/ExternalLogin/ExternalServiceAuthenticator.php`'s `decodeToken()`), not by guessing between the two: core-api never talks to CAS or any external provider itself -- each `authenticatorName` has a pre-shared `jwtSecret` configured server-side, and core-api's whole job is to verify a signature on whatever token this route hands it. So `LoginExternFinalization.js`'s simpler `token`-relay model is the real, general mechanism; `cas.js` describes one specific university's own external bridge infrastructure (outside this project's repos), not something core-api implements. `[authenticatorName]` is a dynamic segment (not hardcoded to `cas`), matching core-api's own generic `/login/{authenticatorName}` and the legacy config's `EXTERNAL_AUTH_SERVICE_ID`. On a `token` present, POSTs it to `${API_BASE_INTERNAL}/login/{authenticatorName}` and reuses F-016's `establishSession()` on success (extracted from login's inline logic into `lib/auth/session-cookie.ts` for this second call site); any failure (missing `token`, non-2xx from core-api, or an undecodable token) redirects to `/login?externalAuthError=1` rather than throwing. Redirects go through a new `lib/http/absolute-url.ts` (`buildAbsoluteUrl()`, also extracted from F-017's logout for this second call site) rather than `new URL(path, request.url)`, per DEC-039. **Verified live in both `next dev` and a rebuilt `output: standalone` Docker container**: unknown `authenticatorName` correctly 400s from core-api and redirects to the error page rather than throwing; missing `token` does the same; login/logout still work after the `establishSession`/`buildAbsoluteUrl` refactor (regression check). Also specifically confirmed in the Docker build (temporary logging, not committed, exactly because this is where DEC-039's bug was originally found) that although `request.url`'s origin is wrong under `standalone` (internal bind address, e.g. `0.0.0.0:3000`), its path+query portion is intact -- `new URL(request.url).searchParams` correctly read a real query param end to end. **Not, and can't yet be, verified**: the actual success path against a real external identity provider -- this deployment has no `EXTERNAL_AUTH_*` configured at all (`docs/QUESTIONS.md` Q-004), so there's no way to obtain a validly-signed external token to test against. Corrected a stale recon-phase assumption in `docs/IA.md` (line ~310), which had guessed a CUNI-specific `/login/extern-finalization/:service` → `/api/auth/cas/callback` shape.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Hardcoding a `/api/auth/cas/callback` route per `cas.js`'s CUNI-specific model (rejected -- reading core-api's own source showed this isn't what core-api implements; a hardcoded single-provider route would also contradict the generic `authenticatorName` config the legacy app and core-api both actually use); reproducing `cas.js`'s client-side popup + `serviceValidate` mechanic (rejected -- that's a university-specific external bridge's own behavior, not something this app or core-api needs to implement); leaving the route unbuilt since it's unverifiable end-to-end here (rejected -- `docs/QUESTIONS.md` Q-004 already called for building it regardless, for parity, same reasoning as local registration)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEC-042 | **User takeover: `POST /api/auth/takeover/[userId]`, authorization delegated entirely to core-api**                                                                                                                                    | F-020. `app/api/auth/takeover/[userId]/route.ts`. Mirrors core-api's own `POST /v1/login/takeover/{userId}` 1:1 (dynamic segment, not a body field), confirmed against `docs/swagger.yaml` and `LoginPresenter::actionTakeOver()`/`checkTakeOver()`. Response shape is identical to login and the external-auth callback (`{payload: {accessToken, user}}`), so this route reuses the same `establishSession()`. This route enforces only one thing itself -- that some caller session exists at all (401 if not) -- and forwards it as `Authorization: Bearer <token>`; whether _that_ caller may take over _this_ target user is entirely core-api's call, not duplicated here. Confirmed directly in `repos/api/app/config/permissions.neon`, not assumed from the BACKLOG.md note: `takeOver` is allowed only for `role: superadmin`, with an explicit `allow: false` safety net underneath for everyone else, specifically to prevent a wildcard rule from accidentally granting it. A non-superadmin's attempt surfaces as a JSON 403 (not a redirect to `/login`, unlike the no-session-at-all case): the caller is legitimately logged in, just not allowed to do this one thing, and bouncing them to `/login` again would be a dead end, not a fix. **Verified live in both `next dev` and a rebuilt Docker `standalone` container** against the real seeded accounts (`docs/SEED_ACCOUNTS.md`): superadmin (`admin@admin.com`) taking over a plain student correctly swaps the session cookie's `sub` to the target user; the same student attempting to take over the superadmin correctly gets a 403; no session cookie gets a 401; a malformed (non-UUID) `userId` gets a 400 from this route's own `z.uuid()` check without ever reaching core-api; a well-formed but nonexistent `userId` gets a 404 forwarded from core-api.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Re-deriving/caching an `isSuperadmin` check in the BFF from the JWT's `effrole` claim and gating the route on it before ever calling core-api (rejected -- duplicates an authorization decision core-api already makes authoritatively via `canTakeOver()`/`permissions.neon`, and DEC-022/DEC-037's own principle is that core-api is the one authority that matters, not a second copy of its logic that could drift out of sync); using `requireSession()` for the missing-session case instead of a direct cookie read (rejected -- its `redirect("/login")` behavior is right for page/data-access guards but wrong for this JSON-API-style POST route, which callers are expected to `fetch()` and read a JSON body from, not follow a redirect out of); accepting `userId` as a request body field instead of a path segment (rejected -- core-api's own endpoint is path-based, and mirroring that 1:1 is simpler, same reasoning as F-019's `[authenticatorName]`)                                                                                                                                                                                                                                                                                                        |
| DEC-043 | **Restricted (application) token generation: raw token returned to the client by design, deliberate exception to DEC-021**                                                                                                             | F-021. `app/api/auth/restricted-token/route.ts`. `POST /v1/login/issue-restricted-token`, confirmed against `docs/swagger.yaml` and `LoginPresenter::actionIssueRestrictedToken()`. Legacy app research (`GenerateTokenForm.js`, `EditUser.js`) confirmed this endpoint has two different consumers with genuinely different semantics: a self-service "Generate Application Token" form (displays the raw token for the user to copy into external scripts/tools, doesn't touch the current session) and `restrictEffectiveRole()` (a sidebar "view as role X" toggle that dispatches a `LOGIN` action, replacing the current session with the narrower one). Scoped this ticket to only the former -- the ticket title and BACKLOG.md note describe the raw BFF primitive, and no doc anywhere in this repo had already committed to building the effective-role-switch UX, so building it now would be undecided scope creep, not what this Foundation ticket asked for. Left as a future ticket (would reuse this same route's response plus its own explicit `establishSession()` call) if the Dashboard/sidebar phase decides to build it. **Deliberately returns the raw generated token in the JSON response body** -- the one intentional exception to DEC-021 ("client components never see the token") in this whole module: that decision is about the BFF's own httpOnly session cookie, not about a token the user explicitly requested to take _out_ of this app, same idea as a GitHub/GitLab personal access token generator. Never calls `establishSession()` and never touches the session cookie -- confirmed live that generating a token leaves the caller's own session cookie byte-for-byte unchanged. `scopes` is validated only as "a non-empty array of strings," not against a hardcoded copy of core-api's `TokenScope` enum -- core-api's own `validateScopeRoles()` already authoritatively rejects forbidden scopes (`change-password`, `email-verification`) and unknown `effectiveRole` values; mirroring that list here would just be one more thing to keep in sync, same reasoning as DEC-042's authorization delegation. **Verified live in both `next dev` and a rebuilt Docker `standalone` container**: a generated `read-all`-scoped token decodes to exactly the requested scopes (not `master`/`refresh`); the caller's own session cookie is unchanged after generating one; no session gets 401; an empty `scopes` array gets 400 from this route's own Zod check; `change-password` gets core-api's 403 with its own message forwarded verbatim; an unknown `effectiveRole` gets core-api's 400; `master` scope with an expiration beyond core-api's 1-week cap gets its 403, also forwarded verbatim.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Also building the effective-role-switch UX (session-replacing `restrictEffectiveRole()` equivalent) in this same ticket (rejected -- undecided UI/UX scope not asked for by this Foundation-phase ticket; better designed alongside the sidebar/dashboard work that will actually consume it); hardcoding a `TokenScope`-mirroring enum for `scopes` validation (rejected -- core-api's own validation is authoritative and already returns a clear error, a local copy could silently drift from the PHP source over time); withholding the raw token from the response and instead only ever setting it as the session cookie (rejected -- defeats the feature's actual purpose, which is explicitly to hand the user a token to use _outside_ this app)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| DEC-044 | **Typed `server-only` API client: `openapi-typescript` for path/request typing, hand-written response types, no caching**                                                                                                              | F-022. `lib/api/client.ts`, generated types at `lib/api/core-api.generated.ts` (`pnpm generate:api-types`, vendored spec at `openapi/core-api.yaml`, a copied-in snapshot of `repos/api/docs/swagger.yaml` -- CI only checks out this repo, so codegen input has to live here, not be reached cross-repo). Brief §4's own table: "API types: Generate from OpenAPI if available (`openapi-typescript`); otherwise hand-written types." Chose `openapi-typescript` alone over the companion `openapi-fetch` runtime library after actually inspecting the generated output: core-api's `swagger.yaml` has **no response schemas anywhere** -- confirmed directly, all ~250 response definitions are literally `description: 'Placeholder response'`, no `content`/`schema`, no `components.schemas` section in the 7409-line file at all. Request bodies and path params generate real, useful shapes (confirmed for several endpoints, e.g. `issue-restricted-token`'s `scopes: unknown[]` matches F-021's own hand-written Zod schema exactly) but response payloads always resolve to `never` -- so a caller must supply the actual response type `T` by hand regardless, same as this repo's auth routes have been doing all along (`{payload: {accessToken: string}}` inline interfaces). Given that, `openapi-fetch`'s main value proposition (fully inferred responses) doesn't apply here, and adding it just for request-side inference wasn't worth a second dependency with the same TS peer-range mismatch (`^5.x` vs. this repo's pinned `6.0.3` -- installs fine, `pnpm peers check` warns, verified `openapi-typescript`'s codegen itself runs and produces correct output despite the warning). `lib/api/client.ts`'s `path` parameter is typed as `keyof paths` -- a genuine compile-time guarantee the endpoint exists and is spelled correctly -- while `pathParams`/`query`/`body` stay as plain `Record`/`unknown`, and callers who want a specific endpoint's real generated request-body shape can still import `operations` directly. Calls `requireSession()` itself on every request (DEC-022/037's boundary, brief §5's own words: "Server-side reads: Server Components call a `server-only` API client that pulls the token from `cookies()`"), sets `cache: "no-store"` unconditionally on every fetch and does not wrap anything in React's `cache()` ("no user caching" per the BACKLOG.md note, and brief rule 6.3: "Never cache user-scoped data... a cross-user cache leak here is a security incident"). Unwraps core-api's fixed response envelope (`{success, error: {message, code, parameters}, payload}`, confirmed directly against `ApiErrorPresenter::sendErrorResponse()`) into a typed `ApiError` class -- `code` is the same short, stable string (`"403-002"`, not the PHP constant name) the legacy app's own `apiErrorMessages.js` keys a localised message table on, confirmed by reading `FrontendErrorMappings.php`'s constant _values_, not just their names -- intended as the future hook for a next-intl-backed equivalent table, not built by this ticket. **Bug found and fixed live, not caught by typechecking**: `API_BASE_INTERNAL`/`API_BASE_PUBLIC` already include the `/v1` prefix (every existing auth route relies on this, e.g. `${apiBase}/login`), but every generated `paths` key is _also_ absolute from core-api's root starting with `/v1` (that's how `swagger.yaml` itself defines them) -- naive concatenation produced a `/v1/v1/...` URL that 404s. Fixed by stripping the leading `/v1` from the resolved path in the client, documented inline, rather than changing what `API_BASE_INTERNAL` means (would have touched every already-verified F-016 through F-021 route for no benefit). **Verified live in both `next dev` and a rebuilt Docker `standalone` container**, via a temporary debug route (not committed): a real `GET /v1/users/{id}` call for the caller's own id returns the correctly-typed payload; a syntactically valid but nonexistent id correctly throws `ApiError` with core-api's real `httpStatus: 404`, `code: "404-000"`, and message, not a generic failure. | `openapi-fetch` for a fully generated, single-call client (rejected after inspecting actual codegen output -- its main benefit, typed responses, doesn't exist for this API since `swagger.yaml` has no response schemas; would have added a dependency and its own TS peer-version friction for request-side typing alone, which a much simpler hand-written client already provides via `keyof paths`); fully generic per-method type inference for `pathParams`/`query`/`body` from the generated `operations` type (rejected as complexity/effort not justified given the response side is unavoidably hand-typed anyway -- callers can still reach into `operations` directly for a specific endpoint's real request shape when it matters); changing `API_BASE_INTERNAL`/`API_BASE_PUBLIC` to exclude `/v1` instead of stripping it from generated paths (rejected -- would require touching every existing, already-verified auth route for a purely cosmetic difference); regenerating types from a live core-api instance in CI instead of a vendored spec (rejected -- CI has no network access to a running core-api and no cross-repo access to the compose repo; a vendored, manually-refreshed copy is the only reproducible option for this repo's actual CI setup) |
| DEC-045 | **Playwright smoke harness: API-driven login (not a form), `next start`, capped worker concurrency, not wired into CI**                                                                                                                | F-023. `playwright.config.ts`, `e2e/smoke.spec.ts`, `e2e/helpers/{accounts,auth,base-url}.ts`. Skipped `@next/playwright` (brief §4's table mentions it for its `instant()` helper) after checking its own published README: it requires Cache Components, which this repo has deliberately left off (DEF-001's `cacheComponents: false` deferral) -- using it would mean either enabling an experimental caching feature this project has explicit, documented reasons to avoid, or installing a helper with a hard requirement it can't meet. Used plain `@playwright/test` instead. Logs in via the real Auth BFF Route Handler (`app/api/auth/login/route.ts`, F-016), not a login _form_ -- there isn't one yet (`/login` is still F-013's `PlaceholderPage`). Runs against `next build` + `next start`, not `next dev`: dev mode's extra warnings/overlay aren't representative of what ships, and DEC-039's `request.url` bug only reproduced in a production `standalone` build -- confirmed `next start` still loads `.env.local` (`node_modules/next/dist/docs/.../environment-variables.md`: skipped only when `NODE_ENV=test`). Chromium only, ~10 planned core journeys deferred to later tickets per brief §8 -- this ticket is explicitly the _skeleton_ (login + visit every route implemented so far + fail on errors), not the full suite. **Not wired into `.github/workflows/ci.yml`**: these tests need a real, reachable core-api (brief: "against the real local API"), which GitHub Actions' runner doesn't have and this repo has no way to stand up on its own -- core-api/mysql/etc. live in the separate `ReCOdex` compose repo, unreachable from this repo's CI. Meant to be run locally (`pnpm test:e2e`) against a developer's own `docker compose up`'d stack. **Three real bugs found and fixed while building this, none hypothetical**: (1) `app/api/_debug/...`-style leading-underscore route directories are Next's private-folder convention and are silently excluded from routing (a first version of an unrelated debug route 404'd for this reason during F-022's own verification, same lesson applies here); (2) `test.use({storageState: path})` declared at `describe` scope silently becomes the default for _any_ context Playwright creates within that scope -- reproduced with both `browser.newContext()` and `playwright.request.newContext()` inside `beforeAll`, both threw `ENOENT` reading a file that exact call was supposed to create. Fixed by moving the login step to a plain `fetch()` (no Playwright context at all) and injecting the resulting cookie per-test via `context.addCookies()` instead of file-based `storageState`; (3) the default CPU-core-count worker total (7, on the machine this was built on) sent enough concurrent bcrypt-hashing logins to the local dev `docker compose` stack's PHP-FPM pool to make some of them genuinely time out (`ConnectTimeoutError` to `recodex.local:80`, confirmed in the Next.js server's own logs, not a code bug) -- fixed with `workers: 2`, verified stable across repeated full runs afterward. Also found and fixed a Vitest/Playwright collision: Vitest's own default include glob picked up `e2e/smoke.spec.ts` and tried to run it as a unit test, failing immediately -- excluded `e2e/**` in `vitest.config.ts`. **Verified live**: 48/48 tests pass, stably, across repeated runs -- 8 anonymous public routes plus 4 seeded personas (`docs/SEED_ACCOUNTS.md`) each visiting all 10 protected `(app)` routes, with a real session cookie, real console-error/page-error/failed-request watching, and a screenshot per route in the already-gitignored `screenshots/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `@next/playwright`'s `instant()` helper (rejected -- hard-requires Cache Components, which this repo deliberately has off); driving a real login form with Playwright's UI interactions (rejected -- no form exists yet; the API-driven approach is also simpler and won't need to change until a form ticket lands, at which point only `helpers/auth.ts` needs updating, not every test); `test.use({storageState})` with a `beforeAll`-written file (rejected after two different Playwright APIs both threw `ENOENT` from the same describe-scope option inheritance -- `context.addCookies()` from a plain `fetch()`-obtained cookie is both simpler and immune to it); leaving `workers` at its default (rejected -- reproducibly flaky against a real local backend, not a one-off); wiring `test:e2e` into CI as-is (rejected -- would be red on every run for an environment reason, not a code reason, until this repo's CI gains a way to stand up core-api, which is out of scope for this ticket).                                                                                                                                                                                                                                                                    |
| DEC-046 | **Token-leakage security test: real browser-set cookie inspection, cookie-diffing, and both HTML and JS-bundle scanning**                                                                                                              | F-024. `e2e/security.spec.ts`, the brief's §8 "one security test, non-negotiable": assert the auth cookie is httpOnly and the raw token appears nowhere in served HTML or client bundles. Deliberately does **not** reuse F-023's `loginAndGetCookie()` + `context.addCookies()` shortcut: that path manually reconstructs a cookie for rendering convenience (F-023 doesn't care about attribute fidelity, only presence), which would make this test check a cookie _this test itself_ labeled `httpOnly: true` -- a tautology, not a verification. Instead uses `page.request.post()` directly so Playwright parses the _real_ `Set-Cookie` response header from `/api/auth/login`, preserving whatever flags the server actually set. Finds the session cookie by diffing `context.cookies()` before/after login rather than assuming its name (configurable via `SESSION_COOKIE_PREFIX`) or shape -- robust to either changing without silently checking nothing. Second assertion goes further than the flag alone: reads `document.cookie` from inside the page's own JS after navigating, confirming the practical consequence (browser JS genuinely cannot read it), not just the attribute Playwright's cookie API reports. Third test scans both the full rendered HTML (`page.content()`) of every protected route and every served JavaScript response body (captured via a `page.on("response")` listener filtering on `content-type: ...javascript`) for the literal token string. **Verified the test can actually fail, not just pass by construction -- both assertions, deliberately, before trusting either**: temporarily flipped the HTML check to assert against `"html"` (guaranteed present) and confirmed a real failure with the full captured page source in the error output; temporarily flipped the httpOnly check to assert `false` and confirmed a real failure reporting `Received: true`. Both reverted immediately after confirming failure, real assertions restored, full suite re-run clean. Route lists (`PUBLIC_ROUTES`/`APP_ROUTES`) extracted from `smoke.spec.ts` into a new shared `e2e/helpers/routes.ts` once this ticket gave them a second consumer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Reusing F-023's `context.addCookies()`-based login for this test too (rejected -- would check a cookie whose `httpOnly` flag this test itself set, not what the server actually sends; a tautological pass that verifies nothing); hardcoding the session cookie's name instead of diffing (rejected -- `SESSION_COOKIE_PREFIX` is deployment-configurable, and a hardcoded name could silently stop matching without failing loud); only checking the `httpOnly` flag via Playwright's cookie API without the `document.cookie` follow-up (rejected -- cheap to add, and demonstrates the actual guarantee the flag is supposed to provide, not just that Playwright parsed it correctly); trusting both assertions to be correct without deliberately forcing a failure first (rejected -- a security test that has never been observed to fail is exactly the kind of test that could be silently broken and no one would notice; the brief calls this test "non-negotiable", which argues for extra rigor here specifically, not less).                                                                                                                                                                                                                                        |
| DEC-047 | **`PageShell`: plain Server Component, `breadcrumbs` required but unresolved, existing token/Link conventions reused as-is**                                                                                                           | D-001. `components/page-shell.tsx`, per `docs/IA.md` §3.3's own `PageShellProps` interface (`title`, `subtitle?`, `breadcrumbs: BreadcrumbItem[]`, `actions?`, `tabs?`, `children`), satisfying brief §9 ("No page builds its own header") and footgun #12 ("Breadcrumbs come from one mechanism"). A plain Server Component, not `"use client"`: nothing in it is interactive on its own -- `actions`/`tabs` are pre-built `ReactNode`s the caller supplies, already client components if they need interactivity, matching brief rule 4. `breadcrumbs` is a **required** prop but this component does not resolve it -- D-002 (central route manifest, async entity-name resolvers) owns turning a route into `BreadcrumbItem[]`; `PageShell` only renders whatever list it's handed, so it stays a pure presentational component with no data dependency, and D-002 can land independently without touching this file. Reused the existing shadcn-style CSS tokens (`text-foreground`/`text-muted-foreground`/`border-border`, already established in `app/globals.css` from earlier tickets) and `@/i18n/navigation`'s `Link` (not `next/link`, required for automatic locale-prefixing per next-intl's own convention already used elsewhere in this repo) -- no new dependencies, no new token system. Wired into `components/placeholder-page.tsx` (F-013's stub-page shell, used by all 18 currently-real routes) with a single, unlinked breadcrumb crumb (just the page's own title, no parent chain) as an immediate, low-risk way to verify `PageShell` renders correctly across the entire live app today, rather than waiting for the S-/T-/A-/AD- tickets that will eventually replace each stub page individually -- real breadcrumb chains are explicitly D-002's job, not faked here ahead of that ticket. **Verified live** (`next dev` and a rebuilt Docker `standalone` container): the full 50-test E2E suite (F-023/F-024) passes unchanged with `PageShell` now in the render path of every route, confirming no console errors, hydration mismatches, or unexpected 4xx/5xx were introduced; a temporary debug route (not committed) exercised the full prop surface `PlaceholderPage` doesn't touch -- `subtitle`, `actions`, `tabs`, and a linked (non-current) breadcrumb crumb -- confirming each renders and that the linked crumb produces a correctly locale-prefixed `href` (`/en/groups`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Having `PageShell` itself resolve breadcrumbs from the current route (rejected -- couples a presentational component to data-fetching/route-parsing logic that D-002 is separately, and more sensibly, scoped to own); a client component to allow, e.g., a collapsible mobile breadcrumb (rejected -- not needed yet, and would require justification in this file per brief rule 4 that the current design doesn't need); introducing Radix or a component library for the actions/tabs chrome now (rejected -- `actions`/`tabs` are plain `ReactNode` slots by design; Radix primitives are D-006's concern, when something with real focus/interaction semantics, like a Dialog, is actually built).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DEC-048 | **Central breadcrumb manifest: prefix-walking a flat pattern registry, throws on any unregistered route, dynamic segments deliberately unregistered**                                                                                  | D-002. `lib/breadcrumbs/manifest.ts`, satisfying footgun #12 exactly: "A central route manifest mapping segments to labels, including async resolvers for entity names... Every page renders breadcrumbs through it. No page builds its own." Design: a flat array of `{namespace, pattern}` (static) or `{pattern, resolve}` (dynamic, `:param`-templated) entries; `resolveBreadcrumbs(pathname, locale)` walks every prefix of the given pathname (e.g. `/forgot-password/change` -> `[/forgot-password, /forgot-password/change]`), looks each prefix up by matching it against every registered `pattern`, and resolves its label -- via next-intl's `getTranslations({locale, namespace})` for static entries, via the entry's own `resolve()` for dynamic ones. **Throws** for any unregistered prefix rather than silently omitting a crumb: every real route must register itself here, so a gap fails loudly (confirmed live: a temporary page using an unregistered namespace produced a clean 500 with the exact message `No breadcrumb manifest entry registered for namespace '...'.`, not a silently broken page). **Nested dynamic segments deliberately not registered** (`/groups/[groupId]`, `/assignments/[id]`, `/solutions/[id]`, `/exercises/[id]`, per `docs/IA.md` §3.2's own examples): no page for any of them exists yet to test a resolver against, and their entity-name resolvers depend on group/assignment/exercise response shapes no ticket has confirmed -- F-022's typed client is ready to call core-api, but nothing has fetched one of these entities for real yet. Adding a guessed `resolve()` now would risk being wrong and needing rework; the `resolve` field's type signature (`(params, locale) => Promise<string>`) is already in place for whichever future ticket builds that route to fill in. Wired into `components/placeholder-page.tsx` via a new `resolveBreadcrumbsForNamespace(namespace, locale)` entry point -- since every stub page already self-identifies by next-intl namespace (`getTranslations("Groups")` etc.), reusing that same string as the manifest's own lookup key let all 17 stub `page.tsx` files simplify (each now just `<PlaceholderPage namespace="Groups" />`, no longer needing to be `async` or import `getTranslations` itself -- `PlaceholderPage` does one resolution and reuses the breadcrumb chain's own last crumb as the `<h1>` title, so the two can never drift apart). **Verified live** (`next dev` and a rebuilt Docker `standalone` container): the two-segment `/forgot-password/change` route correctly renders a two-crumb chain ("Reset password", linked to a properly locale-prefixed `/en/forgot-password` via `PageShell`'s `@/i18n/navigation` `Link`, then "Change forgotten password" as the unlinked current page); all 17 single-segment routes render their one crumb correctly; the full 50-test E2E suite passes unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Silently rendering an empty breadcrumb (or falling back to a raw path segment) for an unregistered route (rejected -- defeats the whole point of a _central_ manifest if a missing registration doesn't fail loudly; footgun #12 exists specifically because per-page breadcrumb construction drifts silently); guessing at group/assignment/exercise API response shapes to register their dynamic resolvers now (rejected -- no ticket has confirmed those shapes yet, and a wrong guess would need rework once the real S-/T-series ticket lands; the type signature is ready, the entries aren't, and that's an honest reflection of what's actually been verified); keying the manifest by pathname only, requiring every caller (including `components/placeholder-page.tsx`) to know its own full pathname (rejected -- namespace is already the identifier every stub page carries, and reusing it let 17 files simplify instead of growing a second, redundant piece of per-page config).                                                                                                                                                                                                                                                                                 |
| DEC-049 | **`DataTable`: client-side sort/filter/paginate over a caller-supplied dataset, URL-synced view state, self-wrapped in `Suspense`**                                                                                                    | D-003. `components/data-table.tsx`, satisfying brief §9/§10's "DataTable (sort/filter/paginate/URL sync/bulk select)" exactly. Read the legacy `repos/web-app/src/components/widgets/SortableTable` first (brief: "source code is your primary reference") rather than guessing the shape: it sorts/paginates entirely client-side over an already-fetched array, and persists sort order in `localStorage`, with **no URL state at all**. Deliberately corrected the latter -- brief §9: "Sharing a URL reproduces the exact view" -- while keeping the former (client-side operation over a caller-supplied `data: T[]`): no entity-listing ticket exists yet to know whether core-api supports server-side pagination/sort/filter for every list this will eventually back, so this component stays data-source-agnostic, same reasoning as D-002's deferred dynamic breadcrumb resolvers. Sort column+direction, filter query, and current page sync to `searchParams` (`${id}-sort`, `${id}-q`, `${id}-page`, prefixed by a required `id` prop so multiple tables can coexist on one page without colliding); row **selection** deliberately stays local React state, not URL-synced -- transient bulk-action state, not a bookmarkable view, and a long ID list in the URL is exactly the unbounded-query-string problem deep-linking isn't about. `"use client"` throughout: genuinely interactive (sortable headers, pagination, a debounced filter input, selection checkboxes), not a layout/page needing rule-4 justification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DEC-050 | **TanStack Table not adopted for `DataTable`: v9's actual API is a headless feature-registration engine, not the simple hook the brief's stack table implies**                                                                         | D-003 follow-up. The brief's own stack table (§4) names "Tables: TanStack Table, in client leaves" as the chosen library -- not investigated before D-003 shipped with a hand-rolled implementation, a real gap noticed only afterward while re-reading §4 for D-004. Installed `@tanstack/react-table@9.1.2` (current latest, confirmed via `npm view`) and read its own bundled skill docs (`node_modules/@tanstack/react-table/skills/getting-started`, `@tanstack/table-core/skills/core`) before deciding anything, per this project's own standing rule to verify against installed docs rather than memory (the same discipline AGENTS.md's Next.js-version warnings already establish). **Finding**: v9 is a substantial rewrite from the v8 API the brief's phrasing almost certainly assumed (a simple `useReactTable({data, columns, getCoreRowModel: getCoreRowModel()})` call). V9 replaces this with an explicit, headless feature-registration model -- `useTable({features, columns, data})`, where sorting, filtering, pagination, and even reactivity bindings (`storeReactivityBindings()`) are each opt-in feature plugins passed to `tableFeatures({...})` before their state or methods exist at all -- confirmed directly from the bundled `getting-started` and `core` skill docs, not assumed from v8-era training knowledge (the docs themselves flag "Copying the v8 table constructor" as a HIGH-severity common mistake). Critically, none of this reduces the actual work this ticket needed: TanStack Table has no built-in URL-sync mechanism in any version, so the `searchParams` wiring D-003 already built (DEC-049) would be exactly as much custom code on top of TanStack Table's controlled-state API as on top of the hand-rolled version -- adopting it would mean learning and correctly wiring an entirely new headless paradigm (feature registration, row models, reactivity bindings) for zero reduction in the URL-sync code that's this component's actual hard part, in exchange for capabilities (virtualization, column pinning/grouping/faceting) no current ReCodEx list view needs. **Decision**: keep D-003's hand-rolled implementation. This is the same category of decision as F-002's TypeScript 6.0.3 pin -- a brief-specified choice investigated properly and found impractical for a _verified_, documented reason, not skipped by assumption.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Adopting TanStack Table v9 as specified (rejected after reading its actual API -- the onboarding cost of its new feature-registration paradigm doesn't buy back any of the URL-sync work, which stays entirely custom either way, and none of its distinguishing capabilities are needed yet); pinning an older v8 release instead, matching the simpler API the brief likely assumed (rejected -- deliberately installing a superseded major version to chase a specific remembered API shape is the same mistake as writing `middleware.ts` from memory instead of checking the current docs, just inverted; if a future ticket's real needs genuinely call for a headless table engine's specific capabilities, that ticket should evaluate the then-current TanStack Table on its own merits rather than this decision reaching for an old version now).                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DEC-051 | **Form kit: RHF `onSubmit` calling a Server Action directly (not `<form action>`), schema split out of the `"use server"` file**                                                                                                       | D-004. `lib/forms/{action-result,use-server-action-form,use-dirty-guard}.ts`, `components/form/{text-field,form-error}.tsx`. Satisfies brief §4/§9/§10's "React Hook Form + Zod (shared schema), submitting through a Server Action" and BACKLOG.md's "field, error surfacing, pending state, dirty guard". Verified `react-hook-form@7.85.0`/`@hookform/resolvers@5.9.1` first, given DEC-050's TanStack Table lesson still fresh -- both are compatible, unremarkable installs (react peer `^19`, zod peer `^4.0.0`), no repeat of that situation. `useServerActionForm()` wires `useForm` to a schema via `zodResolver` and calls the given Server Action **directly as a function** from `handleSubmit`'s callback, not via Next's native `<form action={serverAction}>`/`useActionState` pattern (`node_modules/next/dist/docs/.../forms.md`) -- RHF already owns submission (validation, dirty/touched tracking, its own `handleSubmit` event handling), so the two submission models would fight over the same event; the action dispatch is wrapped in `startTransition` per Next's own bundled guidance for a Server Action invoked outside `<form action>`. Server-side failures map onto RHF's own error model: `fieldErrors` become per-field `form.setError()` calls, `formError` becomes RHF's `root` pseudo-field (confirmed genuinely supported in `react-hook-form/dist/types/errors.d.ts`, not invented for this ticket). `TextField` covers text-shaped inputs only (text/email/password/number/search) -- other shapes added by whichever ticket needs them, not built speculatively. `useDirtyGuard` covers `beforeunload` (full page unload) only; in-app client-side navigation interception was checked against the bundled Next.js docs and found to have no supported primitive (no `useBlocker` equivalent as of 16.3) -- documented as a real, checked gap rather than built as something fragile.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| DEC-052 | **Repo checkout relocated: `../recodex-web-next` sibling → `ReCOdex/repos/web-next/`, fetched by `pull-repos.sh` -- does not reopen Q-003's original violation**                                                                       | Operator-directed change, made from the compose repo side (this repo's own remote and history are untouched by it -- `git@github.com:jurja00/codeUp-web-ui.git`, still `main`, same commits). The operator wanted to move development to another machine and asked for a single-command bootstrap: clone the compose repo, run its `pull-repos.sh`, `docker compose up`, and have everything -- including this frontend -- come up without a second manual clone step. `pull-repos.sh` was extended (compose repo's own change, not this repo's) to fetch this repo the same way it fetches the upstream ReCodEx repos: into `repos/web-next`, gitignored there, independent of this repo's own name on its remote. The compose repo's `docker-compose.yaml` `web-next` service's `build.context` moved from `../recodex-web-next` to `repos/web-next` to match -- verified live, a real `pull-repos.sh` run + `docker compose build/up web-next` succeeded end to end from the new location.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DEC-053 | **Per-partes upload: chunking stays in the browser, each chunk proxied through a Route Handler**                                                                                                                                       | D-005. `lib/upload/{limits,chunked-upload,use-file-upload}.ts`, `components/upload/file-upload.tsx`, `app/api/upload/partial/route.ts`, `app/api/upload/partial/[id]/route.ts`, `app/api/upload/[id]/digest/route.ts`. Brief §6.7 asks whether the legacy app chunks large uploads and says to reproduce it if so. **It does**, and this reproduces it: `POST /uploaded-files/partial {name,size}` → `PUT /uploaded-files/partial/{id}?offset=N` per chunk → `POST /uploaded-files/partial/{id}` to finalize → `GET /uploaded-files/{id}/digest` verified against a locally recomputed SHA-1 → `DELETE /uploaded-files/partial/{id}` to cancel, with the legacy's own adaptive chunk sizing (64 KiB start, ×2 under 2 s, ÷2 over 4 s, clamped to [4 KiB, 4 MiB]). Read out of `repos/web-app/src/redux/modules/upload.js` + `containers/UploadContainer/UploadContainer.js` and cross-checked against core-api's `UploadedFilesPresenter` (`actionStartPartial`/`actionAppendPartial`/`actionCompletePartial`/`actionCancelPartial`/`actionDigest`) rather than trusting the client alone. **The chunk loop runs in the browser, not on the server**: brief §6.7's wording would also permit "POST the whole file to a Route Handler and let it chunk", but three of this flow's four user-visible properties only exist if the browser keeps the bytes -- real progress (a server-side loop can only report browser→Next transfer, which finishes long before core-api has the file), cancel that stops work actually in flight, and above all the checksum check, which verifies nothing if both sides of the comparison derive from the same server-side copy. The Route Handlers are therefore thin authenticated proxies -- the token is read from the httpOnly cookie server-side on every chunk and never reaches client JS (DEC-021 intact), and the chunk body is forwarded as an unbuffered stream (`duplex: "half"`, required by undici for a stream body), which is still exactly the "Route Handler that streams to core-api" §6.7 mandates and the reason this isn't a Server Action (~1 MB body limit). Size and filename are validated in three places -- component, orchestrator, Route Handler -- because a Route Handler is a public HTTP endpoint and §3.4's "a hidden button is not authorisation" applies to limits too. **One deliberate divergence from the legacy app**: local digest verification degrades to a skip when `crypto.subtle` is unavailable (it only exists in a secure context, so on plain HTTP reached by hostname -- exactly how this stack runs locally -- it is `undefined`); the legacy app throws a bare `TypeError` there. Failing an upload that genuinely succeeded, over a check the environment cannot perform, is the worse behaviour: by that point the file is already complete and durable server-side.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Whole-file POST to a Route Handler that chunks server-side (rejected -- kills real progress, in-flight cancel, and makes the checksum check self-referential, for the sole benefit of one round trip instead of N); no chunking at all, one 512 MiB request (rejected -- this is precisely the "expensive surprise" §6.7 warns about, and it would diverge from an upload protocol core-api already implements and the legacy client already exercises); reproducing the legacy's `TypeError` on a missing `crypto.subtle` for exact parity (rejected -- see the divergence note; recorded here rather than silently softened).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DEC-054 | **Dialogs on the unified `radix-ui` package; destructive confirmation on `AlertDialog`, not `Dialog`**                                                                                                                                 | D-006. `components/dialog/{dialog,confirm-dialog}.tsx`, motion tokens in `app/globals.css`. Brief §9 names Radix for "anything with focus semantics" and a modal is the canonical case (focus trap, focus restoration, `Escape`, `aria-modal`, inert background). **Package choice:** the unified `radix-ui` (1.6.7, React 19 peer verified before installing, per DEC-050's lesson) rather than `@radix-ui/react-dialog`, because D-007 (toasts), D-015 (command palette) and ordinary dropdown/tooltip/tabs work all need primitives from the same family -- one version that moves together beats a dozen independently-drifting ranges, and the tree-shaken output is per-primitive either way. **`AlertDialog` for confirmations** is a behavioural choice, not a naming one: it renders `role="alertdialog"`, moves initial focus to Cancel rather than the first focusable, and refuses outside-click dismissal (`onPointerDownOutside`/`onInteractOutside` are overridden with `preventDefault()`, read out of the installed source, not assumed) -- exactly the three properties a "delete this group?" prompt needs, and all three would have to be re-derived on top of the plain `Dialog`. **Two stale-by-a-version details checked instead of copied from memory:** 1.1.23 sets `aria-describedby` to `descriptionPresent ? descriptionId : undefined`, so the `aria-describedby={undefined}` incantation in every older Radix guide is obsolete here; and this version contains no `console` calls at all, so the well-known "DialogTitle is required" runtime warning no longer fires -- a missing title would now be a silent accessibility failure, which is why `title` is a required prop rather than a convention. **One real bug, found by looking at a screenshot rather than at the code:** the open/close keyframes originally animated `transform: translate(-50%,-50%) scale(...)`, but Tailwind v4's `-translate-x-1/2` compiles to the standalone `translate` property, so both applied and the dialog flew in from half a dialog-width off-centre. Fixed by animating the standalone `scale` property, which composes with `translate` instead of replacing it. Motion is disabled under `prefers-reduced-motion` (brief §9), scoped to these two elements' `animation` rather than a blanket `transition: none`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Per-primitive `@radix-ui/react-*` packages (rejected -- version drift across the many primitives this app still needs, for no smaller install once tree-shaken); vendoring shadcn/ui's dialog wholesale per DEC-004 (rejected for the same reason D-003/D-004 hand-rolled their equivalents: this repo's components are small, token-styled, and carry project-specific decisions, and shadcn's file would need editing on arrival anyway); building the confirmation on the plain `Dialog` with a hand-added `role="alertdialog"` (rejected -- the role is the least important of the three behaviours, and the other two are real logic); a blanket `prefers-reduced-motion` rule killing all transitions (rejected -- it would also kill focus-ring and hover transitions, which carry no motion-sickness risk).                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DEC-055 | **Markdown: react-markdown + two compatibility plugins reproducing markdown-it's raw-HTML and KaTeX-delimiter rules**                                                                                                                  | D-010. `components/markdown/markdown.tsx`, `lib/markdown/legacy-compat.ts` (+ its unit tests). Brief §7: "Markdown is not a free swap... Render a sample of real exercise texts both ways early and log the differences; do not discover them during parity sweep." Done -- 16 constructs rendered through both the legacy renderer (`markdown-it` at its defaults + `@iktakahiro/markdown-it-katex`, exactly as the legacy widget configures it) and the candidate remark/rehype pipeline. **10 of 16 differed.** Two of those would damage authored content and are fixed by plugins; the rest are additive and accepted. **(1) Raw HTML vanished.** Legacy runs with `html: false`, which _escapes and shows_ raw HTML; react-markdown without `rehype-raw` _drops_ it -- `<div class="note">Read <b>carefully</b>.</div>` rendered as nothing at all, and inline `<kbd>Enter</kbd>` rendered as the bare word "Enter". `remarkEscapeRawHtml` converts those nodes to text, reproducing legacy exactly. Note this is _safer_ than the obvious `rehype-raw` fix, which would begin executing markup the legacy app has always shown as inert text -- a new injection surface in supervisor-authored content reaching every student who opens the assignment. **(2) `$` false positives.** `remark-math` is far more eager than markdown-it-katex: `It costs $5 and $10` became garbled mathematics. Measured legacy rules: no whitespace immediately inside the delimiters, and `$$` is display math only when it stands alone (mid-sentence `$$x$$` is literal text). `remarkLegacyMathDelimiters` reproduces both by inspecting each node's original source span -- much cheaper and more predictable than a competing micromark extension. **Accepted differences** (additive, recorded rather than fixed): GFM autolinks bare URLs, renders task lists as checkboxes and supports footnotes, none of which legacy did; strikethrough emits `<del>` rather than `<s>`; HTML entities are re-encoded numerically (identical rendering). Code fences use Shiki, shared with D-009, rather than the legacy `highlight.js`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `rehype-raw` to restore raw HTML (rejected -- it _executes_ what legacy escapes, turning a compatibility fix into a new XSS surface in authored content); disabling `singleDollarTextMath` to stop the `$` false positives (rejected -- it would also kill legitimate `$O(n)$` inline math, which legacy supports and exercise texts use); writing a custom micromark extension matching markdown-it-katex exactly (rejected as disproportionate -- post-processing the parsed nodes gets the same measured behaviour in ~40 lines that a unit test pins); keeping `markdown-it` itself (rejected -- it renders to an HTML string, which means `dangerouslySetInnerHTML` on authored content, the thing this pipeline is chosen to avoid).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| DEC-056 | **No autonomous dependency bots. Dependency review is an operator-invoked step (`pnpm deps:check`), not a scheduled robot**                                                                                                            | Reverses F-027. Brief §4 asks for "Renovate or Dependabot on day one, with `next` and `react-server-dom-*` grouped together", and a `.github/dependabot.yml` was added during the post-Phase-2 review pass. On reaching GitHub it did what Dependabot does: ran immediately against the **default branch** rather than waiting for its schedule, and opened three PRs (`actions/checkout` v4→v7, `actions/setup-node` v4→v7, and one grouped npm update) against `main` while the operator's own work was in a PR from `dev`. The operator's response was that they do not want tooling taking actions on its own, and asked for this to be part of the working process instead -- run on request. **That is the requirement's intent met by a different mechanism**: what brief §4 is actually protecting against is a Next.js security release going unnoticed (its own paragraph is about the May 2026 advisories and Vercel's monthly cadence), not the absence of a bot. `pnpm deps:check` runs `pnpm outdated` and `pnpm audit` together and is run at the start of any session where dependencies are in scope, or whenever the operator asks. Trade-off stated plainly: this is **weaker than automation for latency** -- an advisory published the day after a check goes unseen until the next one -- and stronger for control. GitHub's own Dependabot _alerts_ (the Security tab) are a separate, passive feature that needs no config file and opens no PRs; leaving those on costs nothing and closes most of the latency gap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Keeping the config with `open-pull-requests-limit: 0` (security PRs only, no version noise -- rejected because it still opens PRs on its own, which is the specific thing the operator objected to); dropping only the github-actions ecosystem and throttling npm to monthly (rejected for the same reason, just less often); leaving it as configured (rejected -- three PRs against the wrong branch during an active PR was the trigger).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DEC-057 | **Dashboard sections are stacked, not tabbed; `?tab=` stays unimplemented until there is more than one section to switch between**                                                                                                     | `docs/IA.md` §4.1 asks for both at once: "Both sections are visible simultaneously if applicable. No mode switch." §2's route tree also lists `?tab=student\|teacher\|calendar` as deep-links. Those are compatible -- the deep-link is meant to _scroll to_ a section, not to hide the others -- but only once the sections exist. S-001 ships one of three, so a tab bar with one tab, or a URL parameter that selects the only section there is, would be scaffolding pretending to be a feature. Which section renders is decided by membership (student groups → student half, teaching groups → teacher half), exactly as the sidebar decides its own sections (D-014). S-003 owns turning `?tab=` into real anchor/tab behaviour once the calendar makes three.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Build the tab bar now with two stub tabs (ships dead UI); implement `?tab=` as a filter that hides sections (contradicts "no mode switch" outright).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| DEC-058 | **Group admins are read from `/v1/groups`' `privateData.admins`, because `/v1/users/{id}/groups` does not report them**                                                                                                                | Found during S-001, live, not from the spec: `UsersPresenter::actionGroups`' `supervisor` key is `User::getGroupsAsSupervisor()` -- one specific membership type -- so a user who **administers** a group appears in neither `student` nor `supervisor`. Consequence for already-shipped code: D-014's "My Teaching" sidebar section was empty for _every_ seeded persona, including `sasha.mentor` (admin of Large Lecture) and the superadmin (admin of all four seeded groups); the section had in fact never rendered on this instance, and nothing caught it because an absent optional section looks exactly like a correct one. `getMyGroups()` now unions the `supervisor` list with every group from `/v1/groups` whose `privateData.admins` contains the caller -- the same derivation the legacy app makes from its own `fetchAllGroups`. Costs one extra request per render (memoized per request alongside the existing one); core-api restricts that endpoint to groups the caller can see, so for a normal user it is a handful of rows, and for a privileged one it is the same whole-instance list the legacy app fetches on every page load.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Ask core-api for an `admin` key (a core-api change, out of scope by brief §3.1); infer from `primaryAdminsIds` on the `/v1/users/{id}/groups` payload (present, but only for groups already in one of the two lists -- which is exactly the case that fails); leave "My Teaching" broken until S-002 (would have shipped a second feature on top of a known-empty list).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DEC-150 | **The sidebar's "My teaching" is built from direct membership; every other consumer keeps the inherited one** (operator's own menu)                                                                                                    | core-api inherits group-admin membership down the whole subtree, so an administrator of a department container administers -- genuinely -- every course beneath it, and the operator's menu listed his whole department. `getMyGroups` now returns `teachingDirect` alongside `teaching`: supervisor membership (direct-only in core-api) plus `primaryAdminsIds`, the administrators named on the group itself rather than inherited from an ancestor. **Only the sidebar reads it.** Narrowing `teaching` outright was the obvious move and the wrong one: it has eleven consumers, and five of them ask a different question -- what may this person act on -- where the inherited answer is the correct one. A department administrator would have lost the "waiting for review" rows of courses they are genuinely entitled to review, the "teacher" badge on the group list, and the assignment picker's targets. This does not contradict DEC-058, which rejected `primaryAdminsIds` on the `/v1/users/{id}/groups` payload, where it is present only for groups already in one of the two lists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEC-059 | **The teacher dashboard's review queues link to the solution; they carry no "close review" action of their own**                                                                                                                       | The legacy dashboard puts a close-review button directly in its pending-reviews list. Not reproduced: closing a review is the act of saying "I have read this and I am done", and a control that does it from a summary row, one click away, without the reader having opened the solution, is a worse affordance than the extra navigation it saves. The capability is not dropped -- it belongs to the solution's own review screen (S-018), where the comments being closed are visible. Recorded here rather than in `DROPPED.md` because nothing is dropped: the same action, in a place where it means what it says.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Reproduce the inline button (fast, but the fastest path is to a decision made blind); a confirmation dialog on the inline button (D-006 has one -- but a dialog that asks "are you sure" about something the reader has not seen is theatre).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DEC-060 | **`?tab=` renders the named dashboard section first; it does not scroll to it, and it does not hide the others**                                                                                                                       | Supersedes the placeholder S-001 left in DEC-057. `docs/IA.md` §4.1 says every applicable section is visible at once ("No mode switch"), and §2 documents `?tab=student\|teacher\|calendar` as a deep-link — so the parameter has to mean _something_ without hiding anything. Scrolling was the obvious answer and it does not work here, **measured rather than assumed**: with each section behind its own `Suspense` boundary, both a `#fragment` and a scroll-on-mount effect land correctly and are then pushed back down as the sections above them stream in — a Playwright run reported the calendar heading at the same y=1026 with and without the fragment, i.e. neither scrolled. Ordering is immune to that: the requested section is _first in the document_, so a cold deep-link lands on it with no JavaScript, no fragment, and nothing to undo when the rest arrives. The in-page section nav is separately a set of plain `#fragment` links, which the browser handles instantly and correctly on an already-rendered page — the common case — and which cannot re-suspend anything because they are not navigations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Scroll-on-mount effect (measured: defeated by streaming); server-side redirect from `?tab=` to the fragment form (loops, because a fragment never reaches the server to signal the redirect already happened); dropping `Suspense` so fragments work (trades a documented, real deep-link for a page that waits for its slowest section); real tabs that hide the other sections (contradicts §4.1 outright).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DEC-061 | **Submitting is a page (`/assignments/:id/submit`), not the legacy app's modal**                                                                                                                                                       | An upload that takes real time should not be one stray click on a backdrop away from being lost, and a real URL means the back button, a reload and a shared link all behave. `docs/IA.md` §2 does not list this route, but it lists `/assignments/[id]/edit` and `/assignments/[id]/solutions` -- a leaf under the assignment is the pattern, not an exception. The breadcrumb manifest gained its first static entry under a dynamic parent for it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | The legacy modal (`SubmitSolutionContainer`) -- reproduces a dismissal hazard for no gain; a dialog with a dirty guard -- D-004's guard cannot intercept in-app navigation (its own doc records why), so the hazard would remain.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| DEC-062 | **Source code and its review are their own route (`/solutions/:id/sources`), not a second column on the solution screen**                                                                                                              | `docs/IA.md` §4.4 describes two columns; §2 already lists this route, and the route is the honest shape. The evaluation answers "what happened", the sources answer "what did I write and what did my teacher say about it" -- read at different moments, each long enough to own a screen, and only one of them worth linking someone to a specific line of. Splitting them also keeps the reviewer's client island off the page a student opens to read a test table.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | A two-column layout on `/solutions/:id` (rejected -- the columns are read at different times and the review one is only useful to two people); tabs on the solution page (rejected -- `docs/IA.md` §2 already gives sources their own address, and a tab is not linkable to a line)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DEC-063 | **The code viewer emits tokens, not Shiki's HTML string; the reviewable variant is a `<div>`, not a `<pre>`**                                                                                                                          | D-009 rendered `codeToHtml` output through `dangerouslySetInnerHTML`, which cannot have a comment thread put between two lines. `codeToTokens` gives the same colouring as plain data that crosses the RSC boundary without a grammar, theme or highlighter following it, and React escapes every token as a text node. The annotated variant is a `<div>` because `<pre>`'s content model is phrasing content -- a `<form>` inside one is markup the parser may relocate, which surfaces as a hydration mismatch rather than a warning.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Injecting comments into the highlighted HTML in the browser (rejected -- re-parsing a blob the server already structured); highlighting in the client island (rejected -- brief §4: no highlighter reaches the browser); a comment panel beside the code rather than inline (rejected -- the legacy app anchors comments to lines and so does every tool teachers compare this to)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEC-064 | **A review is hidden from its author until it is closed, even though core-api discloses it**                                                                                                                                           | `canViewReview` is true for the solution's author from the moment a review exists, but the legacy app shows them nothing until `closedAt` is set (`groupReviewCommentPerFile`). That is a product rule, not an oversight: a half-written review is not a verdict, and closing is what counts the issues and emails the author. Reproduced in `visibleReviewComments()` and recorded here rather than left as an unexplained filter -- this is the one place in this app where the UI deliberately shows _less_ than the API would allow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Showing everything the API returns (rejected -- students would read comments mid-sentence and act on them); asking core-api to withhold them (rejected -- brief §3.1, this app does not change the API)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DEC-065 | **Live evaluation progress is a self-refreshing Server Component plus the monitor WebSocket, in that order of importance (S-016, closes DEF-004)**                                                                                     | The monitor is running and proxied at `/ws`, and its channel id is disclosed **once**, in the response to the submit that created the job -- so a WebSocket alone cannot serve a solution page opened at any other moment. The page therefore refreshes itself on a timer (`router.refresh()`, five seconds, stopped when hidden and after five minutes), which re-runs the server render so the result is rendered once, on the server, by the code that already renders it. The socket is layered on top when `?monitor=` is present: it reports the job task by task, connects **directly to the monitor** (bypassing the BFF, as the legacy app does -- it is unauthenticated by design and its messages carry only task states), and uses this deployment's own `MONITOR_WS_URL` rather than the `monitorUrl` core-api returns. The two agree in the container (both are built from `APP_DOMAIN`) and disagree on a bare-host dev machine, where core-api still says `ws://recodex.local/ws` and the browser reaches the stack on `localhost` -- but the reason is not the mismatch: a socket URL that arrives in a response body, or worse in the page's own query string, is a destination this app did not choose. Deployment addresses come from deployment config, in one place. Protocol confirmed against a live job, not docs: client sends the channel id, then `DOWNLOADED`, `STARTED`, one `TASK` per task, then `FAILED`/`FINISHED`; the monitor replays a channel's messages for five minutes, so connecting late loses nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | TanStack Query polling core-api from the browser (rejected -- it needs a browser-reachable API and a second, client-side rendering of the test table that could disagree with the server's; DEC-009 scopes it to "genuinely client-driven" cases and this is not one); WebSocket only (rejected -- no channel id exists for a page opened later, which is most of them); polling only (rejected -- the monitor is right there and the legacy app's task-by-task progress is a capability, not decoration)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| DEC-066 | **S-013 adds what the assignment screen can answer on its own, and no link to a screen that does not exist**                                                                                                                           | `docs/IA.md` §4.3 lists three teacher additions: assignment stats, an "all submissions" link (T-003) and an "edit assignment" button (T-002). The two links lead to screens nobody has built, and a button to a 404 is worse than no button -- so this ticket ships the third, in full: how many students submitted, how many solved it, the mean over scored solutions, and the roster itself with each student's standing. The teacher-only _terms_ come with it (visibility, release date, when it was assigned), as does the exercise-synchronisation state, which is read-only here for the same reason: re-syncing is an edit, and belongs with the other edits (T-002). T-002 and T-003 each add their own link to this page when they land.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Building T-003's solutions list inside this ticket (rejected -- it is a screen with filters, sort and seven view modes, not a section); shipping the links disabled or pointing at placeholders (rejected -- brief §9: no dead ends).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-067 | **The class summary is built from `/assignment-solvers` + the group's `students/stats`, never from the solutions list**                                                                                                                | Three cheap responses answer the whole section: who attempted and how many times (`/v1/assignment-solvers?assignmentId=`), what core-api awarded each student for their best solution (`/v1/groups/{id}/students/stats`, which carries a per-assignment row), and names (`/v1/users/list`). Reading `/v1/exercise-assignments/{id}/solutions` would answer the same questions and drag in every attempt by everyone -- T-003's payload, on a page that shows none of it. **The roster is the stats response, not the solver list**: a solver record survives its owner leaving the group, so building rows from solvers alone would list leavers as students and omit everyone who has not started -- who are the half a teacher opens this section for. The average is over students with a _scored_ best solution rather than over the group, because counting the unstarted as zeros turns "half the class hasn't begun" into "the class is failing".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Computing everything from `/solutions` (rejected, above); `/best-solutions` (rejected -- it answers `null` per student until something is scored, so it cannot say who has _attempted_).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DEC-068 | **A null `status` with a non-zero attempt count reads as `failed`, not `not-submitted`**                                                                                                                                               | `assignmentProgress()` maps a missing best-solution status to `not-submitted`, and Q-012 records why that is the least-wrong answer on the dashboard: the stats row cannot tell "never started" from "every attempt died in the pipeline". This view can. `/assignment-solvers` reports `lastAttemptIndex`, so a student with eleven attempts and no scored solution is labelled as the infrastructure failure it is, rather than as someone who did not turn up -- verified live against the seeded student, whose every attempt fails on this host (DEC-031).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Leaving `not-submitted` for consistency with the dashboard (rejected -- consistency with a known-wrong label is not a virtue where better data exists); a new `AssignmentProgress` state (rejected -- the state already exists and means exactly this).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DEC-069 | **`forbidden()` renders the right page but cannot set a 403 status in this app**                                                                                                                                                       | Measured, not assumed: a probe route whose entire body is `forbidden()` still answers **HTTP 200**, because the app shell streams before the interrupt is raised -- the status line is long gone by then. The Forbidden page itself renders correctly, so the reader is told the truth; only the status code is wrong. This is a property of streaming with a data-fetching root layout, not of where the call sits in the page. Recorded because DEC-034 adopted `authInterrupts` specifically to get "a real 403/401 status code", and it does not, in a route inside `(app)`. Q-016 tracks what to do about it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Checking permission in `proxy.ts` (rejected here -- it would need core-api round trips per request to know the hint); accepting the generic error boundary (rejected -- it says "something went wrong" for a decision that went exactly right).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DEC-070 | **A 403 or 404 on a page's render path is an interrupt, not an error; every other status stays `ApiError` (F-030)**                                                                                                                    | core-api answering "you may not see this" or "this is not here" is not a malfunction, and `error.tsx`'s "Something went wrong. This content could not be loaded" told the reader it was one -- observed live in S-013 when a student opened a hidden assignment. `lib/api/read.ts` is the single boundary: `apiRead` (a GET) and `pageRead` (a read that is already a promise) map 403 -> `forbidden()` and 404 -> `notFound()`, and rethrow everything else untouched. Every read the DAL and the breadcrumb resolvers perform during a render goes through it; `lib/api/client.ts` is unchanged and still throws for everyone else. Three reads deliberately stay on the raw client because each already handles its own refusal, and a `try`/`catch` around an interrupt suppresses it (`forbidden.md`, bundled docs): the dashboard's review queue (403 = an empty queue), a group's ancestors (a crumb this reader cannot see, not a page they cannot see), and one file's content on the sources page. Nothing here decides _whether_ an action is offered -- permission hints still do (AGENTS.md constraint 4); this is only what a reader is shown when they ask for something those hints never offered.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Mapping inside `request()` for every caller (rejected -- an interrupt raised inside a Route Handler is not an answer its JSON caller can read, and inside a Server Action it is eaten by the `catch` that builds the form error, leaving the submitter with no page and no message); swapping the import module-wide so every `lib/api/*` file gets the interrupting client (rejected -- the three self-handling reads above live inside those same files); mapping 400 as well, since a malformed id in a path param can name no entity (rejected -- a 400 is core-api telling this app it sent a bad request, which is this app's bug and should keep looking like one); mapping 401 to `unauthorized()` or a redirect to `/login` (rejected here, logged as F-031 -- `proxy.ts` sends anyone holding a session cookie away from `/login`, so redirecting a stale-but-present session there loops, and the fix belongs where cookies can be written, not in a render).                                                                                                                                                                                                                                                                                                           |
| DEC-071 | **The exams tab is `?tab=exams`, and one exam's lock records are `&exam=<id>` on the same URL (S-008)**                                                                                                                                | `docs/IA.md` §4.2 puts every group screen behind `?tab=`, and S-005 built it that way; the legacy app's separate `/group/:id/exams/:examId` route is a _selection_ within one screen, not a screen of its own -- the page shows the same panel and the same list either way, with one row expanded underneath. Keeping it in the query string means the selection is shareable and the back button steps through it, which is what the separate route bought, without a second route that renders 90% of the first.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A nested route (`/groups/[id]/exams/[examId]`, rejected -- it would need its own copy of the tab shell and would still have to answer `?tab=`); a client-side selection with no URL at all (rejected -- brief §9: "Sharing a URL reproduces the exact view").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DEC-072 | **An exam's phase is computed twice: once on the server to choose what to fetch, once in the browser to notice that it changed (S-008)**                                                                                               | Whether an exam is running is a function of the current second, and the two answers are needed for different things. The server's decides what the tab _is_ -- the roster is worth fetching only during an exam, the lock records only after one -- and it is the honest basis for a page rendered at that instant. The browser's, ticking once a second like the legacy page, decides when that render has gone stale, and refreshes the route rather than re-rendering the tab a second way. The status text renders after mount only (`useSyncExternalStore` with a `null` server snapshot, `DeadlineBadge`'s shape), because a phase printed on the server and reprinted in the browser is AGENTS.md §6.6's mismatch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Deciding it only on the server (rejected -- a page open across the beginning of an exam would keep saying "scheduled", which is the one moment this screen is for); deciding it only in the browser (rejected -- the roster and the locks are server data, and fetching both always would double the tab's cost to hide one of them); polling on a timer regardless (rejected -- nothing changes between phase changes that this page shows, so a refresh with no question to answer is pure load).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DEC-073 | **The seed produces a finished exam the only way it can be produced, and the e2e suite produces its own scheduled one (S-008)**                                                                                                        | core-api records a `GroupExam` at the first student lock, never when a period is set, so "a previous exam with lock records" cannot be fabricated -- it has to be sat. `ensureFinishedExam` opens a minute-wide period, has the seeded student lock in, then ends it; idempotent on the group already having a record, and the period is wider than the sequence so a crash mid-way unsecures the group on its own rather than leaving it in exam mode. The _scheduled_ state is the opposite case and is created by the e2e test itself, which then cancels it: a scheduled exam is a moment in the future, so a fixture would be permanently stale, and cancelling leaves nothing behind because nobody locked into it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Seeding a scheduled exam with a far-future date (rejected -- it would sit in the seeded instance forever, putting every group list and every teacher's dashboard permanently "before an exam"); writing the `GroupExam` row into the database directly (rejected -- the seed is API-only by F-025's own rule, and this would encode a schema this app does not own); leaving both states unfixtured (rejected -- the locks table would then never have been rendered with data, which is the S-015/S-017 trap this project keeps falling into).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DEC-074 | **The Settings tab is the legacy Edit Group screen plus the membership management the legacy app keeps elsewhere; invitations are not part of it (S-009)**                                                                             | `docs/IA.md` §4.2 defines Settings as "edit group, manage members, invitations", and the first two merge cleanly: the legacy app manages supervisors on the group's info page and students on its students page, which are the same question asked about two kinds of person. Invitations are the exception, and deliberately so -- **T-018 already owns them** ("send, revoke, list") and **S-023 owns the page an invitation link points at**. Building the manager here would ship a link to a route nobody has written, which is exactly what DEC-066 refused to do for S-013. Both rows now say so.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Building invitations here anyway and stubbing the acceptance page (rejected -- a stub reached from a mailed link is worse than a feature that is honestly not built yet); leaving membership on the Students tab to mirror the legacy layout (rejected -- the IA is explicit, and S-007's tab is a _roster with points_, which is a reading screen, not an administrative one).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DEC-075 | **A blank name in a language deletes that language's text rather than failing validation (S-009)**                                                                                                                                     | core-api replaces the whole `localizedTexts` array with whatever it is sent (`GroupsPresenter::updateLocalizations`), so the form has to submit every locale at once or silently drop the ones it did not show. Given that, the only question is what an empty field means, and "remove this language" is the answer that matches the data: most seeded groups are named in English only, and requiring a Czech name before an English one could be saved would be this form inventing a rule the API does not have. At least one name must remain, which is the one rule the schema does enforce.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Requiring every offered locale to be filled (rejected -- it would block saving any single-language group, which is most of them); editing only the reader's own locale (rejected -- it would delete the others on every save); a separate add/remove-language control like the legacy form's tabs (rejected for now -- two locales and an empty field say the same thing with no widget).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| DEC-076 | **core-api's user search is `filters[search]`, and a bare `search` is silently ignored (fixed in D-015's route as part of S-009)**                                                                                                     | `/v1/users` reads its search term out of a `filters` object (`UsersPresenter::actionDefault` passes `['search', 'instanceId', 'roles']` to `getPagination`) and **ignores an unrecognised top-level `search` without complaint**, answering with the full user list. The command palette has therefore been listing arbitrary users since D-015 -- plausible-looking output, which is why it survived review. S-009's member picker made it undeniable: a picker that ignores what you type is unusable rather than merely odd. Fixed in `app/api/search/route.ts`; `/groups` and `/exercises` genuinely do take a bare `search`, re-checked live rather than assumed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Passing `filters[search]` for every endpoint uniformly (rejected -- `/groups` returns a plain array and takes the bare parameter; changing what works to match what did not is how the next silent mismatch gets introduced); leaving the palette alone and only fixing the picker (rejected -- same route, same bug, and the palette's version is the one a user would report).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DEC-077 | **The similarity view marks exact character ranges as plain text, not lines in `CodeViewer` (S-019)**                                                                                                                                  | A detection tool reports matches as **character offsets into the file** (`SimilarityFragments`: pairs of `{offset, length}` references). D-009's `CodeViewer` highlights whole lines, so routing this through it would widen every match to line boundaries -- turning "these 24 characters matched" into "these three lines matched", a claim the tool did not make, on the one screen where the precise extent is the whole point. `MarkedSource` renders the file verbatim with `<mark>` on the reported ranges, merging overlaps first (two fragments sharing a passage would otherwise nest into a darker patch that means nothing). The cost is syntax colouring, which is not what a reader of this screen is looking for.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Mapping offsets to line ranges and reusing `CodeViewer` (rejected -- see above); rendering only the matched excerpts side by side (rejected -- a reader has to see how much of the file the match covers, which an excerpt hides); teaching `CodeViewer` character ranges (rejected for now -- it would complicate the component every other screen uses for the benefit of this one).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-078 | **The plagiarism fixture is the upload a detection tool would have made (S-019)**                                                                                                                                                      | ReCodEx detects nothing itself: `PlagiarismDetectedSimilarity` rows exist only because an external tool `POST`ed them, and core-api creates the tested solution's batch link inside `actionAddSimilarities`. So the only way this screen has ever had data -- here or in production -- is an upload, and the seed performs exactly that one: a second student, a near-identical solution, a batch, one similarity, `uploadCompleted`. Fragment offsets are computed from the two sources rather than written down, so they stay correct if either seeded solution is edited. Idempotent on a batch already existing for the tested solution, because **there is no endpoint that deletes any of it**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Leaving the screen unfixtured (rejected -- it would join S-015's test table and S-017's truncation notice on the list of things never rendered with data, which this project keeps regretting); inserting rows into the database directly (rejected -- F-025's seed is API-only, and this would encode a schema this app does not own); having the e2e suite upload its own batch per run (rejected -- nothing can delete it afterwards, so every run would leave another).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| DEC-079 | **Shadow assignments are a list beside the group's assignment table, not rows in it (S-020)**                                                                                                                                          | Every column of the assignments table is about a submission -- deadline, second deadline, the reader's own status -- and a shadow assignment has no submission at all: its points are typed in by a teacher. Merging them would mean four empty columns and a status column that has to invent a value, on the one screen where a student decides what to work on next. The legacy group screen keeps them in two tables for the same reason, and the assignment filter (all/open/closed/submitted) deliberately does not apply to the second list, because none of its four states can.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Rows in the same table with blanks (rejected -- see above); a tab of their own (rejected -- most groups have none, and a permanently empty tab is worse than a section that is simply absent); leaving them unlisted and reachable only by URL (rejected -- that is the "screen nobody can navigate to" this project keeps refusing to ship).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DEC-080 | **A user object carries no permission hints, so the profile asks and reads the refusal (S-021)**                                                                                                                                       | Every other screen in this app decides what to offer from `permissionHints` on the entity (brief §3.4). A user has none: `/v1/users/{id}` returns `permissionHints: null`, verified live for a superadmin reading a student, because core-api gates the _fields_ instead -- `privateData` is built only under `canViewPrivateData`, and `/v1/users/{id}/groups` simply 403s under `canViewGroups`. So the profile renders whichever account rows arrived, and **attempts** the group list, reading a 403 as "not disclosed to you" (an absent section) rather than as a broken page. Same shape as the teacher dashboard's review queue, and for the same reason: asking core-api and believing the answer cannot drift from its rule.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Inferring visibility from the reader's own role (rejected -- brief §3.4's "never infer capability from a role name", and it would be wrong for a supervisor in one group reading a student in another); rendering blank rows where data is missing (rejected -- an empty "Email:" line states something false about the account rather than about the reader); letting the 403 reach F-030 and refuse the whole page (rejected -- the reader may legitimately see the person's name, which is what a roster link promised them).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DEC-081 | **`/profile` renders the profile; it does not redirect to `/users/:id` (S-021)**                                                                                                                                                       | "My profile" is the same screen about oneself, so one component serves both routes. The first version redirected, which cost an extra round trip on every visit and -- worse -- left the page mid-navigation for anything that looked at it: the token-leakage test began failing **intermittently**, with "the page is navigating and changing the content", which is the most expensive kind of failure to diagnose later. Rendering directly removes the race instead of waiting it out in the test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Redirecting (rejected, see above); duplicating the page at both routes (rejected -- two copies of a screen drift); linking the sidebar straight at `/users/{me}` and dropping `/profile` (rejected -- it is a stable, bookmarkable URL that does not change identity when the reader does, e.g. after a takeover).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEC-082 | **Changing a password signs the reader out, and lives in its own form (S-022)**                                                                                                                                                        | core-api sets a token validity threshold on every password change (`UsersPresenter::changeUserPassword`), so the token that made the request is dead the moment it returns -- every later click would 401, on a page that still looks signed in. The form therefore ends with this app's own logout route and a trip to `/login`, which is the honest reading of what just happened. It is a **separate form** from the profile for the same reason: name, email and the Gravatar toggle go through the same endpoint, and folding them together would mean losing a session for correcting a name.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Leaving the reader on the page and letting the next request fail (rejected -- a silent 401 minutes later is the worst version of this); refreshing the session silently by re-logging in with the new password (rejected -- the app would be handling a plaintext password after the fact, for a convenience nobody asked for); one form for the whole account (rejected -- see above).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DEC-083 | **A group invitation is offered on core-api's own hint, never on the reader's role (S-023)**                                                                                                                                           | The legacy page gates the whole screen on `isStudentRole(effectiveRole)` and tells anyone else that "the invitation links are available only to students, supervisors may not use them". core-api does not agree: `acceptInvitation` is granted to the `student` role and every other role inherits it, verified live -- a supervisor, a supervisor-student and a superadmin all read `acceptInvitation: true` on the same invitation. AGENTS.md constraint 4 settles which of the two is the answer: the hint is, and a role name is never a capability. So the button follows the hint, and the page **states what accepting does** ("enrols you as a student of this group") rather than refusing a reader core-api would have accepted. The two conditions `checkAccept` applies that the hint does not carry -- the link has not expired, the group is not organizational -- are mirrored here, because a button that 403s is worse than one that is not offered. `archived` is deliberately **not** mirrored: `group.isNotArchived` is already a condition on the hint's own rule in `permissions.neon`, and a second copy of it here would be free to drift.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Reproducing the legacy role gate (rejected -- it presents as authorisation something core-api does not enforce, and it would refuse a supervisor-student, whom legacy itself allows and who is a real student in other groups); offering the button unconditionally and letting core-api's 403 explain (rejected -- F-030's point is that a refusal the reader could have been spared is a defect, not an error page).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-084 | **The invitation token is decoded on the server; the raw token goes back to the form untouched (S-024)**                                                                                                                               | The legacy page decodes the invitation JWT in the browser and picks its `usr` array apart there. Here `lib/auth/invitation-token.ts` does it in a Server Component and the page renders four resolved strings, so no JWT parsing ships to the client. The **raw** token is still handed to the client form as a prop, and that is not the boundary violation brief §5 describes: it is already in the address bar the browser is sitting on, and the form's whole job is to send it back to core-api. What does not cross the boundary is the parser and the payload -- which is the part a later change could get wrong.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Reading `window.location.search` in the form instead of passing a prop (rejected -- same value, one more thing to keep in step with the page's own parse); decoding in the browser as legacy does (rejected -- ships a decoder and the claim-shape validation to every visitor for no gain).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DEC-085 | **Nothing verifies the invitation token before core-api does, because nothing here can (S-024)**                                                                                                                                       | core-api signs the token with `accessManager.verificationKey`; this app does not hold that key and should not -- a second copy of core-api's signing secret in the BFF is a second thing that can leak, for no authority it does not already have. There is no endpoint that validates an invitation token either (checked against the whole spec). So the page decodes for **display only** and grants nothing on the strength of it: the account is created by `POST /v1/users/accept-invitation`, which checks the signature it wrote. A forged token therefore renders a name and an email its own author chose, followed by a refusal. That residual is real and is written up in `QUESTIONS.md` Q-018 rather than papered over.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Verifying locally with a shared key (rejected -- duplicates core-api's authority and its secret); refusing to render anything until the submit succeeds (rejected -- the details are how a recipient tells a real invitation from a stale one, and hiding them until after a password is typed helps nobody).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DEC-086 | **Every call that turns a core-api token into a session cookie is a Route Handler under `app/api/auth/` (S-024)**                                                                                                                      | Accepting an invitation returns `{user, accessToken}` -- core-api's own comment says "so the user can log-in right away" -- so it is a registration and a login in one call and has to set the session cookie. It joins login (F-016), the external-auth callback (F-019) and takeover (F-020) in the auth BFF rather than becoming a Server Action, which makes `establishSession()` have exactly one class of caller. S-022 had already reached for the same rule from the other side, signing out through `/api/auth/logout` instead of clearing the cookie inside its own action. The cost is that S-024's form is hand-rolled rather than using the D-004 form kit, whose contract is a Server Action -- a small, visible price for keeping session minting in one place.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | A Server Action (rejected -- `cookies().set()` works there, so this is not a technical limit, but it would scatter session creation across two mechanisms and make the auth surface harder to audit than it is worth).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-087 | **Shadow assignments get their own dashboard table, and stay out of the calendar (S-025)**                                                                                                                                             | Their points were already on the dashboard -- core-api folds shadow points into the group totals the progress cards read (`GroupViewFactory::getStudentStatsInternal`), so "8/40" already counted them. What was missing is which ones they are. They are a table of their own for the reason DEC-079 gave when the group screen faced the same question: every column of the deadline table is about a submission -- status, points so far, whether a second deadline still applies -- and none can be filled in for something with nothing to submit. They are also deliberately **absent from the calendar**: a shadow deadline is the supervisor's own note about when the work should happen, and core-api's documentation says the supervisor decides whether it was breached -- drawing it on a grid beside enforced deadlines would state something about it that is not true. For the same reason the column is labelled "Deadline (informative)" and carries no urgency badge and no relative time.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Rows in the upcoming-deadlines table (rejected -- four blank columns and an invented status, the exact merge DEC-079 refused); points only, with no names (rejected -- that is the state before this ticket, and the backlog row exists because it is not enough); a calendar entry (rejected as above).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DEC-088 | **`StatusState`'s title takes a heading level, because an empty panel is not a section (S-025)**                                                                                                                                       | It hardcoded `<h2>`, which is right where it was first used -- `not-found.tsx`, `forbidden.tsx` and `unauthorized.tsx` render nothing above it -- and wrong inside a dashboard panel whose own heading is an `<h3>`: the outline jumped, and "Nothing is due" read as a peer of "My studies". Found by a spec that enumerates the page's `h2`s and had been failing intermittently on exactly the persona with no open deadlines. `headingLevel` defaults to `2`, so nothing else moved; the two dashboard panels pass `4`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Rendering the title as a styled `<p>` (rejected -- the full-page states would then have no heading at all); teaching the spec to ignore that one heading (rejected -- it is the outline that is wrong, not the assertion).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| DEC-089 | **A dead session cookie is cleared, and the two ways it dies are handled in two different layers (F-031)**                                                                                                                             | `proxy.ts` tested the cookie for _presence_, so a token past its own `exp` counted as a session: `requireSession()` sent the reader to `/login`, `proxy.ts` saw the cookie and sent them to `/dashboard`, and round again -- **an infinite redirect loop**, reproduced with `curl` before anything was changed (12 hops and still going; a browser shows `ERR_TOO_MANY_REDIRECTS`, so the reader got no page at all, not even the error page this ticket was filed about). Both layers now call one shared `isSessionTokenLive()`, and `proxy.ts` clears what it rejects -- it is the only layer that both sees the request and can write a cookie. The other death is invisible to it: core-api can refuse a token whose `exp` has **not** passed, because a password change sets a validity threshold (S-022) and an administrator can invalidate every token a user holds. That answer arrives as `401` during a render, where no cookie can be written, so `lib/api/read.ts` redirects out to `app/api/auth/session-expired/route.ts` -- which clears and lands on `/login`. Two mechanisms because there are genuinely two failures, not because one was insufficient.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `unauthorized()` and `app/[locale]/unauthorized.tsx` (rejected -- it renders a page while the dead cookie is still set, so every link on it leads back into the loop; the honest response to a dead session is re-authentication, not a description of one); `redirect("/login")` from the render (rejected for the same reason -- that _was_ the behaviour, and it is what looped); verifying the cookie's signature locally (rejected -- it would need core-api's signing key, and would still not know about a validity threshold set after the token was issued).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-090 | **Joining and leaving a group are offered from the group's own fields, because core-api publishes no hint for them (S-026)**                                                                                                           | Every other action in this app is gated on `permissionHints`. These two cannot be: `addStudent` and `removeStudent` are **absent from a group's hints entirely** -- confirmed live, the keys are missing rather than `false` -- because both ACL rules are written against a _student_ subject (`student.isSameUser`, `student.isNotGroupLocked`) and a hint computed for the group alone has nobody to put in that slot. Same shape as DEC-080's user objects, and the same answer: ask what can be asked, and let core-api decide for real. Here that means restating the ACL's own conditions from fields the page already holds -- `public`, `archived`, `organizational`, `detaining`, `exam`, the reader's `myStats` and their `groupLock` -- and calling the endpoint, which refuses authoritatively (joining a non-public group answers 403, verified live). The actions themselves take **no user id**: it comes from the session, so there is nothing in the request that could enrol somebody else. The offer's shape is the legacy screen's own -- including that a group's admins and supervisors are shown neither control, which core-api would permit and which for the administrator of a course is a misclick rather than an intention.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Offering both controls always and letting the 403 explain (rejected -- F-030's rule, a refusal the reader could have been spared is a defect); deriving from the reader's global role (rejected -- brief §3.4, and it is wrong anyway: whether a group is public has nothing to do with who is asking); adding the two calls to S-009's member manager instead (rejected -- that screen is a teacher putting _other people_ in and out, gated on `update`, which is a different question with a different answer).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEC-091 | **An invitation link is printed in full, from the request's own `Host` (T-018)**                                                                                                                                                       | The link _is_ the product of this screen -- a teacher pastes it into a course page -- so it is shown as text rather than hidden behind a copy button. Building it needs an origin, and there are three wrong ways: `window.location` in the client component (a hydration mismatch, and the value is known on the server anyway), `API_BASE_PUBLIC` (that names core-api -- the same origin in the compose deployment and a _different port_ in local development, so the link would work in production and quietly not on a developer's machine), and `request.url` (DEC-039's standalone-build bug). `requestOrigin()` reads `Host` and `X-Forwarded-Proto` from `headers()`, which is the same reasoning `buildAbsoluteUrl()` already uses for redirects, now available to a Server Component that has to render a URL rather than go to one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A copy-to-clipboard button over an invisible value (rejected -- a teacher checking what they are about to paste cannot; it can be added on top of a visible link later); an env var for the public origin (rejected -- one more thing to configure wrongly, when the request already carries the answer).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| DEC-092 | **An assignment's settings are one form and one save, and the page checks `update` itself (T-002)**                                                                                                                                    | core-api's `actionUpdateDetail` **replaces the assignment with what it is sent** -- there is no partial update, so every save carries every field it reads and a field omitted is a field reset. Splitting this into S-022's four independent forms would hide that rather than help: each would still have to send the whole thing. `version` rides along as core-api's own optimistic lock (`400-010`), whose message is surfaced verbatim rather than retried -- the honest response to "someone else saved first" is to reload and look. The **page's own permission check is explicit**, not `apiRead`'s 403: reading an assignment is something a student may legitimately do (it is their assignment), so the fetch succeeds for them and only the save would be refused -- found by this ticket's spec, which walked a student straight into a filled-in settings form. The re-sync joins it here rather than being its own screen, and syncs **everything**: core-api accepts a list of parts, and offering one would ask a teacher to choose between "score config" and "exercise config".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Separate forms per section (rejected -- each would still POST the whole assignment, so the split would be cosmetic and the reader would not know which save clobbered what); retrying on a version conflict (rejected -- that is silently overwriting the other person); relying on `apiRead` for the refusal (rejected -- it never fires, because the read is permitted; the gap was real and only the explicit check closes it).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEC-093 | **Assigning an exercise creates it immediately and lands on its settings; the catalog is searched server-side (T-001)**                                                                                                                | core-api has no call that creates an assignment _and_ configures it -- `actionCreate` takes an exercise and a group and applies its own defaults -- so the choice is between a wizard that holds the settings in the browser until the end, and creating first and going straight to T-002's form. The second is what the legacy app does and what fails better: nothing is lost if the tab is closed halfway, and the assignment is created **invisible**, so it harms nobody while it is half-configured. It also means there is one settings screen rather than a create-shaped copy of it. The picker's **search is a server round trip**, unlike every other list in this app: `/v1/exercises` is genuinely paginated and an instance can hold thousands, so `getGroupList`'s fetch-everything trade (Q-015) does not carry over. Three of the five conditions on assigning are visible to the picker (the exercise's `assign` hint, locked, broken), the group's `assignExercise` gates the page, and the fifth -- an exercise with no reference solution -- is in no list payload, so core-api's own message is what the reader gets on the attempt. **Corrected in T-020: it is in the payload.** `hasReferenceSolutions` is right there on every row of `/v1/exercises`, found while building the catalog on the same endpoint, so the picker now says so before the click and this paragraph is wrong about the fifth condition only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | A wizard that collects the settings before creating (rejected -- it duplicates T-002's form and loses everything if the tab closes); client-side filtering over the whole catalog (rejected -- it is the one list here that cannot be assumed small); predicting the reference-solution rule (rejected -- a request per row to learn something core-api says for free on the attempt).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-094 | **One student's submissions are one table with an Assignment column, not a stack of tables per assignment (T-005)**                                                                                                                    | The legacy screen defaults to a box per assignment and keeps that choice in `localStorage`, with a flat sortable table behind a toggle. Here it is the flat table always, and **sorting by the Assignment column is the grouping** -- which puts the choice in the URL where it is shareable (brief §9, the same correction `DataTable` already made to the legacy `SortableTable`), and adds what the boxes cannot do: a filter box that narrows to one assignment by name, or to a note across all of them. "Best solutions only" survives as a real filter, server-side in `?filter=`, because no sort expresses it. The table itself is **T-003's**, with a `lead` mode swapping the first column between author and assignment: six of the seven columns are the same question asked of the same rows, and a teacher moving between the two screens should not have to re-learn the table. Who is offered the drill-down is the legacy roster's own rule -- staff of the group, or one's own row -- restated from fields the page holds, because `viewStudentStats` is written against group _and_ student and so has no hint of its own (DEC-090's shape). core-api decides for real: a student reading a classmate's page is refused, verified live.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | A stack of collapsible boxes per assignment (rejected -- it is the legacy shape, and its grouping is unshareable and unfilterable); a second table component for this screen (rejected -- the columns would drift, and the two screens are the same rows sliced differently); gating the drill-down link on `viewStats` (rejected -- it is granted to a student wherever a group publishes its stats, so it would offer every student a link to every classmate's page and a 403).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEC-095 | **The points export is a Route Handler serving CSV, semicolon-separated with a BOM (T-007)**                                                                                                                                           | The legacy app builds the file in the browser from what the table already holds. This one is a link to `/api/groups/{id}/points`: it works with no JavaScript, it can be bookmarked or curl'd, the numbers are **re-read from core-api at the moment of the download** rather than being however old the page is, and core-api re-checks the reader on that read -- a hidden button is not authorisation. Semicolons and a UTF-8 BOM because the file's first reader is Excel on a machine whose list separator is `;`, which is exactly what the legacy export produced, so an existing sheet keeps splitting into the same columns; the BOM is the difference between `Jiří Novák` and `JiÅ™Ã­`. **CSV only**: a real `.xlsx` means a new dependency and a hand-rolled ZIP container for a capability the legacy app never had -- its "Excel" export _is_ this file. Three deliberate differences from the screen: **shadow assignments are columns** (their points are inside every row total, and a file whose columns do not add up to its own total is one somebody will spend an afternoon disbelieving -- the screen can explain itself in prose, a CSV cannot), emails ride along where core-api discloses them (same `/v1/users/list` response, no extra request), and a quote inside a field is **doubled per RFC 4180** rather than backslash-escaped, which is what the legacy `escapeString` does to a CSV field and is simply broken for a name like `O"Brien`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | A client-side `Blob` download (rejected -- needs JavaScript, and ships whatever the page was rendered with); an `.xlsx` writer (rejected -- a dependency, or a hand-written ZIP, for no capability legacy had); comma-separated per RFC 4180 (rejected -- it would split into one column in the Excel this deployment's users run, and break every sheet built on the legacy file); reproducing the legacy backslash escaping (rejected -- nothing downstream can depend on it, because nothing could parse it).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DEC-096 | **The submission-failure log opens on what is unresolved, not on everything (T-019)**                                                                                                                                                  | core-api serves both lists (`/v1/submission-failures` and `/v1/submission-failures/unresolved`) and the legacy screen only ever asks for the first. That list is every failure ever recorded, unpaginated and unfiltered, and it only grows -- this dev instance is already at fifty-odd because its sandbox cannot run at all. The screen is a **queue**: a failure somebody has already dealt with is a record, not a task, and the reader who opens this page is here to work through what is left. "Everything" is one click away and keeps the history reachable, so nothing the legacy page could show is lost. Resolving is its own decision inside this one: core-api has **no un-resolve call**, so it is a dialog with a typed note rather than a one-click button, and the notification email that rides along defaults to **off** -- an email is the irreversible half of an already irreversible action.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Defaulting to the whole history like the legacy page (rejected -- the list grows without bound and buries the work); filtering resolved rows out client-side (rejected -- it fetches everything to throw most of it away, when core-api already offers the narrower list); a confirm dialog on top of a one-click resolve (rejected -- the note _is_ the confirmation, and typing it is what makes somebody read the row first).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DEC-097 | **The exercise catalog searches, filters, orders and pages in core-api, and creating one is not on it (T-020)**                                                                                                                        | This is the one list in this app that is **not** fetched whole -- the inversion of Q-015's trade, and deliberately: `/v1/exercises` is genuinely paginated, an instance can hold thousands, and core-api collates by name in the reader's own locale, which a page of twenty sorted in the browser cannot. So every control is a query parameter: a plain `GET` form, two pager links, no JavaScript needed, and a narrowed view that is an address somebody can send to a colleague. Getting the parameter _names_ right turned out to matter more than the design: core-api reads its filters from a **`filters` array** (`filters[search]`, `filters[archived]`, `filters[runtimeEnvironments][]`), and an unknown top-level parameter is silently ignored -- which is how T-001's picker shipped with a search box that did nothing (fixed here, and its spec now asserts on an exercise that must disappear). **Creating an exercise is not on this screen**: `POST /exercises` makes an empty, broken, unassignable exercise, so it belongs with T-008's editor the way assigning belongs with T-002's settings (DEC-093) -- a create button here would drop the reader on a screen that does not exist yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Fetching the catalog whole and filtering client-side with `DataTable` (rejected -- it is the one list that cannot be assumed small, and the sort would be a sort of the visible page); keeping the filters in component state (rejected -- brief §9, the view has to be an address); putting the create form here as the legacy page does (rejected -- it leads to `/exercises/{id}/edit`, which is T-008 and does not exist; DEC-066's rule).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEC-098 | **A new exercise is created empty and configured afterwards, and its settings screen is gated on more than `update` (T-008)**                                                                                                          | core-api's `actionCreate` takes a **group and nothing else**: it applies defaults, names the exercise "Exercise by <author>" in the author's own language and leaves it without tests. There is nothing a wizard could usefully collect first, so creating lands the reader on the settings form with the exercise already real -- DEC-093's shape, and it fails the same way better: nothing is lost if the tab closes, and a new exercise is **broken by construction**, so nobody can assign it and no student can meet it in the meantime. The settings save then carries every field, because core-api replaces the exercise with what it is sent, with `version` as the optimistic lock whose `400-010` is surfaced rather than retried (DEC-092, second time). Tags, groups, archiving and deletion are **not** fields of that save -- each is its own call that takes effect immediately -- so they sit beside the form rather than in it. **The page is gated on any of `update`, `archive` or `remove`, not on `update` alone**, and that is not a nicety: core-api's update rule carries `exercise.notArchived`, so archiving an exercise **removes `update`** -- gating on it locked the reader out of the very screen holding the button that undoes it. Found by this ticket's own spec, on the step after archiving. Attaching and detaching a group have no hint at all (both rules are written against the exercise _and_ the group, DEC-090's shape), so the offer is built from the groups the reader teaches and core-api decides for real; detaching the last group is not offered, because `exercise.hasAtLeastTwoAttachedGroups` refuses it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A create form that collects the name and text first (rejected -- it duplicates the settings form and loses everything if the tab closes; the exercise is unassignable until it has tests anyway); folding tags and groups into the settings save (rejected -- they are separate endpoints with no version, so the form would have to fake a transaction it does not have); leaving the archive control on the detail screen only (rejected -- it belongs with the other things that change the exercise, and the gate fix makes it work where it belongs).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| DEC-099 | **The sign-in form posts to the Auth BFF Route Handler, and `?from=` is a path on this app or nothing (A-002)**                                                                                                                        | Every other form in this app goes through a Server Action; this one does not, and the reason is the cookie. Brief §5 puts login in a Route Handler precisely because that is what receives the credentials, calls core-api and sets the httpOnly session -- and that route has existed since F-016, used by the external-auth callback and by every e2e run. A Server Action wrapper in front of it would add a hop and a second thing to keep in step. So the form is a plain `fetch`, and `router.refresh()` runs before the navigation because the app shell is a Server Component that reads the session and would otherwise render the next page still believing nobody is signed in. **`?from=` is an open-redirect guard first and a convenience second** (`safeRedirectTarget`, unit-tested): `proxy.ts` writes the refused path there, and anything that is not a single-slash path on this app -- `//evil.example`, an absolute URL, a backslash form some browsers normalise -- becomes the dashboard instead. A sign-in form that forwards the browser wherever a link says is a phishing tool. **Short sessions** are core-api's own `expiration` parameter, offered only where the deployment sets `SHORT_SESSION_MINUTES` (the legacy `SHORT_SESSION` config, same unit) and computed **server-side**: core-api caps it anyway, but a client that names its own session length is a contract this app does not need.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A Server Action calling the route (rejected -- two layers doing one thing, and the cookie still has to be set in the route); `redirect()` from the server after a form POST (rejected -- the credentials would ride a full page navigation and the failure case loses what was typed); trusting `from` (rejected -- it is attacker-controlled by construction, since anybody can link to `/login?from=`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| DEC-100 | **A password reset says the same thing about every address, and this app answers the address core-api's own emails point at (A-004, A-005)**                                                                                           | core-api's `ForgottenPasswordPresenter::actionDefault` throws `NotFound` for an unknown login. Passing that through would turn the reset form into an **account-existence oracle** -- type addresses, read which ones exist -- so the route swallows it and the page says "if we know that address, a message is on its way", which is true and is all anybody needs. The second half is a deployment fact, not a preference: `WebappLinks.php` builds the emailed link as `"%webapp.address%/forgotten-password/change?{token}"`, this app's IA spells that screen `/forgot-password/change` (F-013), and **the token is the bare query string** -- so `proxy.ts` forwards the old address, query intact, and `readQueryToken` (extracted from S-024's invitation page, which receives its token the same way) reads it at the other end. Otherwise every deployment would have to override core-api's `linkTemplates` before its own password-reset emails worked, and the failure would be a 404 nobody could explain. Done in `proxy.ts` rather than in a page calling `redirect()`: a page must await `searchParams` first, by which point Next has begun streaming and falls back to a one-second `<meta refresh>` -- verified, not assumed. The token itself is **never a session**: it is the bearer for exactly one call, and core-api invalidates it (and every other token that user holds) the moment the password changes, which is why the reader is sent to sign in rather than signed in.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Forwarding core-api's 404 to the reader (rejected -- it is the oracle); renaming this app's routes to `/forgotten-password` (rejected -- it churns the IA, the specs and the docs to work around a template that a redirect handles); asking the operator to set `linkTemplates` (rejected as the _only_ answer -- worth doing, but the app should work before anybody does); establishing a session from the reset token (rejected -- it is scoped `change-password`, and dead by the time the change returns).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DEC-101 | **The configuration editor edits the simple configuration only, and refuses to rewrite an advanced one (T-009)**                                                                                                                       | core-api stores two kinds of exercise configuration. A `simpleExerciseConfig` is a set of values per test, and the instance's own pipelines are chosen for it; an `advancedExerciseConfig` is built from **hand-picked pipelines**, which is what the legacy screen's second half exists for. This screen writes the first kind. For the second it renders the tests -- those are shared and safe -- and says plainly that the rest is left alone, because `writeSimpleConfig` rebuilds the pipeline list from `relevantPipelines()`: saving an advanced exercise through it would silently replace the author's pipelines with the standard ones and lose everything configured on them, and core-api would accept the result. The refusal is **in the Server Action as well as the UI** (`updateExerciseConfig` re-reads `configurationType`), because a hidden form is not a safeguard -- and here the boundary has to be this app's, since core-api has no rule against it. The advanced editor, the `/config/variables` call it needs and the switch between the two kinds are T-024; the switch matters, because there is currently no way _out_ of an advanced configuration from this app.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Rendering the advanced editor read-only (rejected -- it is most of the work of building it, with none of the use); offering the simple form anyway with a warning (rejected -- the warning is read once and the loss is permanent); refusing the whole screen for an advanced exercise (rejected -- the tests and the score are shared, and locking somebody out of renaming a test because of how its pipelines were chosen is a worse answer than saying which half is unavailable).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-102 | **The configuration is read permissively and written normalised, and variables the editor does not know are kept (T-009)**                                                                                                             | Reading looks through every pipeline a test names and takes the first variable of each name, ignoring the pipeline filters entirely -- so a configuration written by hand, by the legacy advanced editor, or by an older vocabulary still loads into the form. Writing does the opposite: it rebuilds the pipeline list for each environment from the instance's catalogue and places each variable in whichever pipeline _declares_ it (`isCompilationPipeline`, `hasEntryPoint`, `hasSuccessExitCodes` -- flags core-api does publish, which is the one part of this contract that is not guesswork). The asymmetry is the legacy behaviour and it is deliberate: **saving is not a no-op**, it normalises. What keeps that from being destructive is `mergeVariables()`, which starts from the variables already in that pipeline and lets the form override by name -- so a variable this app has never heard of survives a save it was not part of. Verified as a real round trip against a live two-environment exercise, not only in unit tests: read, write, re-read, identical.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Writing only the variables that changed (rejected -- core-api replaces the configuration whole, so there is no such call); refusing to save a configuration this editor did not produce (rejected -- it would strand every exercise the legacy app configured); dropping unknown variables (rejected -- it makes this app's vocabulary the ceiling of what an exercise may hold).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| DEC-103 | **A custom score expression is shown and left alone, not converted to an average (T-009)**                                                                                                                                             | core-api has three score calculators: `uniform`, `weighted` and `universal`, the last being an expression tree over test results with a 3,000-line editor in the legacy app. This screen offers the two averages. An exercise already using an expression keeps it: the calculator radio is not rendered for one, so there is no control that would throw the expression away. The legacy app _does_ allow switching away (behind a confirmation), and this is deliberately narrower -- because this app cannot write an expression back, offering the switch would build a one-way door out of a state it cannot restore, which is the same defect S-026 and T-001 were filed for. The tests themselves stay editable, since the weights are not what an expression refers to. **Superseded by T-025**, which built the editor: all three calculators are now offered, switching _to_ the expression is seeded from the current weights and so is lossless, and switching _back_ names the weights the expression becomes where it is an average and says it will be lost where it is not. The reasoning here was right for as long as this app could not write an expression; the door is not one-way any more.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Offering the switch with a confirmation, as legacy does (rejected -- reversible there, one-way here); hiding the whole tests section for such an exercise (rejected -- renaming a test is unrelated to how the score is summed); silently rewriting the expression as a weighted average by extracting weights the way legacy's `attemptExtractWeights` does (rejected -- it only works for expressions that happen to _be_ an average, and guessing wrong rewrites the grading of a live exercise).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| DEC-104 | **The limits grid is the one form in this app that is not React Hook Form (T-010)**                                                                                                                                                    | Every other form here is RHF with a Zod resolver, and this one is the exception that rule needs. Its fields are a **grid whose shape is data** -- a cell per test per language, both of which T-009's screen changes -- and its three copy controls each write a whole row, column or grid in one gesture. A `useState` object with one `setCell` expresses that; a resolver over a record of records would have to be rebuilt whenever the tests change and would still miss the thing that actually gets a save refused, which is not any single cell but the **sum of a column**: a hardware group caps the time of one test _and_ of the whole exercise in one language. So validation is a pure function over the whole grid (`validateLimits`), rendered as marked fields plus a running total per language, and re-run in the Server Action against ceilings read fresh from core-api rather than taken from the client.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Forcing it into RHF with `useFieldArray` (rejected -- a two-dimensional array of records is exactly what that API is worst at, and the copy controls would each be a `setValue` storm); validating only in the action (rejected -- the reader would learn about a bad cell from a sentence naming a hardware group, with nothing marked); letting core-api be the only judge (rejected for the same reason -- it reports the first cell it dislikes, and a grid needs to be told which cells).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEC-105 | **`%%key%%` file links are resolved before the markdown is parsed, and the editor is bound to the unresolved text (T-023)**                                                                                                            | An exercise author writes `![figure](%%fig1%%)` and defines `fig1` as a link to one of the exercise's own files. The substitution has to happen on the **string**, before parsing: a placeholder can stand inside a markdown link target, where no post-parse transform could reach it, which is why the legacy app does it the same way. That leaves one trap, and it is the whole reason this is a decision -- the settings _form_ must not be bound to the resolved copy, or saving would write the substituted URLs back over the author's own placeholders and the exercise would stop being editable in the way it was written. So `getExerciseDetail` returns both: `texts` for display and `rawTexts` for the form. A placeholder with no matching link is **left visible** rather than deleted, because the only person who can fix a broken reference is the one looking at it. The URL substituted is a **public core-api address** carrying no token; the link's own `requiredRole` decides who may fetch it, and `null` genuinely means anybody -- which is the point, for an exercise text that becomes a printed handout.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Resolving at render time inside the markdown component (rejected -- it cannot reach a link target, and it would have to be threaded through every renderer); storing resolved text (rejected -- it would bake this deployment's API address into the database, and core-api may not be written to anyway); rendering an unresolved placeholder as an error (rejected -- it is the author's own draft state, not a failure).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| DEC-106 | **An empty reference-solution list means "none you may read", not "none exist" (T-011)**                                                                                                                                               | core-api filters `/reference-solutions/exercise/{id}` **one solution at a time** (`canViewDetail` per row, read in the presenter and confirmed live): the seeded private answers are the superadmin's and a supervisor sees an empty array for an exercise that is demonstrably assignable. So the obvious empty state -- "this exercise has no reference solution, so it cannot be assigned" -- would be a plain falsehood for the commonest reader. The exercise's own `hasReferenceSolutions` is **not** filtered, and is what separates the two: nobody has written one, or none of them is yours. The screen says which. The same distinction is why deleting confirms with the unassignable warning based on what this reader can see and lets core-api be the real judge -- claiming certainty about rows the reader cannot see would be inventing them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Showing one message for both (rejected -- it is wrong for anybody who is not the author); hiding the screen from a reader with no visible solutions (rejected -- submitting one's own is exactly what they came to do); asking for an unfiltered count (rejected -- no endpoint offers one, and `hasReferenceSolutions` already answers it).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DEC-107 | **The pipeline graph is laid out and drawn on the server, by this app, with no Graphviz at all (T-014/T-016)**                                                                                                                         | The brief names this as a decision to make early and gives three options: port `viz.js` behind `next/dynamic`, render server-side, or replace it with a JS graph layout. This is the third, and the reason is not preference. **The modern Graphviz WASM build crashes on real ReCodEx pipelines** -- `@viz-js/viz@3.30.0` (Graphviz 16.0.0) throws `RuntimeError: table index is out of bounds` on the seeded Python pipeline, and the crash was reduced to plain synthetic input: ten record nodes with an empty leading cell, the exact label shape the legacy renderer emits. Five is fine, ten is not; it is a size threshold in the engine, not a syntax error in the document, so any pipeline of realistic size is a coin toss. Building a product screen on that was not defensible, and pinning the 2019 build the legacy app ships would have been worse. So the layout is a pure function (`lib/pipelines/layout.ts`: as-late-as-possible layering, barycentre ordering with a fractional **port** term so producers fan out in the order their wires arrive) and the drawing is string building (`svg.ts`). Both run on the server, in the shape D-009 and D-010 already chose for highlighting and markdown: nothing in the client bundle, the picture in the HTML, and it works with JavaScript off. Two things came free that Graphviz could not give: the colours are **design-system tokens**, so the diagram is themed like the rest of the app instead of being a pale-green PNG-alike on a dark page, and every node carries `data-name`, so selection needs no parsing of generated markup for `<title>` elements the way the legacy click handler does. What was given up is Graphviz's edge routing; with a dozen boxes in five or six layers that is a smaller loss than it sounds, and the layering is unit-tested where a picture could only be looked at.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Porting `viz.js` to the client behind `next/dynamic` as the legacy app does (rejected -- a multi-megabyte WASM payload on a screen most readers never open, and it is the build whose successor demonstrably crashes); running Graphviz server-side (tried first, and the crash above is why it was abandoned); pinning `viz.js@2.1.2` server-side (rejected -- shipping a 2019 engine to dodge a bug in its successor, with no path forward); a graph library like dagre or elkjs (rejected -- both are layout only, so the drawing would still be this code, and the layering a pipeline needs is a hundred lines).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-108 | **Switching between the two configuration kinds is offered in both directions, and only one of them confirms (T-024)**                                                                                                                 | DEC-101 made T-009 refuse to rewrite an advanced configuration, which was right and left an exercise in that state with **no way out of this app** -- the one-way door S-026 and T-001 were each filed for, this time built deliberately. Both directions exist now and they are genuinely asymmetric, so they are presented that way. Going **to** the advanced kind sets `configurationType` and nothing else: the configuration in place is kept and the other editor reads it, so nothing is lost and nothing is asked. Coming **back** cannot be a flag -- the simple form can only express the instance's own pipelines and a fixed vocabulary, so the configuration is rebuilt: environments narrowed to one, pipelines replaced by what `relevantPipelines()` picks, and every variable outside the vocabulary dropped. That is a real loss, so it confirms -- and the dialog **names what will actually go**, read from this exercise (`previewSwitchToSimple`), rather than warning in the abstract. "Something may be lost" is not a warning anybody can act on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | One button that toggles (rejected -- it hides that one direction is destructive and the other is not); refusing the way back entirely, as T-009 shipped (rejected -- that is the one-way door this ticket exists to close); rebuilding without confirming (rejected); confirming with generic prose (rejected -- the two lists are cheap and are the whole difference between a warning and a decision).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DEC-109 | **A custom score is edited as an expression in text, parsed to and from core-api's tree (T-025)**                                                                                                                                      | core-api's `universal` calculator stores an expression tree -- eleven node types, `{type: "avg", children: [...]}` and the rest -- and the legacy app edits that tree _with_ a tree: `scoreAst.js` plus `ScoreConfigUniversalExpression` is about three thousand lines of node forms, placeholder nodes and drag targets. This ships a grammar instead: infix `+ - * /`, brackets, `avg`/`min`/`max`/`sum`/`mul`/`clamp`, numbers, and a test result written as its **name in quotes** (test names contain spaces, dots and brackets, and nothing else in the grammar is quoted, so the quoting is unambiguous rather than decorative). Parser and printer are two pure functions with a round-trip test, against a tree editor that could only be checked by clicking through it -- and an expression can be read at a glance and pasted between exercises, which a tree cannot. What is sent to core-api is always the tree; the text is this app's notation and core-api has never heard of it. **Two things are checked before the save is offered, and they are different failures**: the syntax, by the parser, with the character it gave up at; and every test name, against the exercise's own tests -- a misspelt name is not a syntax error, it is an exercise graded on something that does not exist, and core-api's refusal names the calculator rather than the word.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Porting the tree editor (rejected -- three thousand lines to express a grammar of eleven productions, and the bulk of it is drag-and-drop that a text field does not need); a form per node type without the tree (rejected -- the same thing with worse ergonomics); leaving it read-only as T-009 shipped (rejected -- that was DEC-103's temporary answer and it left a door that only closed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEC-110 | **The user list offers its account actions on the reader's role, because a user carries no permission hints at all (AD-001)**                                                                                                          | Every other screen in this app gates a control on core-api's own `permissionHints` for the entity in front of it (constraint 4). **A user has none** -- DEC-080 found the field `null` on `/v1/users/{id}` and this ticket confirmed the list payload is the same, and `permissionHints` turns out to be emitted for exactly one entity in the whole API (`GroupFormat`). So the question is not which hint to read but what to do when there is no hint to read. Three facts decide it. First, the split is a real one and it is core-api's: `user.viewAll` (the list itself) is granted from the `supervisor` role upwards, while `setIsAllowed`, `delete` and `create` fall under `permissions.neon`'s blanket `role: superadmin` allow -- so the screen and its actions have different audiences, and a `supervisor-student` is refused even the screen (verified live, 403, and the reason the sidebar's own role check is not the rule here). Second, the enforcement is not this app's to lose: a Server Action calls core-api on **the caller's own session token**, so a reader who forges the call is refused by the same check that would have refused the button -- "a hidden button is not authorisation" is satisfied by the layer below, not by a second copy of the rule here. Third, the legacy app does exactly this (`isSuperadminRole(effectiveRole)` around the same four controls), so matching it is parity rather than invention. What this app does **not** do is infer anything finer from the role -- the two per-account rules core-api adds on top (`setIsAllowed` and `setRole` both refuse the reader's own account) are restated where they are visible, which is why the reader's own row has no Disable button.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Rendering the actions for everybody and letting core-api refuse (rejected -- a screen of buttons that 403 is not a screen, and brief §3.4 forbids offering an action the reader may not perform); probing the API on render to discover the permission (rejected -- there is no read-only probe for `setIsAllowed`, and a probe that mutates to find out is absurd); dropping the actions until core-api publishes hints for users (rejected -- that is the whole of what this ticket is for, and it would be a parity gap filed against an API change this repo may not make).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DEC-111 | **Editing one's own account is a different screen, and the administrator's one redirects there rather than hiding its controls (AD-002)**                                                                                              | The legacy app has **one** `EditUser` page for both audiences and branches inside it -- `data.id === loggedUserId` gates the settings and calendar sections, `isSuperAdmin && data.id !== loggedUserId` gates the role form and the forced password change. That is four independent conditions on one screen, and the reason there are four is that core-api draws the line in four places. This repo already had the other half built (S-022's `/profile/edit`), so AD-002 keeps them apart: `/users/:id/edit` **redirects to `/profile/edit`** when the id is the reader's own. Three live checks say that is not merely tidy. `checkSetRole` and `checkSetAllowed` each carry an explicit second check refusing the _current user_ before the ACL runs. And a **forced** password change on oneself is refused by `- allow: false ... forceChangePassword ... user.isSameUser`, a rule sitting above the superadmin's blanket allow in `permissions.neon` -- confirmed by asking (`400-103 "Your current password does not match"` when a superadmin sends no old password for their own account), not by reading the file. So of the four things this screen adds, three would fail on one's own account and the fourth (changing a password one knows) is already on the other screen, complete with the sign-out it needs. **The two screens also differ in what core-api will even disclose**: `settings` and `uiData` are attached to `privateData` only for the account's owner (verified live -- absent when a superadmin reads a student), so notification preferences and the iCal tokens are not omitted here by choice, they are unreadable. What _is_ shared is shared verbatim: the name-and-email form is S-022's `ProfileForm`, unchanged, because `updateProfile` is the same call whoever makes it -- two of its strings were reworded from first person so they are true on both screens.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | One screen with four branches, as legacy has (rejected -- the branches are load-bearing and invisible, and the self case needs a different password form, not a hidden one); showing the controls on one's own account and letting core-api refuse (rejected -- three guaranteed failures, and brief §3.4); a redirect for the whole `/users/:id` profile too rather than only its edit screen (rejected -- reading one's own profile at its public address is fine and S-021 already renders it, it is only _editing_ that splits).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| DEC-112 | **Taking over an account is a sign-in with no way back, and the confirmation says so rather than the app pretending otherwise (AD-003)**                                                                                               | core-api's `actionTakeOver` calls the same `sendAccessTokenResponse` that login does: an ordinary master+refresh token for the target user, **carrying nothing that says whose doing it was** (read directly, not inferred). So there is no impersonation state for this app to detect, no banner it could honestly render, and no token it could exchange back -- the legacy app has none of those either, and its own control simply navigates to the dashboard as the other person. This ticket keeps that and spends its words on the confirmation instead: "this is a sign-in, not a preview... the way back is to sign out and sign in again as yourself", which is the whole of what a reader needs to know before clicking. **The one thing done differently from the legacy app is the navigation: a full page load, not `router.push`.** The session cookie now identifies a different person while Next's client Router Cache still holds RSC payloads rendered for the administrator; every server read in this app is `no-store` precisely so one user's data cannot reach another (DEC-021), and discarding the client is the one-line way to keep that true on the client too. That is also the one place in this repo where `@next/next/no-location-assign-relative-destination` is deliberately suppressed. **Where the control lives is a rearrangement**: the legacy app offers it on the user list _and_ on the edit screen; here it is on the profile only (S-021), which every name in the app already links to and where the reader can see who they are about to become.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A second cookie holding the administrator's own token, so "return to my account" could exist (rejected **for this ticket**, not on principle: it is a real improvement and the security argument is weak against it -- the browser held that token a moment earlier -- but it adds a second privileged credential and a route to swap it back, which is an auth-model change rather than "add the button", and belongs in a ticket that says so); a banner announcing the impersonation (rejected -- there is nothing in the token to render it from, so it could only be a guess); repeating the control on the user list as legacy does (rejected -- the name in each row already links to the profile, and a one-click sign-in-as-somebody-else in a list of thirty rows is a misclick waiting to happen).                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DEC-113 | **The three instance screens become two, because core-api's "edit instance" is one boolean (AD-004, AD-005, AD-008)**                                                                                                                  | The legacy app has `Instances` (a list), `Instance` (description, admin, licences, groups tree) and `EditInstance` -- and that third page contains a **single checkbox**, because `POST /v1/instances/{id}`'s entire request body is `{isOpen}`. `BACKLOG.md` promised AD-005 "settings, limits"; there are no limits and there is one setting. **The reason is that an instance is mostly its root group wearing a hat**: the name and description shown everywhere are the group's, set once at creation and edited afterwards through the group. So this ships `/admin/instances` (the list, plus creation) and `/admin/instances/:id` (everything about one, including its licences), and the latter _links to_ the root group rather than offering fields it could not save. The legacy groups tree is not reproduced there either -- `/groups` is that screen, built in S-004. **The list page is gated on the Admin section's own audience, and that gate is explicitly not an authorisation claim**: core-api grants `instance.viewAll` to the `unauthenticated` role, which is how the registration form offers a choice of instance before anyone signs in, so nothing here is secret. It is that `/admin/*` is a section `IA.md` §3.1 gives to superadmins and empowered supervisors, and a route inside it that a student can open is a navigation bug -- found by opening it as one. The detail page is narrower still (superadmin), because `viewLicences` is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Keeping the legacy split so `EditInstance` exists as its own route (rejected -- a route whose entire content is one checkbox, when the screen before it already has a settings section); offering name and description fields on the detail screen (rejected -- the update endpoint would drop them silently, which is the worst of the three options); reproducing the groups tree on the instance page (rejected -- `/groups` is that screen and this one links to the root group); leaving the list ungated because core-api does (rejected -- see above; the data is public, the _route_ is not).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-114 | **"Server management" is the broker and the background jobs, not runtime environments and hardware groups (AD-006)**                                                                                                                   | `BACKLOG.md`'s note for this ticket was written from a route map during recon and names two things the legacy `ServerManagement` page does not contain. That page has exactly two panels: the **ZeroMQ broker** (its statistics, and freeze/unfreeze) and **core-api's background job queue** (list, ping, abort). Runtime environments and hardware groups have **no administration screen anywhere in the legacy app** -- grepped, not assumed: they appear in `EditExerciseLimits` and in the exercise configuration editor, both read-only vocabularies, and both already built here (T-009, T-010). There is nothing to port and nothing to file as a gap. So this ticket ships what the page is. Two details are the screen's own: **only one of freeze and unfreeze is ever offered**, because `is-frozen` is in the statistics and rendering both would be offering an action that cannot apply; and the **statistic names are core-api's, untranslated**, because it answers a flat map with no schema and a table that renamed them would go stale the first time the broker gains a counter. The freeze confirmation says what freezing costs -- evaluation stops deployment-wide and nobody is told why -- rather than asking whether the reader is sure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Building runtime-environment and hardware-group CRUD anyway (rejected -- core-api exposes reads for both and no writes worth a screen, and the legacy app manages neither, so this would be inventing a feature and calling it parity); filing the missing environments screen as a parity gap (rejected for the same reason -- there is no gap); translating the broker statistic names (rejected -- an unschema'd map whose keys this app does not control).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEC-115 | **Broadcasts sit above the page rather than behind a bell, and the screen that writes them is narrower than the permission to write one (AD-007)**                                                                                     | Three things had to be decided and none of them was in the ticket. **First, where the endpoints are**: the legacy module is `systemMessages` and the paths are `/v1/notifications` -- nothing in `openapi/core-api.yaml` matches "system message", and they turned up in `redux/modules/systemMessages.js`. **Second, where a message goes.** The legacy app hides active messages behind a bell in its header with an unread badge; this app's shell has no header bar to hang one on, and a broadcast is the instance saying evaluation is down or a deadline moved -- worth reading without opening a dropdown. So they render as callouts across the top of `<main>`, on every page, and "mark as read" is one control for all of them. It has to be one control because that is all core-api stores: the legacy app keeps a single `systemMessagesAccepted` timestamp in the reader's `uiData` and treats everything older as seen, so there is no per-message flag to build on and inventing one would mean inventing storage. **Third, and least comfortable: `notification.create` is granted from the `supervisor` role up, while `viewAll` -- the management list -- is the superadmin's alone** (both verified live). A supervisor may write a broadcast and has nowhere to see, edit or withdraw it afterwards. This app does not paper over that by building a supervisor's half-screen out of `/v1/notifications`, which answers _active_ messages rather than _theirs_; the screen stays the superadmin's and the gap is recorded here. Two smaller notes: the message text is **not** markdown-rendered in the shell (the legacy dropdown does render it), because arbitrary authored markdown in the frame of every screen is a bigger surface than the feature needs; and `role` is a **floor** -- core-api's "this role and its children" -- so the editor labels it "lowest role that sees it", since "role: student" reads like the opposite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | A bell in the sidebar (rejected -- a notice nobody opens is a notice nobody read, and the sidebar is navigation); per-message dismissal (rejected -- core-api stores one timestamp and nothing else, so this would be a second store to keep in sync); building a supervisor-facing message screen out of `/v1/notifications` (rejected -- that endpoint answers what is _active for the reader_, not what they wrote, so the screen could not offer editing or withdrawal and would be a worse lie than its absence); markdown in the shell banner (rejected -- see above).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DEC-116 | **The landing page is purely public, and `/` and `/dashboard` are two different pages rather than one that branches (A-001)**                                                                                                          | The legacy `Home` is a single screen for both audiences: it branches on the reader's effective role, hides the exercises section from students, and shows the instance block only to those signed in. This app's IA already splits that -- `/` is the front door and `/dashboard` is the signed-in home -- so the landing page reads the same for everybody and **reads no session at all**. That is not laziness: `readSessionToken()` carries an explicit note that it is _not_ a general-purpose "is the reader signed in" helper, and a marketing page is the wrong place to introduce one. The split has a neat consequence: the sign-in link needs no branch, because `proxy.ts` already sends a visitor who has a session from `/login` to `/dashboard`. The quick-start prose is rewritten rather than transcribed -- the capability the legacy page provides is "explain the system and this instance to somebody who has just arrived", and five tight sections deliver it where the original's fifteen long paragraphs were written to be skimmed past. Moved into the `(anon)` route group on the way, which is a bug fix as much as a tidy: at `app/[locale]/page.tsx` it had **no `<main>` landmark and no language switch**, the same gap S-024 found on the other anonymous pages.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Branching on the session so a signed-in visitor gets "go to dashboard" (rejected -- needs a session helper this app deliberately does not have, to save one click that `proxy.ts` already saves); transcribing the legacy prose (rejected -- it is long enough that nobody reads it, and this is the one page whose whole job is being read); leaving `/` outside `(anon)` (rejected -- it was missing the landmark every other anonymous page has).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| DEC-117 | **There is no CAS finalisation page, because this app's external login is a redirect and not a popup (A-007)**                                                                                                                         | The legacy `LoginExternFinalization` screen exists **only** to serve a popup: the sign-in page opens the identity provider in a second window, that window comes back to `/login-extern/:service?token=...`, and the page's entire job is to `postMessage` the token to `window.opener`, wait to be told it arrived, and close itself. F-019 built this app's side as a plain redirect target (`/api/auth/external/{name}/callback`), so there is no second window, no `postMessage` handshake, no opener to talk to -- and nothing for that page to do. **Porting it would mean porting the popup**, which is the mechanism, not the capability. What was genuinely missing is the opposite end: the callback and its failure state were built by F-019 and **nothing ever sent anybody to the provider**, so the capability was unreachable. This ticket is that entry point -- the same three configuration values the legacy app reads (`EXTERNAL_AUTH_URL`, `EXTERNAL_AUTH_SERVICE_ID`, `EXTERNAL_AUTH_NAME`) and the same condition for offering it (both the URL and the service id set). The URL is used **exactly as configured, with nothing appended**, which is also what the legacy does: where the provider returns to is the provider's configuration, not this app's guess. Unconfigured on this deployment (Q-004), so the button is absent here -- but the configured render was verified by hand, by setting the three variables, rebuilding, and confirming the button carries exactly the configured URL.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Porting the popup page (rejected -- see above: the page is an artefact of a mechanism this app does not use); constructing the provider URL from parts (rejected -- the legacy hardcodes one university's CAS in `helpers/cas.js`, which is that institution's bridge rather than the general mechanism, and building a CAS URL from a template would be inventing a protocol core-api never speaks); leaving A-007 as "blocked, no provider configured" (rejected -- the missing piece was the button, and a button can be built and reviewed without a provider to click it through to).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| DEC-118 | **A `loading.tsx` skeleton announces nothing, deliberately, because making it speak costs two routes their static rendering (P-002)**                                                                                                  | P-002 found `TableSkeleton` rendering `role="status" aria-busy="true"` over descendants that are all `aria-hidden` -- a live region with nothing to read, which announces silence rather than "loading". Two ways to fix that: give it text, or drop the role. Giving it text is the obvious one and it was built first, as a small `"use client"` leaf calling `useTranslations`, because a `loading.tsx` is a `Suspense` fallback and so must be synchronous, which rules out `getTranslations`. **Then `next build` said what it cost**: `/faq` and `/forgot-password` -- the only two prerendered routes in the whole app -- went from `●` to `ƒ`. Confirmed by bisection, not guessed: reverting that one file restored both. next-intl's client translator needs the request locale, and reading it inside a fallback opts the segment out of static rendering; `setRequestLocale` in the page's `generateMetadata` does not lift it. So the role is now applied **only when a caller passes a label**, and the two `loading.tsx` files pass none. The announcement is kept where it is both free and more useful: the dashboard's streamed `Suspense` boundaries, whose fallbacks are rendered by an async Server Component that already has a translator. A page-level skeleton is on screen for the length of one server render of a page that makes no API call; a dashboard panel's is on screen for the length of a real fan-out.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Keeping the client leaf and losing the two static routes (rejected -- it trades a measurable, permanent cost on the two cheapest pages in the app for an announcement on the two pages least likely to need one); leaving `role="status"` unconditional and unlabelled (rejected -- that is the defect P-002 filed, not a fix); making `loading.tsx` async to reach `getTranslations` (rejected -- a fallback that suspends is not a fallback); passing the string down from the layout (rejected -- `loading.tsx` takes no props, there is nowhere to pass it from)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| DEC-119 | **The shell's four core-api reads run in one wave, and the shell still blocks the page -- the second half is PF-002, not this ticket (P-003)**                                                                                         | `components/app-shell/app-shell.tsx` awaited `getCurrentUser()` and only then `getMyGroups(locale)` and `getActiveSystemMessages()`, putting one core-api round trip in front of every authenticated render on **44 of the 46 routes**. Nothing made that ordering necessary: `getMyGroups` takes only the locale and calls `requireSession()` itself, `getActiveSystemMessages()` takes nothing, `user` is first read afterwards for the unread filter, and every request re-authorises independently through `lib/api/client.ts`. `getLocale()` and `getTranslations` are not core-api calls, so they join the same wave. Serial depth 2 -> 1. **What this does not fix, deliberately:** `AppShell` is still an async Server Component wrapping `{children}`, so no page starts its own fetching until the shell finishes -- TTFB is shell + page rather than `max()`, and `(app)/loading.tsx` is unreachable. Fixing that means the sidebar and the banner in their own `<Suspense>` boundaries with `{children}` as their sibling, which changes what the first byte contains and contradicts this file's own docblock promise that the sidebar is in the initial HTML. That is a visible product change and it gets its own ticket rather than riding along in a performance pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Wrapping the sidebar and banner in `<Suspense>` here (rejected -- see above, it changes what the reader sees first and deserves its own ticket, filed as PF-002); leaving the two stages alone until a profiler confirms the cost (rejected -- the cost is a round trip that is visible in the code, and DEF-002's "measure first" is about React Compiler's speculative benefit, not about whether a serialised fetch is serialised)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-120 | **The FAQ document is the one fetch in this app that is cached, and the page around it is deliberately not prerendered (G-024)**                                                                                                       | Every other read in this app goes through `lib/api/client.ts`'s unconditional `cache: "no-store"` for the reason DEC-021 gives -- core-api's answers are per-user and a cross-user cache leak is a security incident. **This one is not core-api's and is not per-user**: it is one public markdown document, identical for a student, a teacher and a visitor with no account, so `next: { revalidate: 3600 }` is a correctness-neutral way to stop a request to a third party's server on every page view. Verified live, not assumed: four page loads against a local fixture server produced **one** upstream GET. The second half is the one that had to be found rather than reasoned about -- **`connection()` before the fetch**, because without it `next build` reports `/en/faq` and `/cs/faq` as `●` (prerendered), and a prerendered page reads `process.env.FAQ_URI` on the **build** host. This deployment builds an image once (F-003) and supplies its environment through compose at run time, so the page would have baked in a variable that is not set anywhere at build. Confirmed by building both ways and reading the route table: `●` without it, `ƒ` with it. The Data Cache still applies to the fetch afterwards, which is the whole point of using `connection()` rather than `dynamic = "force-dynamic"` -- the latter rewrites every fetch in the segment to `no-store` (Next's own `caching-without-cache-components.md`, read in `node_modules`, not recalled)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-121 | **Authored markdown inside a client island is rendered on the server and handed down as a slot, never rendered in the browser (G-027)**                                                                                                | `Markdown` is `async` -- it awaits D-009's Shiki highlighter -- and only a Server Component may be. Review comments are the first place the app needed markdown _inside_ a client component: the comment thread is an island because it has to appear **between two lines of code**, which server-rendered HTML cannot be given to a child of. The two ways out were to render markdown in the browser or to render it on the server and pass the result down. The first loses D-010's whole shape -- react-markdown, remark, KaTeX and Shiki in the bundle, on the screen a teacher reads most, while PF-001 is open about bundle size -- so the page renders one `<Markdown>` per comment and threads them through `ReviewSummary`, `SourceFile` and `ReviewableCode` as `Record<string, ReactNode>`. **What makes it correct rather than merely working is the refresh discipline that was already there**: every write in `review-comment.tsx` calls `router.refresh()`, so the server re-renders and the new body arrives with it -- verified live by editing a comment and watching `## heading` become an `<h2>` without a reload. The prop is optional and falls back to the plain text, so a comment that somehow arrives without one reads as it did before this. Applies to any future markdown in an island (G-028's preview is the next one)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DEC-122 | **The message catalogue is trimmed per route, from a map derived from the import graph, at the cost of the one prerendered route (PF-001)**                                                                                            | `<NextIntlClientProvider>` with no `messages` defaults to the entire catalogue -- 126,514 bytes on every page, of which the median route's client subtree can reach 2,968. Three designs were measured rather than argued: pruning globally to every namespace any client component uses leaves **71%** of it, splitting by route group leaves `(app)` at **65%** (and `(app)` is most of the product), and per route leaves **2%** at the median and 12% at the worst (`/groups/[groupId]`). Only the third is worth the machinery. **The map is generated, not written**, because the failure mode is a runtime `MISSING_MESSAGE` on one screen that neither `typecheck` nor `build` can see: `scripts/route-messages.ts` walks each page's imports, carries an "inside a client module" flag across the boundary (a client component's own imports are client too), and unions in every layout above the page plus the error/not-found pages, which can render anywhere. A test re-derives it and fails when the committed file is stale. **A layout is not told which route it wraps**, so `proxy.ts` sets the locale-stripped path on the _request_ headers before next-intl runs -- its pass-through copies `request.headers` into what it forwards, so this rides the mechanism already there rather than reaching into Next's `x-middleware-override-headers`. Reading a header makes the layout dynamic, which costs `/forgot-password` its prerender: it was the only route in the app still being prerendered (73 of 75 were already `ƒ`), and a form that posts to core-api on submit is a thin thing to keep static. An unmatched path falls back to the whole catalogue, so a route added without regenerating is heavy rather than broken                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEC-123 | **An assignment's own text is a second form on the settings screen, and the warning that a re-sync overwrites it lives on that form because nothing else ever says so (G-007)**                                                        | core-api keeps the two saves apart and its own comment gives the reason -- an assignment's texts arrive as a copy of the exercise's, so overriding them "needs to be handled carefully". `actionUpdateDetail` does not touch them at all; what it carries is the per-locale _hints_, which belong to the assignment and survive a re-sync. So this is two endpoints and two forms rather than one longer form, and the cost of that is a shared `version`: **both saves increment it**, which means the texts form has to refresh the route on success rather than navigate away, or the settings form still on screen saves against a lock core-api has already moved. The second half is the one a reader cannot discover. `Assignment::areLocalizedTextsInSync` calls a locale stale only when the _exercise's_ copy is newer than the assignment's, so **an override never registers as drift** -- S-013's sync notice stays quiet, and the next re-sync, pressed for some unrelated reason, silently replaces the text. That is why the warning is a permanent part of this form rather than a toast on save. (The legacy app's callout in the same place says something stronger and no longer true: that an ordinary settings save overwrites these from the exercise.) A blank name deletes a language, as in T-008, because `Localizations::updateCollection` replaces the whole collection with what it is sent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DEC-124 | **The mail-the-class link reports a recipient list long enough to be truncated, rather than trusting the `mailto:` (G-011)**                                                                                                           | The legacy app builds `mailto:?bcc=` over the whole roster and hands it over, and on a small group that is right. There is no length limit in the `mailto:` specification -- but there is one in nearly every handler that opens the link, and **they truncate rather than refuse**: Windows' shell handler and several desktop clients stop reading at roughly two thousand characters, which a course of a hundred students passes. The failure is therefore silent and in the worst direction -- a teacher gets a composer holding some of their class, presses send, and nothing anywhere says who was left off. So `groupMailto` reports the ceiling as a flag, the screen says so in words, and the addresses are offered as copyable text beside the link so there is a way through that does not depend on the client's limit. The number is a conservative real-world figure rather than a specified one, and is named in one place for that reason. The same function also counts the addresses core-api **did not disclose** (`privateData` is omitted per person), because mailing twenty-eight of the thirty students the table above shows is the other way this link can quietly be less than it looks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DEC-125 | **`getCurrentUser().role` is the session's effective role, not the account's, and narrowing it is a preview rather than a privilege drop (G-023)**                                                                                     | Two decisions that only make sense together. **First:** core-api authorises against the token's `effrole` claim, so a reader who has narrowed their session is, to core-api, that lesser role -- and every gate in this app that asked the _account's_ role would then offer screens core-api refuses. A narrowed superadmin would see the Admin section and be refused at each click, which is the worst of both. So the field every gate already reads now means "what this session may do", the account's own role moves to `accountRole`, and the ten gate sites needed no edit -- the one place that resolves it changed meaning instead. `accountRole` has exactly two readers, the banner and the switcher, both of which exist to show the difference. **Second:** this is offered as "view as" and never as a way to work more safely, because it cannot be. `validateEffectiveRole` compares the requested role against the account's role in the database rather than the calling token's, so a narrowed session can ask for its full role back and be granted it -- verified live, a token narrowed to `student` re-issued itself as `superadmin`. It is a preview of somebody else's app, it contains nothing, and both the section and the banner say so in words; describing it as reduced privileges would be a security claim this mechanism does not support. Offered to anyone with a role below their own (the legacy panel's rule and core-api's), not to superadmins alone as G-023's row supposed -- seeing what a student sees is most useful to the supervisor teaching them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DEC-126 | **`API_BASE_INTERNAL` points at `127.0.0.1`, not `recodex.local`, for bare-host dev -- the `.local` suffix costs 5 seconds per connection**                                                                                            | Found while verifying G-019, and it is the real cause of what **PF-002** recorded as an IPv6 penalty and parked its measurement over. `.local` is mDNS/Bonjour's reserved suffix on macOS, so every name lookup for `recodex.local` is handed to `mDNSResponder` and waits out a **5-second** multicast timeout before the resolver falls back to the `/etc/hosts` entry that was there all along. Isolated rather than inferred: `net.connect` to `127.0.0.1` answers in 0-2 ms and to `recodex.local` in 5005 ms, **repeatably and with no caching benefit**, and the same 5 s appears identically through `net`, `http.get` and `fetch` -- so it is neither undici nor Happy Eyeballs nor the address family. `/etc/hosts` holds only an IPv4 line for the name and `getaddrinfo` returns only `AF_INET`, which is what rules IPv6 out: there is no IPv6 address to try. `curl` escapes it because it resolves differently; Node does not. The consequence is not cosmetic -- a page making several core-api calls exceeded this app's own 10 s fetch timeout and rendered the error boundary, which is how it was noticed (`/en/exercises` 500ing while `curl -4` answered the same endpoint in 38 ms). Setting the **internal** URL to the IP fixes it outright (that page then rendered in well under a second) and costs nothing, because `API_BASE_INTERNAL` is by definition the server's own route to core-api; `API_BASE_PUBLIC` must stay `recodex.local`, since it is what the browser resolves and browsers do not pay this. Only bare-host dev is affected: the containerized service gets `http://api:80/v1` from the compose file (DEC-028), which is not a `.local` name                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DEC-127 | **The page QR code hardcodes black-on-white and sets a 4-module quiet zone, against this project's own theming rule (G-025)**                                                                                                          | Brief §9 says never hardcode a colour, and this is the deliberate exception: a QR code is a machine-readable image, not decoration, and the two rules it must satisfy come from ISO/IEC 18004 rather than from the design system. **Dark-on-light**, because the standard defines the modules as the dark, low-reflectance element and inverted codes are decoder-dependent -- following the theme would produce a light-on-dark code in dark mode that some phones simply will not read, and "the QR code works unless you use dark mode" is a worse outcome than an inconsistent swatch. So it renders on its own white plate in both themes, which also keeps it legible against either page background. **`marginSize={4}`**, because `qrcode.react` defaults to `0` and the standard requires a four-module quiet zone; without it the code runs to the edge of its own SVG and scanners lose the finder pattern against whatever is behind it. `level="M"` over the library's default `"L"` for the same practical reason: these get photographed off a projector at an angle. All three are cheap insurance on a feature whose entire value is that it scans on the first try in front of a room                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

| DEC-128 | **A markdown preview is a Server Action that returns a React node, not an endpoint that returns HTML (G-028)** | `Markdown` is an async Server Component that deliberately never touches `dangerouslySetInnerHTML`: it builds React elements, and raw HTML in the source is escaped to text before it reaches the renderer. Rendering it to a string and injecting that into the form would throw away the one property that makes it safe to point at authored content, and writing a second, client-side renderer would reintroduce exactly the divergence this ticket exists to prevent -- the KaTeX delimiter that renders one way in the preview and another way for the student. So the action calls the component, awaits it, and returns the node; React serialises it across the RSC boundary. **Probed against the real component before the ticket was built**, because the bundled Next docs defer to React's on what a Server Function may return and state nothing about elements: a throwaway page confirmed KaTeX, Shiki and the heading all arriving in a client component, and the shipped spec asserts that `<script>` still comes back as text. The component is **called and awaited** rather than returned as JSX so that a parser failure on arbitrary input becomes a form error instead of a rejected action. The cost is a round trip per preview, which is why the preview renders **when the tab is opened** and not as the author types |
| DEC-129 | **The data-processing consent is asked on both screens that create an account, not only on the registration form the legacy app puts it on (G-026)** | Legacy asks for the GDPR tick in one place, `RegistrationForm`. But this app has a second screen that creates an account and it is the one that is actually reachable here: `RegistrationPresenter::actionAcceptInvitation` looks the invited address up and, finding no login, **builds the `User` entity on the spot** -- and on this deployment local registration is off (A-003), so the invitation is the only way an account is ever made through this frontend. The tick exists for a legal reason rather than a functional one, so putting it where accounts are created rather than where legacy happened to put it is the whole point of restoring it. Two consequences recorded rather than discovered later. **It is stored nowhere**: core-api has no field for it (no `gdpr` anywhere in its source) and legacy sends it only as a stray key on the registration body, so this is a gate in front of a request, not a record of consent -- if the deployment ever needs to _prove_ consent, that needs core-api, not this app. And **it is asked of a returning reader too**: the same endpoint signs an existing login in and enrols them without creating anything, and the page cannot tell the two cases apart before submitting, which makes one redundant tick the cheaper of the two mistakes. One shared component (`ConsentCheckbox`), because a sentence with legal weight should not exist twice. |
| DEC-130 | **A file pairing a reader made by hand lives in the URL, not in `localStorage` where the legacy app keeps it (G-030)** | Two things decided this and only one of them is taste. The pairing decides **which files the server fetches, reads and tokenises** -- code is highlighted on the server here (DEC-012, `lib/code/highlight.ts`), so a mapping the browser alone knows about cannot reach the thing that acts on it without moving the whole viewer client-side. And a comparison somebody set up by hand is exactly the sort of thing worth sending to a colleague, which is the same reason G-005 put both solution ids in the path rather than in a picker. So the shape is `?pair=left:right`, each side percent-encoded so a filename may contain a colon,, repeatable, and the control that writes it is a plain `GET` form whose select carries whole `left:right` values -- no JavaScript, one param name, and T-020's trade for the exercise catalog restated. What is given up against legacy is that the mapping does not follow the reader to their next visit; what is gained is that it follows the **link**. A pair naming a file neither side has is ignored rather than refused, because a stale link is an ordinary thing to be sent and the pairing the names give is the honest fallback. |
| DEC-131 | **The app shell fetches nothing; the sidebar and the notices stream in beside the page rather than in front of it (PF-002)** | `AppShell` was one `async` function awaiting four core-api calls, and `{children}` is part of the JSX it returns -- so every page under `(app)` waited for the frame before starting its own reads. It is now synchronous, with the two fetching subtrees as siblings of `{children}` in their own `<Suspense>` boundaries. **Measured on this host, median of 7: time to first byte 43-53 ms -> 5-8 ms across six routes**, an 85-90% cut, with no route completing later than before. Two things follow and both are deliberate. **`(app)/loading.tsx` finally renders** -- the boundary the layout puts around `{children}` was itself inside an unresolved async component, so the group's `PageSkeleton` had never once been shown. And **the document streams**, which is visible in three places: the sidebar arrives after the page, the Ctrl-K listener that lives in it is armed a beat later (the trigger button is visibly absent until then, which is what makes that acceptable), and a one-shot DOM query can now run before the page's own content lands. The skip link moved inside the sidebar boundary because its label needs the message catalogue, and that one `await` would put the whole frame back in front of the page. |
| DEC-132 | **The schemas are rewritten in `zod/mini`, so client-side validation stays and the validator shrinks (PF-004)** | Zod 4 classic is not tree-shakable through the `z` namespace -- the chunk carries 100 `Zod*` exports, `ZodJWT` and `ZodCIDRv6` among them, none of which any schema here uses -- so **283,397 bytes raw / 52,108 brotli, ~16% of all this app's client JavaScript**, sat in the initial chunk group of every route with a form. Three ways out were weighed, and the row's own two were both bad. **Moving validation server-side** removes Zod outright and costs the instant feedback eighteen forms are built on (a password mismatch, a second deadline before the first, "fill in the text or give a link") -- the brief picked "React Hook Form + Zod (shared schema)" for exactly that. **Deferring it** -- handing `useServerActionForm` a schema _loader_ rather than a schema, so the specifier stays static, the bundler still splits it and a `useEffect` warms the chunk -- was built and measured (`/profile/edit` 258,602 -> 205,501 brotli) and **is not what landed**: it moves the bytes out of the first paint without removing them, and it makes a schema module unable to export anything a client component needs as a _value_, since one such import anchors the whole runtime again. **`zod/mini` is the third way, and it was already inside the installed Zod 4.4.3**: same engine, tree-shakable function API, no new dependency beyond the types-only `@standard-schema/spec`. Measured against the real build: the 283,397 / 52,108 chunk is gone and `zod/mini` is **45,031 raw / 10,900 brotli** -- 41 kB brotli off twelve routes, total client JS **1,834,168 -> 1,593,673 raw (-13.1%)**. `useServerActionForm` now takes a `StandardSchemaV1` and uses `standardSchemaResolver`, so it names no validator at all. The cost is twenty modules rewritten into a functional API whose readability is load-bearing here -- these schemas carry core-api's own rules in prose beside them -- and the trap is `.min`/`.max`, which is `minLength` on a string or array and `minimum` on a number with only the base type to say which; `lib/actions/group-settings.schema.test.ts` was written before the conversion started, to pin the behaviour across it. |
| DEC-133 | **A flag core-api keeps unique per author is written on the classmate, never on the student the fixtures are built around (PF-013)** | `AssignmentSolutionsPresenter::actionSetFlag` treats `accepted` and `reviewRequest` as **unique flags**: setting one to `true` clears it on every other solution of the same author for the same assignment. So a spec that asks for a review on one of Alice's attempts silently withdraws the seed's own request from another of them -- and that one is a fixture, the row the teacher dashboard's queue and the plagiarism report are both reached through. It failed the way shared state always does: intermittently, in a different spec file, depending on which of two workers finished first. **Clearing the flag that was set is not the same as restoring what setting it displaced**, so a `finally` that sets its own flag back to `false` does not undo the damage; the state it overwrote has to be put back explicitly, and there is nothing in the response to say what it was. Hence the rule: a per-author unique flag is written on `CLASSMATE` (Bob), whose flags nothing reads, and `restoreSeededReviewRequest()` re-asserts the seed's own request afterwards regardless. The alternative -- serialising every spec that touches a flag -- would cost the whole suite's parallelism to protect one fixture. **And it is not only `setFlag` that writes this flag.** `AssignmentSolution::setReviewedAt()` clears `reviewRequest` whenever a review is **closed** -- in the entity, so it appears in no presenter and in no response -- and erasing or reopening the review does **not** put it back. So `solution-sources.spec.ts`, which closes a supervisor's review on the very solution the request fixture lives on, was consuming that fixture every run; it restores it explicitly now. That behaviour is right for the product (the teacher has answered the question) and it is only a problem for a test that treats the flag as something it owns. **The general form, worth carrying to any flag added later:** if core-api writes a flag from somewhere the caller did not name -- uniqueness enforced across sibling rows, or a side effect of a state change in an entity setter -- a test cannot restore state by remembering only what it asked for. |
| DEC-134 | **A spec locator is scoped to `main`, because since PF-002 the document holds two copies of a streaming boundary (PF-010)** | React's out-of-order streaming delivers a Suspense boundary's content into a staging `div` at the end of `<body>` and an inline script relocates it into place. While that is in flight the page genuinely contains **both** copies -- measured: one `input[placeholder="Filter groups"]` under `main#main-content`, an identical one under `div#S:2` -- so an unscoped `page.getByPlaceholder(...)` is a strict-mode violation that depends on timing. This is what PF-010's bucket (c) had been reporting all along, and **both earlier readings of it were wrong**: the row guessed the app renders the control twice for its responsive layouts, and a later pass closed it as "does not reproduce, and the code says it cannot" -- `data-table.tsx` does render exactly one filter input, which is why that reading was persuasive and why it was still wrong. It reproduced on the very next fresh instance. **So: reach for `page.getByRole("main").getBy*` rather than `page.getBy*` whenever the target is page content**, not because of duplicate markup but because the staging area is part of the document. `main` is also the honest scope for "what the reader sees", and most of the suite already uses it. |
| DEC-135 | **A Classroom `timeout` is read as minutes, which is an assumption and not a verified fact (X-001)** | `autograding.json` carries a per-test `timeout`, GitHub's own autograding documentation describes it in **minutes**, and ReCodEx's wall-time limit is in seconds -- so the importer multiplies by 60 and clamps to the 60 s ceiling a limits screen will accept. **Nothing here has been run against a real exported file yet**, which is why this is a decision rather than a constant: the operator has Classroom templates, and the first import of one settles it. Recorded rather than deferred because the direction of a wrong guess matters and this one is safe -- reading minutes as minutes when they were seconds produces a limit that is too _generous_, so no correct submission fails and the error is visible the first time an author opens the limits screen. The opposite reading would have failed correct submissions with a timeout, which is the failure nobody diagnoses. `SECONDS_PER_CLASSROOM_TIMEOUT_UNIT` is the one place to change. |
| DEC-136 | **An import writes nothing until the author has read what could not be carried over (X-001)** | The parser is a pure module the _browser_ runs, and the screen shows its report -- test by test, with a reason in words -- before any endpoint is called. That ordering is the whole design rather than a nicety: a Classroom assignment whose tests are decided by pytest, or compared by a regular expression, has no ReCodEx equivalent at all, and an importer that silently skipped those would produce an exercise which _looks_ imported and grades a fraction of what it should. The other half of the same decision is that the result is deliberately **not assignable** -- core-api refuses an exercise with no reference solution (T-011), a template does not carry one, and the screen says so rather than letting the author discover it when they try to assign it. **What was rejected:** a one-click import, and a fallback that turns a framework-run test into a single exit-code test. The second is technically possible -- ReCodEx has success-exit-code configuration -- and was left out because collapsing twenty cases into one pass/fail is a decision only the author can make, so the report states the option instead of taking it. |
| DEC-137 | **The entry point is resolved at submit, defaulting to the first file by name, and only asked for when there is more than one (PF-016)** | An exercise config may bind `entry-point` to the sentinel `$entry-point`, which makes it a **submit-time** variable: core-api then refuses any submission that does not carry `solutionParams`, with one file or twenty. This app sent none, so the moment the seeded exercise was configured correctly its own submit screen stopped working -- the gap was invisible only because no exercise here had ever demanded the variable. **The rule is legacy's, adopted deliberately rather than reinvented** (`SubmitSolutionContainer.getEntryPoint` / `needsToSelectEntryPoint`): always send one when `pre-submit` declares the variable, defaulting to the alphabetically first uploaded file; show a picker, and block the submit until it is answered, only when the solution has **more than one file**, because with one file the default is the only answer and asking would be a question with a single option. **Where this app differs from legacy is where the value lives**: the form field holds the reader's _choice_ and the action resolves choice-or-default, so a file removed after being picked clears the pick rather than naming a file core-api no longer holds. `preSubmitSolution` reports this as `entryPointEnvironments` -- a list of environments, not a variable table -- because `entry-point` is the only submit-time variable ReCodEx defines and a general table would have columns nothing reads. **What was rejected:** sending the first file unconditionally without a picker (it guesses which file is the program, and a wrong guess surfaces as a failed test rather than an error -- the worst kind), and deriving the value inside the field with an effect (it makes "the reader has not chosen yet" indistinguishable from "the reader chose the default"). **The rule is applied twice**: S-014's student form and T-011's reference-solution form both submit, both have their own pre-submit and their own action, and the second was found by its spec rather than by review -- worth remembering as the shape of this app, where "the submit path" is two paths. |
| DEC-138 | **The seed's multi-file fixture is several real files, not a ZIP, because a ZIP cannot be graded (PF-016)** | The seed submitted one solution as `solution.zip` to produce the `zipEntries` the source viewer expands, and two specs were built on it. With the exercise configured correctly that submission is **refused**: `Solution::getFileNames()` reports the uploaded name, not the entries inside, so the exercise's `source-files` wildcard `*.py` matches nothing and core-api answers "None of the submitted files matched regular expression '*.py'". That is upstream behaviour and not a limitation of the seed -- it was simply invisible while the broken configuration skipped wildcard matching altogether, and the ZIP fixture never graded either. **The fixture is now `main.py` + `greeting.py`**, which keeps every property it was used for -- one solution, several files, names that pair with no other attempt, so G-005's diff viewer and G-030's hand-pairing still have their case -- and adds two: it grades (10/10, verified), and it is the only seeded submission that reaches DEC-137's entry-point picker, which appears exactly when a solution has more than one file. **The entry point is the first file _listed_, not the first by name**, and the distinction is not academic: `greeting.py` sorts before `main.py`, so deriving it the way the form does seeded a solution that imports the program and never runs it -- found as a fixture scoring zero. **What was rejected:** loosening the exercise to `source-files: "solution.*"` so the ZIP still matched (it keeps the fixture at the cost of making the seeded exercise unlike a real one -- ReCodEx's own python3 default is `*.py` -- and a hand-uploaded `main.py` would then not be detected at all), and leaving the ZIP fixture to fail and filing it (it breaks `pnpm seed` on a clean instance, which is the one thing the seed must not do). **What is lost:** nothing on this deployment exercises a ZIP submission's display any more. |
| DEC-139 | **The guides are markdown files in this repo, not entries in `messages/`** (X-002) | A message catalogue is for interface strings; these are documents of a few hundred lines each, with tables and fenced commands in them. As JSON they would be one escaped line apiece, unreviewable in a diff and unreadable as a whole -- and the one thing that matters about a document is reading it end to end. They sit in `content/docs/<slug>.<locale>.md`, one file per guide per locale, and render through the same `Markdown` component as an exercise text, which is what the FAQ already does with an operator-hosted file (G-024). The cost is that both locales are maintained by hand rather than caught by the catalogue test: constraint 6 is enforced here by `getGuide()` falling back to English and by `docs.spec.ts` asserting the Czech and English headings of the same guide, not by `messages.test.ts`. |
| DEC-140 | **An organizational group offers neither students nor exams, though core-api forbids only the first** | Measured rather than assumed, against the running instance: adding a student answers `400 - It is forbidden to add students to organizational groups`, while setting an exam period answers `200` and stores it. So the students tab was offering an action that can never succeed — a defect — and the exams tab an action that succeeds and does nothing, since an exam locks students into a group and this kind has none. Both are hidden. The students half needed no decision; this entry is for the exams half, where the product is deliberately narrower than the API. The settings tab stays visible either way: it is the only place the organizational flag can be turned off again, and hiding it would strand the group in that state. **The sidebar's "My teaching" leaves them out for the same reason**: core-api inherits admin rights down the tree, so an administrator of a department is an admin of every group beneath it, and the operator's own sidebar listed two containers next to the one course he actually teaches -- under a heading that says he teaches them. They stay reachable through the group list, the parent link and the breadcrumbs. **Only the sidebar filters**: `getMyGroups().teaching` still reports the rights as they are, because the exercise screens build their "new exercise in" and "fork into" offers from it and an organizational group is a perfectly good home for an exercise -- it is students, not exercises, that it cannot have. Found by the operator building his own group tree on a fresh instance. |
| DEC-141 | **A data-only exercise gets its one test written for it, and is asked about neither tests nor scoring** | core-api makes no special case for an exercise whose only environment is `data-linux`: the same validation demands at least one test, because a test is the unit its judge runs in. So a teacher collecting essays was sent to a screen that asked them to name tests and write a score expression -- machinery they have no use for, standing between them and an assignable exercise. Saving the environments now writes a single test when there is none, and the tests and scoring sections are hidden for such an exercise. **The test is named `Data`, in ASCII, and is not translated**: core-api's test names are `[-a-zA-Z0-9_()[].! ]`, so the Czech name this shipped with first was refused outright (`Invalid Argument 'tests' - test name contains illicit characters`) -- measured against the running instance, `Odevzdání` → 400, `Data` → 200. The teacher may rename it; what matters is that it exists. **What was rejected:** teaching the validation list to say something else for a data-only exercise (it explains a demand instead of removing it), and hiding the per-test configuration too (a data-only exercise still needs its judge, which is the one thing that decides how a collected file is marked). Found by the operator building a non-programming assignment on the fresh instance. |
| DEC-142 | **The sidebar offers only what the reader's own role may open, and the floors are read out of `permissions.neon`** | Three links on a student's sidebar -- the user directory, the exercise catalogue and the pipelines -- ended in the refusal page, which reads as a broken product rather than as a permission. The floors are core-api's: `user.viewAll` from `supervisor` up (**not** `user.viewList`, which a student does have -- that is the different action that made the link look plausible), `exercise.viewAll` and `pipeline.viewAll` from `supervisor-student` up. Checked against the **effective** role, so a narrowed session is offered what that role may reach; same shape as `canSeeAdminSection`, and the same caveat -- this is not the authorisation boundary, core-api refuses the pages themselves. **What is not hidden:** the group list, which a student may open and which is how they find a course to join. Verified against the running deployment by issuing restricted tokens for each role and reading the rendered `<nav>`: a student gets the dashboard, the groups and their profile; `supervisor-student` gains the exercises and pipelines; `supervisor` gains the directory. **That measurement needed a long-lived token to be valid at all** -- `proxy.ts` refreshes anything within a day of expiry, and core-api's `issueRefreshedToken` passes `null` for the effective role, so a narrowed session silently becomes the full account again. Upstream behaviour, worth knowing before trusting any view-as test. |
| DEC-143 | **The interface wears the department's own colours, and dark mode is a choice rather than a fate** (X-006) | The operator's word for the previous look was impersonal, and it was: shadcn's neutral defaults, `primary` a near-black, nothing on any screen saying whose deployment this is. The brand is inf.upol.cz's, read off its computed styles: `#016BAB` is `primary` in light mode and the hue of `accent`, `ring` and `info`, so the four read as one family; dark mode gets a lighter tint of the same hue (`oklch(0.75 0.11 240)`) because the site has no dark mode to copy and the light value fails contrast on a dark ground. The neutrals are untouched -- their contrast was measured when they were chosen, and the brand asks nothing of them. **The mark's letters are `currentColor`**, not the site's `#1d1d1b`: a black wordmark on a dark ground is invisible, and the dot and the diagonal carry enough of the blue to be recognisable either way. **IBM Plex Sans comes through `next/font/google`**, which downloads the files at `next build` and serves them from this app -- no request to Google from a reader's browser, and no layout shift, at the cost of needing network during the build. Both places that build (CI and the Docker image) already fetch from the network in the same step, so that cost is nil today; if a build ever has to run offline, `next/font/local` over the OFL files is the way out. **The appearance switch is three states**, because `defaultTheme="system"` was already the behaviour and a two-state toggle would have quietly taken it away from everybody who never touched it. It sits where the language switch sits -- the sidebar's footer and the anonymous header -- since both are preferences rather than actions on the page. Its pressed state renders only after mount (`useSyncExternalStore` with a constant snapshot, not an effect setting state, which the compiler's lint refuses): `next-themes` resolves the stored choice on the client, and a server that guessed would be wrong for every reader who chose. |
| DEC-144 | **A breadcrumb trail is the way back up: it starts where the reader can go, and it is not shown when it would only repeat the title** (X-007) | Two rules the manifest already half-followed. **Every dynamic leaf whose address hides its parents carries `ancestors`** -- assignments and shadow assignments did, solutions did not, and a reader arriving at a solution from a review queue had _Solutions_, unlinked, as the whole of their context. The cost is the two reads the page makes anyway: `/v1/assignment-solutions/{id}` for the assignment, `/v1/exercise-assignments/{id}` for the course. **The current page's crumb stays** -- the WAI-ARIA pattern names the current page (`aria-current="page"`) and `group-invitations.spec.ts` reads it -- but it uses the words the `<h1>` uses. **A trail that is only the current page is not rendered:** it repeated the title a line above it on every top-level screen, and a trail exists to lead somewhere. The threshold is the crumb count, not the route, so a section page under a resolved parent still shows that parent. |
| DEC-145 | **Accessibility is enforced at serious and critical, and a syntax token must read against its own block** (X-009) | axe-core's WCAG 2.x A/AA rules run over five screens in both colour schemes, and only findings axe rates serious or critical fail the test; moderate and minor ones are printed. The threshold is deliberate: axe adds rules between versions, and a suite that goes red because a dependency learned a new opinion trains people to ignore it. What is enforced is what a reader would actually hit -- contrast, names, roles. **Token colours are corrected in the palette, not in a stylesheet override.** The two Shiki themes ship colours that were chosen on their own backgrounds, and one of them (github-dark's comment grey) does not reach 4.5:1 there. `lib/code/palette.ts` now mixes any token towards the opposite pole in tenths until it reads, against the theme's own block background (`#fff`, `#24292e`), and keeps a colour that already reads exactly as written -- so the class names and the emitted rules are unchanged for every token that was fine, and the tests that pin them still pin them. A `.dark .shiki .tk…` override would have named a hash and broken on the next theme. |
| DEC-146 | **A binary submission is offered, not rendered, and the list names binaries rather than text** (operator's testing round) | core-api's file listing carries a name and a size and no MIME type, so the only thing to judge a file by before fetching it is its name. `lib/code/binary-files.ts` enumerates the **unreadable** families -- documents, archives, images, media, compiled artefacts, fonts, databases -- and treats everything else as text. Inverting it would have been the obvious shape and the wrong one: source files have endless extensions, and a list of readable types silently refuses the first `.zig` anybody submits. Correctness does not rest on the list being complete -- a type nobody named is still fetched and caught by core-api's own `malformedCharacters`, and offered rather than shown as mojibake. The list exists to avoid _downloading_ forty megabytes to learn that. |
| DEC-147 | **Only the most specific sidebar link is highlighted** (operator's testing round) | Active state was any item whose path contained the current one, and `/groups` contains every group page -- so opening a course lit both "All groups" and the course, on a teacher's sidebar and a student's. `lib/nav/active-href.ts` keeps the longest containing href. The behaviour worth preserving through that change is the fallback: when the open group is not in the sidebar at all (an admin in somebody else's course), `/groups` is the only match and still lights. |
| DEC-148 | **A file's fold is a `<details>`, not React state** (operator's testing round) | A submitted file can be thousands of lines and the operator asked to fold them away. The browser owns exactly that state -- keyboard toggling, find-in-page reaching inside, no hydration -- and lifting it into React would have meant a client component wrapped around every file _and_ around its highlighted tokens, the largest thing on the page and the one worth never shipping to the client. "Expand all" therefore sets `open` on the elements directly, by a `data-source-file` marker. The one thing that had to be repaired alongside it: a link into a collapsed `<details>` scrolls nowhere in browsers without the auto-expanding behaviour, so the file index above opens its target on `hashchange`. |
| DEC-149 | **A teacher's points are a state, not a footnote** (operator's testing round) | `overriddenPoints` is set only when a person decided the figure, so `evaluationStatus` reports `overridden` and the row stops saying "10/10 · Špatně". It replaces the three score-derived states and only those: `correct`, `partial` and `incorrect` all answer "what did the scoring make of it", which is the question an override settles differently, while a compilation failure or a lost job is what happened to the _run_ and no award changes that. A bonus is deliberately not this — it adds to the automatic result rather than replacing it. The field is absent from the group stats row, so the two summary screens built on it keep the automatic verdict rather than guess; the class table looks it up in the assignment's own solutions, beside the test tally it fetches there anyway. |
| DEC-151 | **A group carries permission hints, so the import is offered on one rather than on the reader's role** (X-015) | The bulk import was gated on `viewer.role === "superadmin"` in three places, and the operator asked the question that exposes it: how is a teacher meant to get students into a course when the students have no accounts yet? DEC-110's reasoning was sound where it was made -- a _user_ payload carries no hints, so "may this person invite anybody at all" has nothing to ask -- and it does not transfer to "may this person invite into _this group_", which core-api answers on every group payload. `PermissionHints::get` reflects any public `can*` method taking one required argument, and `canInviteStudents(Group)` is one, so `inviteStudents` has been on the wire all along; `lib/api/group-detail.ts` already passes the whole hint map through as `can`. **Measured rather than assumed**, against the running deployment, one role per process -- `BasePermissionPolicy::$membershipCache` is static and keyed by group id alone, so four identities evaluated in one process all get the first one's answer, and a probe that ignores this reports `false` for everybody. A direct supervisor gets `inviteStudents` from `supervisor-student` up; `addStudent` needs `supervisor`, and is **absent from the hints entirely** because `canAddStudent(Group, User)` takes two arguments. That gap is real and handled: somebody in it may open the screen and be refused the directory search, so existing and new accounts cannot be told apart and core-api's "email already taken" becomes the answer the row reports. **`organizational` is checked separately**: the hint is `true` for an organizational group even though `RegistrationPresenter` refuses every invitation into one, so the hint alone would have offered a screen on which every row fails. An import with no `?group=` at all stays the superadmin's -- nothing scopes it. |
| DEC-152 | **A spreadsheet is read here, narrowed here, and dropped into the box the reader already checks** (X-015) | The operator's STAG export is a thirty-six-column database dump in a binary `.xls`, and the import took pasted text. Three things had to be true at once: the file must be readable as downloaded, nothing unexamined may be sent, and the screen's existing preview -- which is the whole reason the import is safe -- must keep working. Choosing a file therefore reads it **in the browser**, narrows it to the six columns that mean something, and writes tab-separated text into the same `<textarea>`; `parseRoster` is untouched and the file never leaves the machine. **The narrowing is an inversion of the paste rule and has to be**: pasted text treats an unknown column as an identifier, which is right for a table somebody typed and catastrophic for this one -- thirty columns would become identifiers, `stav` is `S` for every student, and identifiers are unique per service, so every row after the first would report thirty collisions. What was dropped is printed, because a silently discarded column is the one way this could lose something without saying so. **No dependency**: `xlsx` is the only mainstream reader of the legacy binary format and the maintained build is not the one on npm, which is not a thing to install in a deployment holding student records. `lib/users/spreadsheet.ts` implements the subset a table of text uses -- OLE2 sectors, the shared string table **including strings split across a `CONTINUE` boundary**, RK and MULRK, plus OOXML over `DecompressionStream`, an HTML table (which portals of this kind serve under an `.xls` name) and delimited text, decided by the first bytes rather than the extension. Every fixture is built byte by byte in the test rather than committed, since the real export carries a student's name, address and study number. |
| DEC-153 | **The catalogue's group filter answers "assignable in", while its Group column answers "stored in"** (X-016) | The two are different questions and the screen shows both, because a teacher asks the first and needs the second to make sense of the answer. core-api's `filters[groupsIds][]` is not a membership test: `getPreparedForPaginationGroupsFilter` expands the id through `groupsIdsAncestralClosure` before matching, so filtering by a lab returns what its course and its faculty hold as well. Measured on the deployment -- the child group returns its parent's five exercises, an unrelated branch returns none. The column, by contrast, prints `groupsIds`, which is attachment and nothing more; the field was on the wire all along (`ExerciseViewFactory.php:60`) and only the local type failed to declare it. **That combination is correct and reads as a bug**, so filtering prints one sentence above the table saying the column may name a different group than the filter did -- the same reasoning as the assign screen's picker, which has relied on this closure since G-010 without ever explaining it to anybody. **Names come from `getGroupList`**, already built on the memoized `/v1/groups` fetch, so the column costs no round trip; a group the reader cannot see is absent from that list and prints as nothing rather than as a UUID, which is what `GroupListEntry.path` already does for an invisible ancestor. **The filter's options are the immediate parent and the name**, not the chain from the instance root -- that chain is a hundred characters here and `group-info.tsx` already settled on showing one parent; the whole path stays on the option's `title`. The options are the groups where `membership` is `teacher`, so inherited administration counts (DEC-150): the question is what this person may act on, not whose courses these are. |
| DEC-154 | **The catalogue's group filter is a combobox that keeps a match's ancestors on screen** (X-022) | A `<select>` could not answer the question being put to it. Its options are a flat list of `parent / name` strings, so a reader hunting for `KMI/JP` had to recognise the course and its seminar groups by name alone, with nothing to say that one sits inside the other -- which is the whole of the decision, because filtering by the course returns what any of its seminar groups would return and more (DEC-153). So: a `cmdk` combobox in a Radix popover, for the same reason DEC-054 and the command palette chose it -- combobox keyboard and `aria-activedescendant` semantics look finished long before they are correct. **A query is matched against the full path, not the name** (`lib/groups/group-search.ts`), which is what makes one search return a course and everything beneath it; the survivors then go through `groupRows`, the assign offer's own tree flattener (T-012), so every match arrives under the containers it hangs in and the indentation is the real hierarchy rather than an artefact of which rows survived. Headings are `aria-hidden` decoration -- a listbox may hold nothing but options -- and the chain they draw is on each option's own `aria-label` instead, so a screen reader hears `Výuka / KMI/JP - Jazyk Python / 2025/26` where the eye reads indentation. Matching folds case and diacritics but **keeps punctuation**: course codes are written `KMI/JP`, and stripping the slash would turn that query into a fuzzy match on every path containing those letters. **The screen stays usable without JavaScript**: the native `<select>` is what the server renders and what a reader without scripting keeps, and the combobox replaces it on the first client render -- `useSyncExternalStore` rather than a `setState` in an effect, which is both what React provides for a value that differs between server and client and what `react-hooks/set-state-in-effect` insists on. | Indenting `<option>` elements with non-breaking spaces (rejected -- it renders inconsistently across browsers, cannot be searched, and would still leave the reader typing nothing); a `<datalist>` (rejected -- native, no-JS and no dependency, but it submits the typed _text_ rather than an id, shows no hierarchy, and its presentation is entirely the browser's); showing only matches without their ancestors (rejected -- that is the current filter with a search box, and it withholds exactly the relationship the operator asked to see) |

**Distinguishing this from Q-003's original, real violation**: that one was F-001 scaffolding this app's actual source files (`package.json`, `app/`, `docs/`) directly into the compose repo's own git-tracked tree -- i.e., vendoring this repo's _history_ into the compose repo's _history_, exactly what brief §1 forbids ("do not vendor your frontend's source inside the compose repo"). This relocation is different in the way that actually matters: `repos/` is gitignored by the compose repo (confirmed: `repos/` is the first line of its `.gitignore`), so nothing about this repo's source ever enters the compose repo's own git history or commits -- this repo remains, in every sense the brief's §1 cares about, "its own repository." What changed is purely which _local filesystem path_ a `git clone`/`pull-repos.sh` happens to check it out to on a given machine, which was never the thing §1 was protecting against.

**Practical consequence for future sessions**: every `../ReCOdex/...`-style relative path reference written into this repo's own docs/comments _before_ this decision assumed the old sibling layout and is now one directory level short (the compose repo root is `../../` from here, not `../`, when checked out via `pull-repos.sh`). Fixed the still-actionable references (`docs/DECISIONS.md`, `docs/SEED_ACCOUNTS.md`, `lib/auth/session-cookie.ts`, `scripts/seed.ts`) to say "the compose repo's ..." instead of hardcoding a specific relative depth -- robust to this or any future relocation, matching the pattern `AGENTS.md` already used. Left `docs/PROGRESS.md`/`docs/BACKLOG.md`'s own historical entries describing _what was true when they were written_ untouched, since rewriting those would misrepresent the actual history of an append-only log; this entry and the `AGENTS.md` note are what a future reader should rely on for the current layout, not old log entries. `AGENTS.md` itself updated with an explicit note about the new two-levels-up relationship, since it's the one file read at the start of every future session. | Leaving this repo as a sibling directory and only fixing `pull-repos.sh`/docker-compose.yaml for the _old_ path (rejected -- the operator explicitly asked for it to live under `repos/`, matching the other fetched repos exactly, not to merely automate cloning a sibling); moving this repo's actual Dockerfile out to the compose repo's `services/web-next/` to fully match the upstream `services/<name>/Dockerfile` + `COPY repos/<name>/...` pattern the brief originally described (considered, rejected for now -- DEC-026's original reasoning for a self-contained Dockerfile still holds independent of where the source happens to be checked out: this repo's own CI already builds and tests this exact Dockerfile standalone, which would regress if the Dockerfile moved into the compose repo and started assuming a `repos/web-next` COPY source that only exists there; revisit only if a concrete reason to unify the pattern shows up); rewriting old `../ReCOdex`-style path references in `docs/PROGRESS.md`/`docs/BACKLOG.md`'s historical entries to match the new layout (rejected -- those entries are accurate records of what was true when written; correcting them would be revising history, not fixing a stale reference). |

**One real bug found live, the same category as D-003's two**: a first version defined the Zod schema in the same file as the `"use server"` action (natural, since brief says "shared schema"). This produced a genuine runtime error -- `zodResolver` throwing "Invalid input: not a Zod schema" -- because a `"use server"` file's compiler pass only handles (async) function exports; the co-located schema object is silently replaced with something else by the time the client imports it. Fixed by moving the schema into its own plain module, imported by both the client form and the action file -- documented prominently in `use-server-action-form.ts`'s own doc comment so the next form-building ticket doesn't rediscover this the same way.

**Verified live** (both `next dev` and, specifically given D-002/D-003's pattern of Docker-only failures, a rebuilt Docker `standalone` container) via a real demo Server Action exercising all four paths in one flow: client-side Zod validation (empty submit shows a field error, no server round trip); a server-only field error ("taken" -> per-field message a client schema alone couldn't produce); a server-only form-level error ("forbidden" -> the `root` error, rendered by `FormError`); a success path; and the dirty-guard's tracked state genuinely flipping from clean to dirty on input. | Next's native `<form action={serverAction}>` + `useActionState` for these forms too (rejected -- that's the right pattern for _simple_ forms per the brief's own stack table row ("Complex forms: React Hook Form + Zod"), but two systems both trying to own form submission and error state would conflict; RHF's `handleSubmit` calling the action as a plain function sidesteps this entirely); building every field type (select, checkbox, textarea, radio) now (rejected -- no concrete consuming form has asked for them yet; `TextField` covers what's needed today, more added the same way when a real ticket needs one); a custom Link-click interceptor to approximate in-app dirty-guard navigation blocking (rejected -- checked the bundled docs first and found no supported primitive to build on; a hand-rolled interceptor would need to independently rediscover every way a navigation can start and would drift out of sync with the router, exactly the kind of fragile workaround the brief's own verification discipline argues against). |

**Two real bugs found live, both by actually building and testing this rather than assuming the design was right on paper:**

(1) A first version passed `columns` (with `cell`/`sortValue`/`filterValue` function props) from a Server Component debug page straight into `<DataTable>` -- a real "Functions cannot be passed directly to Client Components" runtime error, since React can't serialize a function across the RSC boundary. Documented prominently in the component's own doc comment: `columns` must be defined inside a `"use client"` wrapper the caller owns (the Server Component fetches `data`, a small client component defines `columns` and renders `<DataTable>`), not passed down from the page itself. This is the shape every future S-/T-/AD- list-view ticket will need to follow.

(2) A local `next dev` session and even a local ad-hoc `next build` (run without the debug route present at the time) both looked clean, but rebuilding the Docker image with a debug route present failed at prerender: "useSearchParams() should be wrapped in a suspense boundary." `next dev` has no static-generation step to enforce this against, so it silently never caught it -- only a real production build did, and specifically the Docker build this project already treats as the authoritative production check (same category of dev/production divergence as DEC-039's `request.url` bug). Fixed by having `DataTable` wrap its own `useSearchParams()`-reading implementation in an internal `<Suspense fallback={null}>`, so every future caller just imports `DataTable` and it works, rather than every consuming ticket needing to remember its own boundary.

**Verified live**, specifically re-verified against a rebuilt Docker `standalone` container after the `Suspense` fix (not just `next dev`, given finding #2 above): sort cycles correctly through ascending -> descending -> cleared across three header clicks, each state reflected in the URL; pagination advances and is reflected in the URL; a debounced filter narrows the dataset, composes correctly with pagination (11 matches, pageSize 10 -> exactly 10 rows shown), and resets to page 1; clearing the filter restores the full set; selecting a row toggles its checkbox and leaves the URL completely unchanged; the header "select all" checkbox correctly selects every row on the current page. | Server-side pagination/sort/filter query params sent to core-api directly (rejected -- no ticket has confirmed whether core-api's list endpoints even support this per-entity yet; premature for a Design System ticket that predates every actual list-view ticket); persisting sort order in `localStorage` like the legacy `SortableTable` (rejected -- exactly the pattern brief §9 calls out wanting corrected: "Deep-linkable everything... Sharing a URL reproduces the exact view", which `localStorage` state can't do); URL-syncing row selection alongside sort/filter/page (rejected -- selection is transient bulk-action UI state, not a view worth bookmarking, and could grow unboundedly long); requiring every caller to wrap `<DataTable>` in its own `<Suspense>` (rejected once the internal-wrapping alternative was implemented and verified -- strictly better ergonomics for every future consumer, no downside). |

---

## Deferred

| #           | Decision                                       | Reason for Deferral                                                                                                                        | Revisit When                                     |
| ----------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------ |
| DEF-001     | **`cacheComponents` and `partialPrefetching`** | Brief §4: essentially every byte is per-user and permission-dependent. Implicit caching is wrong bet until app exists.                     | After parity is reached, static shell identified |
| DEF-002     | **React Compiler**                             | Brief §4: measure first. Enable only after profiling shows benefit.                                                                        | After performance baseline established           |
| DEF-003     | **Graphviz rendering approach**                | Brief §7: decide early but budget significant time. Port viz.js initially, evaluate alternatives.                                          | After pipeline editor is functional              |
| ~~DEF-004~~ | ~~**WebSocket for evaluation progress**~~      | **Resolved in S-016 (DEC-065): both.** The page refreshes itself; the monitor socket adds task-by-task progress where a channel id exists. | Closed                                           |
| DEF-005     | **Extension token handoff**                    | Brief §7: investigate, don't silently drop or hand out token.                                                                              | After SIS-ext-webapp mechanism understood        |

---

## Dropped

| #        | Item                             | Reason                                                                                       |
| -------- | -------------------------------- | -------------------------------------------------------------------------------------------- |
| DROP-001 | **SKIN config**                  | AdminLTE color skin. Replaced by proper Tailwind dark/light theme. Recorded in `DROPPED.md`. |
| DROP-002 | **Redux, redux-form, immutable** | Legacy state management. Replaced by Server Components, Server Actions, React Hook Form.     |
| DROP-003 | **moment.js**                    | Deprecated. Replaced by `date-fns` + native `Intl`.                                          |
| DROP-004 | **moment-timezone**              | Replaced by server-side absolute dates + client-side relative formatting.                    |
| DROP-005 | **Webpack/Babel**                | Replaced by Next.js built-in Turbopack. No custom build config needed.                       |

---

## Assumptions (Tagged `IA-ASSUMPTION`)

| #       | Assumption                                | Where Used                        | If Wrong                                   |
| ------- | ----------------------------------------- | --------------------------------- | ------------------------------------------ |
| ASS-002 | New app binds to port `3000`              | Dockerfile, compose               | Update `PORT` env and compose port mapping |
| ASS-004 | Single instance deployment                | Auth BFF, `instanceId` handling   | Add instance selector to login flow        |
| ASS-008 | SMTP not configured, use `mail.debugMode` | Registration/password reset flows | Configure real SMTP, test end-to-end       |

**ASS-007 was right and is now verified** (D-005), so it has left this table: the 512 MiB ceiling is read directly from the compose repo's `services/proxy/nginx.conf.template` and `services/api/nginx-site.conf` (`client_max_body_size 512M`) and `services/api/php-recodex.ini` (`upload_max_filesize`/`post_max_size = 512M`), and lives in one place in code (`MAX_UPLOAD_BYTES`, `lib/upload/limits.ts`). Worth noting core-api's own `actionStartPartial()` allows a _larger_ file than that (1 GiB) -- this deployment's nginx/PHP layer is the binding constraint, not the API, so raising the ceiling means changing the compose repo, not this one.

**ASS-001, ASS-003, ASS-005, ASS-006 were wrong** and are removed from this table rather than
corrected in place — see `QUESTIONS.md`'s Resolved section (Q-001, Q-004, Q-006, Q-010) for the
actual verified values. Removing rather than editing keeps this table honest about what was
_assumed and risky_ versus _known_; the "If Wrong" column already did its job for these four.

**Process note:** all four were guesses about _this specific running deployment_ (its public URL,
its CAS config, its monitor wiring, its API-doc exposure) that `ReCOdex/.env` and
`ReCOdex/docker-compose.yaml` answer directly — no guessing needed. Recon read the legacy
_application source_ thoroughly but not the _deployment_ this app is meant to run alongside. Both
count as "the compose repo" per §1's inputs list, and both need reading before an environment
assumption goes in this table.
